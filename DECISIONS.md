# Decisions Log — Oragrol Website

Permanent record of choices made on purpose, so Claude Code (or future-you) never "fixes" something that was already a deliberate call. Add a new entry any time a real architectural, design, tooling, or content decision is made. Don't duplicate this into PROJECT_MEMORY.md — reference the ID instead.

Format: one entry per decision, numbered sequentially, never delete — mark superseded if reversed.

---

### D-001 — Header has no max-width; background is fully caller-owned
- **Date:** 2026-08-12 (backfilled — decided during Header Fix Pass 2/3, undated in source docs)
- **Context:** Header Fix Pass 2 needed the header to blend into the full-bleed Hero image instead of sitting on a hard black bar, and to stay correctly aligned at large desktop widths. A finite `max-w-*` cap (tried at both 1280px/`max-w-7xl` and 1920px) kept breaking at whatever width was one step larger — confirmed by measuring the logo's x-position jumping from a consistent 48px to 368px once the cap engaged and centered the content instead of pinning it to the edge.
- **Decision:** `NavBar` (`app/components/ui/nav.tsx`) has no max-width constraint at all — `px-6 md:px-12` alone keeps logo/actions a constant distance from the viewport edge, matching the Hero's own edge-to-edge behavior. `NavBar` also no longer bakes in `bg-background`; background is left entirely to the caller via `className`, because `bg-background` (a `background-color`) was fighting a gradient override (`background-image`) at the CSS-property level instead of being replaced by it.
- **Alternatives considered:** A larger finite cap (e.g. `max-w-[2560px]`) — rejected, just moves the breakpoint where the same bug reappears, doesn't fix it. Verified clean at 1280/1440/1920/2560px via Playwright screenshots.
- **Status:** Active

---

### D-002 — Header logo is an inlined SVG rebuild of the source file, not a direct file import
- **Date:** 2026-08-12 (backfilled — decided during Header Fix Pass 3, undated in source docs)
- **Context:** Header Fix Pass 2 asked for the header logo to use the real `Oragrol_Logo_Final.svg` file directly rather than a hand-coded approximation. A prior pass (`OragrolRing` + separately hand-typed text in a flex column) guessed at spacing/scale and didn't match the source file's own gap/proportions.
- **Decision:** `OragrolLogo` (`app/components/brand/oragrol-logo.tsx`) inlines the source SVG's own three graphic elements (ring `<circle>` + both `<text>` nodes) with their exact source coordinates (cx/cy/r/stroke-width/dasharray/rotate, x/y/font-size/letter-spacing) untouched, cropped to a measured bounding-box `viewBox`. The only changes from the raw file: cropped viewBox, and fill/stroke swapped from the source's fixed dark-on-white values to CSS custom-property tokens (`--color-accent`, `--color-text-primary`, `--color-text-secondary` — same hex values as the source) so the mark stays legible across the dark/white/light-blue section environments. `Oragrol_Logo_Final.svg` at the project root remains the authoring source of truth; it is not imported directly at runtime.
- **Alternatives considered:** Direct `<Image>`/inline-`<img>` import of the raw SVG file — rejected because the source file's colors are fixed (dark-on-white), which would break on the dark header and any future light section without a token-based recolor.
- **Status:** Active

---

### D-003 — Pricing removed from the live site pending Solutions page (Step 6)
- **Date:** 2026-08-12 (backfilled, undated in source)
- **Context:** `PROJECT_MASTER.md` Step 6 marks package names/inclusions/pricing as unconfirmed and says not to lock/publish them yet. A standalone `/pricing` route (`app/pricing/page.tsx`) and its `pricing-card.tsx` component existed from initial scaffolding.
- **Decision:** Both were deleted. Pricing will resurface only as part of Step 6 (Solutions page), using the "This section is currently being finalized" status language — not as its own route.
- **Alternatives considered:** Keep `/pricing` live with placeholder copy — rejected, an indexed route with no real content ahead of Step 6 isn't worth the SEO/UX cost.
- **Status:** Active

---

### D-004 — Hero stays a single-pass scroll-scrub; no auto-loop, no wrap-around
- **Date:** 2026-08-12
- **Context:** All 16 planned hero frames became available (previously only 8 existed). The request to wire them in mentioned "a short crossfade transition when the sequence loops from frame-16 back to frame-01," which doesn't describe any mechanism the existing scroll-driven `hero.tsx` actually has — it's a one-pass scrub (frame-01 at scroll start, frame-16 at scroll end, then releases to the next section), never a timer-based or wrap-around loop. Asked the user to clarify rather than assume a mechanism change, since it would have meant picking between (a) a genuine auto-playing ambient loop matching `PROJECT_MASTER.md`'s original "seamless loop" language, or (b) extending the scroll range so scrolling further wraps back to frame-01.
- **Decision:** Neither. Keep the scroll-scrub exactly as it already was — frame-01 at scroll start, frame-16 at scroll end, normal release, reverse-scroll just plays the same envelopes backward. No timer, no wrap-around, no crossfade added, because the sequence never cuts from frame-16 directly to frame-01 in this mechanism — the visual mismatch between those two frames (confirmed: frame-01 is a calm dark-blue night sky, frame-16 is a warm orange sky with a teal network overlay) never becomes visible.
- **Alternatives considered:** Auto-playing ambient loop (would match the original brief's language but is a real architecture change to a scroll-scrub that was already tuned and shipped); scroll wrap-around (would mean continuing to scroll down replays the hero instead of advancing to the next section — likely to read as broken/confusing). Both explicitly declined by the user.
- **Status:** Active

---

### D-005 — Hero's `scrollYProgress` forces off framer-motion's ViewTimeline acceleration
- **Date:** 2026-08-12
- **Context:** User reported a visible flicker right at the start of the Hero's scroll range — frame-02 would briefly show, then "correct back" to frame-01. Root-caused (not guessed — verified with a Playwright script sweeping `window.scrollTo` across the full pinned range and reading `getComputedStyle` opacity on every frame's wrapper) to framer-motion's automatic hardware acceleration: `useScroll({ target: sectionRef, offset: ["start start", "end end"] })` — that exact offset normalizes to the library's "contain"/`ScrollOffset.All` preset (see `presets`/`offsetToViewTimelineRange`/`canAccelerateScroll` in `node_modules/framer-motion/dist/cjs/index.js`), which makes `useScroll` set a private `.accelerate` config on the returned `scrollYProgress` MotionValue and hand this Hero's ~32 concurrent derived opacity/scale transforms off to the browser's native `ViewTimeline`. That path desyncs badly with this many concurrent derived transforms sharing one source: measured frame-01 opacity going non-monotonic — correctly declining through ~0-300px of scroll, then climbing back to a stuck `1.0` for any scrollY beyond ~2250px, simultaneously with frame-16 also reading `1.0`. The hand-derived math in `frameEnvelope`/`frameOpacityStops` was proven correct throughout (verified against framer-motion's own `interpolate()` clamp/per-segment-easing source) — the bug is entirely in the acceleration path, not the project's own formula.
- **Decision:** Immediately after the `useScroll()` call in `Hero()`, delete `scrollYProgress.accelerate` (same property the library itself conditionally sets — not a private field invented here) to force the MotionValue onto framer-motion's plain JS/rAF-driven update path. Verified via the same Playwright sweep that this restores exact agreement with the hand-calculated breakpoints (e.g. frame-02 now peaks at scrollY≈210px, matching `center = 0.09375 × 2250px ≈ 211px`) and eliminates the non-monotonic behavior at every scroll depth tested, including well past the pinned section's end. Must be done synchronously in the render body at that exact point, not in an effect — framer-motion sets `.accelerate` synchronously during `useScroll`'s own render pass, before any of its internal effects run; a `useLayoutEffect` on our side would fire after useScroll's internal one and could race the attachment. `react-hooks/immutability` is disabled for that one line with an inline justification (MotionValue is a mutable, ref-like object owned by framer-motion, not React-tracked state).
- **Alternatives considered:** Changing the `offset` to avoid the preset match — rejected: `normaliseOffset` compares the *normalized* semantic range, so any offset expressing the same "full target scroll-through" (`[[0,0],[1,1]]`) matches "contain" regardless of string vs. array syntax; avoiding the match would require a different, slightly-wrong range (e.g. an epsilon-offset hack) rather than a clean fix. No public `useScroll` option to disable acceleration was found in the library's exports.
- **Watch for:** This is a project-wide gotcha, not Hero-specific — any future `useScroll({ target, offset })` call whose offset normalizes to one of the four named presets (`entry`/`exit`/`cover`/`contain`) will silently get the same acceleration, and with it the same risk of desync if it drives more than a couple of simultaneous derived transforms. Apply the same `.accelerate = undefined` pattern if that happens again.
- **Status:** Active

---

### D-006 — Hero motion (16-frame sequence) frozen as-is; known quality limitations intentionally accepted for now
- **Date:** 2026-08-12
- **Context:** After D-004 (all 16 frames wired in) and D-005 (scroll-mapping bug fixed and verified monotonic), the Hero motion is now mechanically correct but still has known limitations in visual quality, smoothness, 3D depth, cinematic presentation, cybersecurity connection, and overall premium/modern feel relative to the brand bar set in `PROJECT_MASTER.md` (Bell/CrowdStrike/Apple-level restraint and polish).
- **Decision:** Explicitly stop here. This is a deliberate decision to unblock the rest of the website, not an oversight or something left unfinished by accident. Do NOT attempt to fix, regenerate, replace, or further iterate on the hero frames or motion approach unless explicitly instructed by Mohammad. The hero will be revisited later, likely via a fundamentally different approach — e.g. a single strong hero image with code-driven motion, true video generation, or another higher-quality motion technique — rather than incremental polish of the current 16-frame scroll-crossfade.
- **Alternatives considered:** N/A — this is a scope/sequencing decision (defer non-blocking polish to keep moving through the build sequence), not a technical alternatives comparison.
- **Status:** Superseded by [[D-057]] (2026-08-18) — the 16-frame sequence this decision froze has been replaced entirely (ring perimeter-scan visual, explicitly instructed and approved). Historically, this also superseded the framing in D-004's/PROJECT_MEMORY's earlier "hero frame resolution" open question.

---

### D-007 — Services page: Lucide icons (not custom), hub page only for this pass
- **Date:** 2026-08-12
- **Context:** Step 5 (Services Page) inspection found no custom iconography prepared anywhere in the project — every icon used site-wide so far (Home teaser, Approach, Trust, Solutions) is a stock Lucide icon via the shared `Icon` wrapper, even though `PROJECT_MASTER.md` names "custom iconography" as a design goal for this page. Also found `PROJECT_MASTER.md` describing two distinct layers of scope: the main `/services` hub page, and a separate 8-page "individual service page template" (Hero/Problem/What Oragrol Does/What You Get/How It Works/Who It's For/Related Services/CTA) — a much larger, separately-reviewable body of work.
- **Decision:** (1) Ship the hub page with refined Lucide icons, consistent with the rest of the site — same pattern as [[D-006]] (accept a known gap now, revisit deliberately later rather than block progress on an unavailable asset). (2) Scope this pass to the `/services` hub page only. The 8 individual service detail pages are explicitly deferred to a separate future step, not built or stubbed as part of this pass.
- **Alternatives considered:** Commissioning/generating custom icons before building the page — rejected for now, matches the hero precedent of not letting a visual-polish gap block forward progress through the build sequence. Building the hub page + all 8 detail pages together — rejected as too large a unit to review in one pass; user explicitly chose to defer detail pages.
- **Status:** Active — superseded in part by [[D-008]] on the icon question specifically: Services' schematic-linework treatment is the intended direction going forward, Lucide icons remain the stopgap until that's built.

---

### D-008 — Per-page visual identity system: distinct motif per page, code-generated, same shared material
- **Date:** 2026-08-12
- **Context:** After Home + Services were both built, the user flagged a real design-direction problem: the two pages were differentiated mostly by `Section` background color (dark/white/light-blue) and icon choice, not by actual distinct visual content — a pattern that, if repeated across all 9 pages, would make the site feel like "one template, nine colors" rather than nine purposeful pages. Asked for a proposed visual direction per page before any Step 6 work, explicitly not asking for final art yet.
- **Decision:** Each of the 9 pages gets its own content-specific visual metaphor, expressed as code-generated SVG/CSS (not commissioned imagery, not stock photography) for now, while all pages continue sharing one underlying "material" — the already-locked color tokens, typography, spacing, line-weight/corner-radius language, and `Reveal` motion restraint. Approved per-page directions:
  - **Home** — ring + city skyline, scroll-driven (built; reserved to Home, not reused elsewhere per the existing "never invent a similar ring" rule). Superseded 2026-08-18 by [[D-057]]: the skyline photo is gone, the ring stays (still Home-reserved) as a perimeter-scan visual, no longer scroll-driven.
  - **Services** — systems/schematic linework (connector lines + nodes, blueprint-like). Not yet retrofitted onto the already-built hub page — current icon-only treatment (D-007) is the interim state.
  - **Solutions** — ascending stacked planes / strata, conveying escalating protection levels spatially.
  - **Cyber Health** — instrument-panel data visuals: segmented arcs/gauges (deliberately not closed rings, to keep the ring motif reserved to Home) and bar-based score meters, leaning on JetBrains Mono.
  - **How We Work** — a path/route motif that visually resolves from chaotic to ordered across the 4 stages, literalizing "security clarity."
  - **Industries** — a distinct abstract line-motif per sector, revealed via in-page interaction/selection (not 9 separate pages, per the existing brief constraint).
  - **Resources/Insights** — editorial/publication layout, typography-led, minimal signal/waveform accents.
  - **Company** — architectural/material-study abstractions (structural cross-sections, light studies) — no fabricated people/office photography, since none exists.
  - **Contact** — a single minimal wayfinding line resolving to one point — deliberately the quietest page.
- **Alternatives considered:** Commissioned/generated imagery for every page (like the Hero's 16 frames) — rejected for now as too slow across 9 pages; user chose code-generated for speed and full editability. Photographic treatment for Company/Industries specifically was raised as having a real ceiling code can't reach — user explicitly deferred that as a **separate future decision**, not blocking, same pattern as [[D-006]]'s hero deferral.
- **Status:** Active. Not yet applied to Services (built before this decision). Build order decided: Step 6 (Solutions) first, built with its direction from the start; Services' schematic-linework retrofit comes after, as its own separate pass — not an oversight if Solutions ships first without Services having been touched yet. Superseded in part by [[D-009]]: the retrofit's design direction is now governed by the full Visual Redesign Blueprint, not just this entry's original per-page table, though the per-page motifs listed above (strata, gauge, schematic linework, etc.) remain the approved starting points the blueprint builds on.

---

### D-009 — Visual Redesign Blueprint: approval-gated, Services-first rollout
- **Date:** 2026-08-13
- **Context:** With Home, Services, Solutions, and Cyber Health all built, Mohammad requested a full visual/UX audit of the live site (explicitly AUDIT ONLY — no code changes) to diagnose why the site still reads as "text + flat backgrounds + simple UI" rather than a premium, art-directed cybersecurity site, ahead of any further page work. The audit was scoped to the actual rendered site (Playwright screenshots across 4 pages × 3 breakpoints, interactive-element checks, computed WCAG contrast from locked tokens), not source code alone and not a formatting-reference image supplied alongside the brief (that image was explicitly style-only — no finding from it was used). The brief also introduced a new hero concept, "Aperture O," named as the intended successor to the 16-frame sequence frozen under [[D-006]].
- **Decision:** The audit's output — the **Oragrol Visual Redesign Blueprint** (executive summary, prioritized findings, page-by-page audit, visual-system-gap analysis, three highest-impact changes, three recommended prototypes, full strategy blueprint) — is delivered as a private Claude artifact and also saved verbatim as `VISUAL_REDESIGN_BLUEPRINT.md` in the project root (added 2026-08-13, on request, for plain-text reading/sharing without the interactive viewer). Both require Mohammad's explicit review/approval before any implementation begins. Once approved, the new visual system is built on **`/services` only** first — chosen as the proving ground because it already has a queued retrofit ([[D-007]]/[[D-008]]) and was independently confirmed by this audit to have the clearest version of the site-wide "generic card grid" problem. The system must not extend to Home, Solutions, Cyber Health, or any unbuilt page (Industries, Resources, Company, Contact, How We Work) until Services is explicitly reviewed and approved against the blueprint. Expansion after that is page-by-page, not all at once.
- **Key audit findings worth preserving here** (full detail in the artifact, not duplicated in `PROJECT_MEMORY.md` per this file's own rule): the closing-CTA section is pixel-identical across Home/Services/Solutions; Solutions' strata and Cyber Health's gauge motifs each appear once in their hero and never again on the same page; two computed WCAG AA contrast failures (primary-CTA white-on-accent text at 3.90:1, `text-muted` on dark backgrounds at 4.09:1); a real mobile-nav overlay defect (menu panel doesn't cover the full viewport, hero content bleeds through). The contrast and mobile-nav findings are independent defects, fixable at any time, not gated behind blueprint approval.
- **Alternatives considered:** Rolling the new system out to all pages at once once approved — rejected, matches this project's repeated pattern (hero, then Solutions/Services build order) of proving a direction on one small surface before wider investment, explicitly to avoid a third costly regeneration cycle after the hero and the initial Services/Solutions "plain" builds.
- **Status:** Active. Blocking: no implementation of the blueprint on any page until Mohammad approves it.

---

### D-010 — Two independent audit defects fixed: mobile nav overlay, WCAG AA contrast
- **Date:** 2026-08-13
- **Context:** [[D-009]]'s audit found the mobile nav overlay defect (#5) and two contrast shortfalls (#6, #7) were real bugs independent of the blueprint's design-direction work, not gated behind blueprint approval. Mohammad confirmed to fix both immediately.
- **Decision — mobile nav overlay:** `SiteHeader`'s mobile panel (`app/components/site/site-header.tsx`) was an in-flow `max-height` accordion capped at `28rem`; once open, page content below its own bottom edge (including a second "Get Your Cyber Health Score" button) was visible underneath it. Changed to a `fixed` full-viewport overlay (`top-20` to `bottom-0`, matching NavBar's `h-20`), opaque `bg-background`, `overflow-y-auto` for short viewports. Added `inert` + `aria-hidden` when closed (prevents keyboard/AT focus reaching hidden links — not present before) and a body-scroll lock while open (`document.body.style.overflow = "hidden"`, restored on close). Verified: body overflow toggles correctly, `panel.inert` is `true` when closed, no bleed-through at 390px, zero console errors.
- **Decision — contrast:** Rather than change the locked brand `--accent` (#018ABE, used everywhere — links, icons, borders, focus rings) or the shared `--text-muted` raw value (already ≥4.5:1 on White/Light-blue), added two new, narrowly-scoped tokens in `tokens.css`: `--accent-strong` (`--palette-blue-700`, `#017CAC`, same hue family, ~4.7:1 with white text — used only by `Button`/`ButtonLink`'s `primary` variant fill, replacing `bg-accent` with `bg-accent-strong`) and an `.env-dark`-only override of `--text-muted` to `--palette-gray-450` (`#75828D`, ~5.0:1 on `#0A0A0A`, vs. the old 4.09:1 — White/Light-blue environments keep the original `--palette-gray-500` since it already passes there). Verified computed button fill is `rgb(1, 124, 172)` with white text at build time.
- **Alternatives considered:** Darkening `--accent` globally — rejected, it's the locked brand primary and used in far more places (links, icons, focus outlines, brand mark) than just button-fill text contrast; a global change would be a much bigger, unreviewed visual shift for a fix that only needed to touch one component. Lightening the shared `--text-muted` raw value — rejected for the same reason; White/Light-blue environments don't have this problem and shouldn't be touched to fix Dark.
- **Scope check:** `bg-accent` usages other than `Button`/`ButtonLink` (the pentest-addon "+" badge icon, tier-cards progress bar fill, checkbox/radio form controls) were reviewed and left unchanged — none of them are text-on-fill; the badge is a graphical icon (WCAG's 3:1 non-text threshold applies, already cleared), the rest are pure decorative fills with no text on top.
- **Status:** Active. Verified via `npx tsc --noEmit`, `npm run lint`, `npm run build` (all clean) plus Playwright screenshot/computed-style verification; screenshots sent to Mohammad.

---

### D-011 — Prototype 2 built (Services schematic-linework, one row); discovered a `--color-*` token gotcha
- **Date:** 2026-08-13
- **Context:** Blueprint approved; per its own sequencing, Prototype 2 (one Services capability row in the schematic-linework treatment) was built for review before rolling out to all 8 rows.
- **Decision — the prototype:** New `SchematicVisual` (`app/components/sections/services/schematic-visual.tsx`) — a hub-and-spoke blueprint diagram (2 inputs converging orthogonally into a central hub, 1 output dropping out) on a faint dot-grid background, same `color-mix` fill-progression technique as StrataVisual. The Lucide service icon sits on the hub as an absolutely-positioned HTML overlay (SVG itself stays pure decoration/`aria-hidden`, same convention as Strata/Gauge). Applied to exactly one row — "Risk Assessment & Compliance" (n=02) — via `SchematicRowPrototype` (`app/components/sections/services/schematic-row-prototype.tsx`), copy copied verbatim from `live-services.tsx`, nothing invented. Shown on a new noindex, nav-unlinked preview route, `/services/prototype`, next to the current live row for direct comparison — same disposable-preview pattern as `/style-guide` — **not wired into the live `/services` page.**
- **Decision — the discovered bug:** Building this surfaced a real, previously-latent bug: `var(--color-surface)` / `var(--color-border)` (the Tailwind `@theme inline`-namespaced tokens) do not reliably track a `Section`'s per-environment override when read directly via `var()` outside Tailwind's own utility-class generation — confirmed by computed-style inspection (rendered `#141414`/Dark's surface even inside an `.env-white` Section, while the underlying `--surface`/`--border` semantic tokens correctly read `#f4f4f5`/`#e4e4e7` at the same element). StrataVisual/GaugeVisual never hit this because both only ever render inside `environment="dark"`, where Dark's values happen to equal the buggy fallback — this is the first code-generated SVG motif built for a White/Light-blue section. Fixed in `SchematicVisual` by consuming `var(--surface)`/`var(--border)` (the raw semantic tokens) instead of the `--color-*` names; `--color-accent`/`--color-accent-light` are unaffected (constant across all three environments by design, not repointed per-Section, per `section.tsx`'s own comment).
- **Action needed later, not done here:** `StrataVisual` and `GaugeVisual` still use `var(--color-surface)`/`var(--color-border)` — harmless today (both Dark-only), but worth the same swap if either component is ever reused in a non-Dark context, so the same bug doesn't resurface silently. Not fixed in this pass — out of scope for Prototype 2, flagging so it isn't forgotten.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean; Playwright screenshots at desktop/mobile on `/services/prototype`, zero console errors, zero horizontal overflow; confirmed `/services` itself renders unchanged (no regression, since no existing file was modified for this prototype — only new files added).
- **Status:** Superseded by [[D-012]] — the prototype was approved and the treatment is now live on all 8 capabilities.

---

### D-012 — Services schematic-linework retrofit shipped (all 8 capabilities); D-008 Services retrofit now COMPLETE
- **Date:** 2026-08-13
- **Context:** Mohammad approved Prototype 2 ("genuinely good") and asked for the treatment on the remaining 7 capabilities. The 8 capabilities span two structurally different sections — `LiveServices` (4 full-width alternating rows, the layout the prototype used) and `AdditionalCapabilities` (4 compact cards, deliberately smaller/no-CTA since those aren't live yet — an existing content-governance choice, [[D-007]]). Asked Mohammad which he meant before touching either; answer: keep both structures, adapt the schematic motif to each rather than unifying them into one layout.
- **Decision:** `LiveServices.tsx`'s remaining 3 rows (Virtual CISO n=01, Vulnerability Assessment & Management n=03, Security Awareness Training n=04) now use the exact same `SchematicVisual` + icon-overlay + `FIG.0X` tag pattern the approved prototype used for n=02 — all 4 live rows are now visually consistent. `AdditionalCapabilities.tsx`'s 4 cards keep their existing compact grid, no-CTA, "Capability currently being finalized" badge — the live/not-yet-live visual-weight distinction is preserved — but the plain Lucide icon is replaced with a new compact variant, `SchematicMark` (same hub-and-connector language, reduced to two short corner ticks, no dot grid, no third output node — the full diagram doesn't fit a small card's icon slot proportionately), so the visual language still carries through without overstating readiness. Both new components live in `schematic-visual.tsx`.
- **Cleanup:** The `/services/prototype` comparison route and its dedicated `SchematicRowPrototype` component are deleted now that the treatment is live — no longer needed. `.next` cache cleared to drop the stale route from `next/type` validation after deletion.
- **Result:** The D-008 Services schematic-linework retrofit ([[D-007]], [[D-008]]) is now COMPLETE — Services no longer runs the pre-D-008 icon-only treatment flagged throughout the 2026-08-13 audit ([[D-009]]) as the site's single largest visual-system gap.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (8 routes, `/services/prototype` confirmed gone — 404 on a freshly restarted dev server); full-page Playwright screenshots at desktop (1440px, real incremental scroll to trigger all `Reveal`s) and mobile (390px), zero console errors, zero horizontal overflow.
- **Status:** Superseded in part by [[D-013]] — a quality correction to this same retrofit, reviewed live and found not to meet the bar.

---

### D-013 — Services Design Correction: prototype round (not yet applied to live page)
- **Date:** 2026-08-13
- **Context:** Mohammad reviewed the shipped D-012 schematic-linework retrofit live in-browser and found it doesn't meet the bar — "still small icon decoration on a flat background, not real graphic design" (`SERVICES_REDESIGN_PROMPT.md`, supplied via `C:\Users\60183\Downloads\`). The brief supplied 3 concrete Dribbble references (QClay, Cybershield, Tokenex) and 7 specific, checkable requirements (scale, light-based depth, hero-scale illustration, 3D/soft-shaded icon style, large numerals, mixed/tilted charts where applicable), plus process requirements: use 21st.dev/Magic MCP if available (flag explicitly if not), benchmark against the references, state technique before generating, prototype one row first, show side-by-side before wider rollout.
- **Process followed:** Opened and viewed all 3 references directly (Chrome). `21st` MCP was unauthenticated — asked Mohammad how to proceed; he chose to authorize it (OAuth flow completed). Used `get_inspiration`/`search` to find real components, then pulled real code via `get_component` for **"Card with Glow" (id 59, author ibelick)** — a `motion/react` conic/radial-gradient glow layer — adapted into `glow-effect.tsx`: swapped its 4-color rainbow demo palette for the locked accent family only (no new primary color), and defaulted `mode` to `"static"` (the original's other modes loop infinitely, which conflicts with this project's own documented motion rule in `tokens.css` — flagged, not silently applied). Stated the full technique plan to Mohammad in chat before writing any component code.
- **Decision — what was built:** A prototype-only round, same "one row first" discipline as D-011/D-012. New components: `glow-effect.tsx` (above), `hero-schematic-visual.tsx` (a ~40%-larger variant of the shipped `SchematicVisual`, left as a separate file so the live D-012 component isn't touched), `capability-spotlight.tsx` (the full row: the visual now sits in a real nested `.env-dark` panel — a dark, shadow-elevated card floating on the White-environment row, the trait all 3 references share — with the `GlowEffect` layer plus a second SVG-blurred rim glow behind the hub, an oversized low-opacity background numeral using the row's real index, and a soft-3D icon badge built from a radial-gradient fill and layered box-shadow — CSS-simulated depth, not literal 3D geometry, per Mohammad's explicit choice over sourcing real 3D-rendered assets). Shown on `/services/prototype` (recreated, noindex) next to the currently-shipped D-012 row and a written 3-reference benchmark table — not wired into the live `/services` page.
- **Color note:** every gradient/glow uses only the locked accent family. White/black appear once, mixed into the accent as tonal highlight/shadow inside the icon badge (standard material-shading technique) — flagged per the brief's own color rule, not applied silently.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean; Playwright screenshots at desktop (1280px) and mobile (390px), zero console errors, zero horizontal overflow; confirmed no existing shipped file was modified (only new files added) — `/services` itself is unaffected.
- **Status:** Superseded by [[D-014]] — the prototype was approved and the treatment is now live on all 8 capabilities.

---

### D-014 — Design Correction shipped to all 8 capabilities; /services/prototype removed
- **Date:** 2026-08-13
- **Context:** Mohammad reviewed the D-013 prototype live in-browser — "this is a real improvement, meets the brief" — and approved it for the remaining 7 capabilities, "adapted per their existing size/weight distinction as before" (the same LiveServices-rows-vs-AdditionalCapabilities-cards split D-012 already established). Mid-rollout, Mohammad asked for a side investigation into a third-party site (`ui-ux-pro-max-skill.nextlevelbuilder.io`, presented as a Claude-Code-integrated design-reference database with a `uip`/`uipro init` setup command) to check for anything that would improve the approved D-013 prototype further, before touching the other 7 rows. Its "How it Works" page hung the browser tab twice (30s timeouts, unresponsive renderer) before any setup command could even be seen. Did not run any install/init command from the unverified domain — flagged that as a standing rule regardless of the hang (executing setup scripts from an unfamiliar third-party source without being able to see what they do first isn't something to do unilaterally). Mohammad chose to skip it and proceed with the already-approved 21st.dev-based version.
- **Decision — generalized the prototype components:** `capability-spotlight.tsx` was rewritten from the single hardcoded "Risk Assessment & Compliance" prototype into two reusable, parameterized components — `CapabilitySpotlightVisual({ n, icon })` (the full dark glow-lit panel, for `LiveServices`' 4 rows) and `CapabilitySpotlightMark({ icon })` (a smaller, simpler version — no schematic diagram, no numeral — for `AdditionalCapabilities`' 4 cards, preserving the live/not-yet-live visual-weight distinction from D-007/D-012). `live-services.tsx` and `additional-capabilities.tsx` both updated to use them in place of D-012's `SchematicVisual`/`SchematicMark`.
- **Cleanup:** `/services/prototype` and its now-superseded standalone usage deleted — no longer needed now the treatment is live. `.next` cache cleared to drop the stale route from Next's type validator.
- **Result:** All 8 Services capabilities now use the Design Correction visual language (nested dark `env-dark` elevated panels, `GlowEffect`-driven light depth, soft-3D icon badges, large real-index numerals on the 4 live rows; a compact version of the same language on the 4 finalizing cards). D-012's flatter schematic-linework treatment is fully retired.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (8 routes, `/services/prototype` confirmed 404 on a freshly restarted dev server). Full-page Playwright screenshots at desktop (1440px, real incremental scroll) and mobile (390px), zero console errors, zero horizontal overflow.
- **Status:** Active.

---

### D-015 — Container widened site-wide; Services typography hierarchy + real hero
- **Date:** 2026-08-13
- **Context:** Four-part instruction, Services-scoped except item 1 (explicitly global): (1) a reported "no max-width, dead gutters at 1920/2560px" bug; (2) capability-row headline vs. field-label hierarchy read too close in weight; (3) audit the shipped panels for "schematic-linework" connector/circle drift, reportedly reviewed-and-rejected earlier; (4) Services' hero had no illustration, unlike Solutions/Cyber Health.
- **Decision — item 1 (global):** Investigated empirically before changing anything — every section already used a capped `Container`; the actual defect was a mismatch between the header (deliberately full-bleed, D-001, to match the Hero's own edge-to-edge image) and body content capped at only 1024px (`lg`) — at 2560px the header's nav sits near the true viewport edge while content floats in a much narrower centered island. Fixed at `app/components/ui/container.tsx`: `lg` 1024px→1280px, `xl` 1280px→1440px — both inside the requested 1280–1440px range, two tiers preserved (not collapsed into one). Global by necessity (shared component, used by every page) — verified zero horizontal overflow and no visual breakage on Home/Solutions/Cyber Health at 1440/1920/2560/768/375px afterward, per the explicit "verify it did not break..." instruction. Some header-vs-content gutter mismatch remains at very wide viewports — inherent to a full-bleed header (D-001, not reopened here) plus any capped-width content; this narrows it substantially, doesn't eliminate it.
- **Decision — item 2 (Services-scoped):** Added opt-in `size` variants to the shared `H3`/`Caption` components — `H3` `size="lg"` (`text-3xl md:text-4xl font-bold`, up from the default `text-2xl md:text-3xl font-semibold`) and `Caption` `size="sm"` (`text-[10px] tracking-[0.18em]`, down from the default `text-xs tracking-widest`) — defaults unchanged everywhere else (same non-breaking pattern `H1`'s existing `size` prop already uses), applied only in `live-services.tsx`'s row headline and `ServiceField` labels.
- **Decision — item 3 (audit, no change):** Checked whether the shipped panels' connector-circle elements were D-013-approved or reintroduced drift. They are `HeroSchematicVisual`, built as part of D-013 itself and visible in every screenshot from both of D-013's approvals (single-row prototype and the full 8-row rollout) — no record of a separate connector/circle motif being reviewed and rejected; what *was* rejected was D-012's flatter pre-glow treatment as a whole. Flagged this discrepancy rather than silently acting on the stated premise, per this project's own "if you still think a past decision is wrong, say so explicitly and ask" rule. Mohammad confirmed after seeing the finding: keep as-is, no change needed.
- **Decision — item 4 (Services-scoped):** Built `ServicesNetworkVisual` (`network-visual.tsx`) — 8 nodes (one per capability, the real count from the H1's own "Eight capabilities," not invented) converging to one hub, literalizing "one coordinated approach," in a glow-lit elevated panel (`GlowEffect`, same D-013 material). Deliberately a different composition from `HeroSchematicVisual`'s per-row diagram, not a bigger copy of it — `ServicesHero` (`hero.tsx`) rebuilt to a 2-column text|visual layout matching Solutions/Cyber Health's established hero pattern; previously text-only since Step 5.
- **Held, not done this round:** Item 5 (regrouping the 8 capabilities into categories, benchmarked structurally — not visually — against a Bell Business reference) is explicitly deferred pending a separate scope confirmation, per Mohammad's own instruction not to block the visual fixes on it.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean. Playwright screenshots at 1440/1920/2560/768/375px across `/`, `/services`, `/solutions`, `/cyber-health` — zero horizontal overflow anywhere, no visual regressions found on the three pages this wasn't meant to touch.
- **Status:** Active.

---

### D-016 — Container "fix" was real but insufficient; sidebar nav resolves it; stronger typography; hover motion (+ a real Tailwind bug found and fixed)
- **Date:** 2026-08-13
- **Context:** Round 2 of Services review. Mohammad reported D-015's container fix "did not actually resolve the empty gutters," with an explicit instruction not to report success from code inspection alone.
- **Decision — item 1, re-investigated:** Confirmed on a fresh production build (`.next` cleared, `npm run start`, not dev-server) that the D-015 fix genuinely computes `max-width: 1280px` at every tested width — it was never reverted or broken. The real issue: **math, not a bug.** A 1280px cap centered in a 2560px viewport always leaves 640px per side, by definition; 1920px leaves 320px. D-015 only moved the cap from 1024px→1280px, a 128px/16% reduction — real but small enough to not look different at a glance. "Capped at 1280–1440px" and "no visible gutter at 2560px" are mathematically incompatible on any monitor wider than the cap. Presented this with screenshots and exact DOM measurements before touching anything further, then asked Mohammad how to resolve it rather than guessing. He chose: **use item 3's new sidebar nav to occupy the gutter space** rather than widening the cap further.
- **Decision — items 1+3 (built together):** `LiveServices`' outer wrapper widened from `Container size="lg"` (1280px) to a custom `max-w-[1680px]` grid (`[220px_1fr]`) — `CategoryNav` (new, `category-nav.tsx`) now occupies real width in what was previously empty margin, instead of chasing the max-width number itself. `CategoryNav` lists all 8 real capabilities (4 live + 4 finalizing — copied verbatim from each section's own data, nothing invented), jump-scrolls via real DOM `id`s (`capability-01`...`capability-08`, added to both `LiveServices` rows and `AdditionalCapabilities` cards, with `scroll-mt-28` so the fixed header doesn't obscure the target), and tracks the active item via `IntersectionObserver`. Active state reuses `NavLink`'s existing accent-color convention (not a new pattern) plus a left accent-bar. Verified: DOM measurements at 1440/1920/2560px, zero horizontal overflow at all 5 tested widths, a real click-scroll test (correct target scrolled to, correct active state updated), and confirmed the nav is hidden on mobile (`hidden lg:block`) as intended.
- **Decision — item 2 (typography, strengthened):** D-015's first pass (H3 `size="lg"`, Caption `size="sm"`) was judged still not enough contrast. Strengthened in place (same components, same non-breaking `size`-variant pattern): H3 `lg` bumped `text-3xl/4xl`→`text-4xl/5xl`; Caption `sm` tracking widened `0.18em`→`0.22em`; body copy (`ServiceField`'s `Text`) bumped `size="sm"`(14px)→`size="base"`(16px) for a third, clearly distinct tier. Added a small accent-colored eyebrow ("Capability 0X") above each headline for a real color jump, matching the *spirit* of "large bold headline in the accent color" from the Bell reference while preserving the site's own existing rule that full headlines stay `text-primary` (accent is reserved for eyebrows/active-states/CTAs everywhere else on the site — recoloring an entire H3 would be an unprecedented, unrequested brand-system change; the eyebrow achieves the same hierarchy effect without it).
- **Decision — item 4 (hover motion) — real bug found and fixed:** Added `group`/`group-hover` hover states to `CapabilitySpotlightVisual` (panel border/shadow lift, icon badge scale+brightness). Verified via computed-style inspection (not just visual screenshot) and found the panel's box-shadow — written as a Tailwind arbitrary value with `color-mix()` nested inside (`shadow-[0_30px_80px_-24px_color-mix(in_srgb,...)]`) — generated **no CSS at all**, silently: the *base* shadow was invisible, not just the hover state. Root cause: Tailwind v4's arbitrary-value bracket parser doesn't reliably handle a `color-mix()` with its own internal commas nested inside a shadow utility's brackets. Fixed by switching to Tailwind's built-in `shadow-{color}/{opacity}` mechanic (`shadow-2xl shadow-accent/30`, hover `shadow-accent/50`) — the same mechanism `bg-accent`/`text-accent`/`border-accent` already use for the registered `accent` theme color, no hand-rolled arbitrary value needed. Also discovered (not a bug, a Tailwind v4 behavior change worth documenting): `hover:-translate-y-1`/`group-hover:scale-110` compute via the standalone CSS `translate`/`scale` properties in Tailwind v4, not the composed `transform` property — a naive `getComputedStyle(el).transform === 'none'` check reads as "broken" when it isn't; confirmed actually-correct behavior by reading `translate`/`scale` directly. On-scroll motion (fade+drift as each row enters viewport) was already satisfied by the existing per-row `Reveal` usage — not rebuilt.
- **Verified overall:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean; fresh production server; zero horizontal overflow at 1440/1920/2560/768/375px; zero non-pre-existing console errors (23 pre-existing `/industries`, `/insights`, `/company` nav-prefetch 404s found and confirmed unrelated — those pages don't exist yet, Next.js `<Link>` prefetches them from every page site-wide regardless of this round's changes, not a regression).
- **Held:** Item 5 (footer overhaul, site-wide) explicitly done separately per Mohammad's own instruction — see [[D-017]].
- **Status:** Active.

---

### D-017 — Footer overhaul, site-wide (separate commit per instruction)
- **Date:** 2026-08-13
- **Context:** D-016's item 5, held back and done separately. Brief: Company/Resources/Legal 3-column grid (structure only from a Bell Business reference, not its visual skin), a "Blog" nav item, LinkedIn + Instagram social icons (no X/Twitter yet), and a floating "Under attack?" emergency CTA — with an explicit instruction to flag rather than invent anything not already in the project docs (Blog/Resources page existence, real social URLs, real emergency contact).
- **Investigation before building (per instruction):** Checked `PROJECT_MASTER.md`/`DECISIONS.md`/`PROJECT_MEMORY.md` and found: "Blog" isn't this project's real name for that page — the actual planned page is "Resources / Insights" (Step 10, still PENDING/not built), already linked as "Resources" → `/insights` in the header nav and prior footer. No LinkedIn/Instagram URLs or emergency-contact info anywhere in any project doc — the *existing* footer already had LinkedIn as an explicit `href="#"` placeholder with its own code comment flagging the same gap. Presented all of this to Mohammad rather than guessing, and asked directly for the missing values instead of inventing them.
- **Real values supplied by Mohammad (2026-08-13):** LinkedIn — `https://www.linkedin.com/company/oragrol-global/` (real, now live in the footer). Instagram — no real page yet; left as an explicit `href="#"` placeholder with a code comment, same pattern as the pre-existing LinkedIn gap. Emergency contact — `+60 18-377-2761`, explicitly supplied as a **TEMPORARY** placeholder, to be replaced with a real Canadian contact line before public launch — flagged with an explicit code comment (not a silent number, not to be removed without confirming the real line first). Blog/Resources — keep the existing "Resources" label, route to `/insights` (not introducing separate "Blog" terminology for the same not-yet-built page).
- **Decision — structure:** `SiteFooter` rebuilt with three real nav columns — Company (Services, Solutions, Cyber Health, Company, Contact), Resources (just the one real item — deliberately not padded with invented links to look fuller), Legal (unchanged). `Grid`'s `md` column map didn't support 5 columns (needed for brand-block-span-2 + 3 nav columns); added a `5` entry — additive, non-breaking, doesn't affect any existing `cols` usage.
- **Decision — social icons:** `lucide-react` 1.31.0 has no brand/social icon exports (documented in the prior footer's own comment). Built small hand-drawn outline glyphs (`social-icons.tsx`) matching the site's existing icon convention (thin stroke, `currentColor`, matte) rather than the platforms' literal filled trademark logos — consistent with the "matte, not glossy" brand direction already used everywhere else, not a new pattern.
- **Decision — emergency CTA:** New `EmergencyCta` component, fixed bottom-right, rendered from `RootLayout` (not `SiteFooter` itself) so it's genuinely site-wide and independent of scroll position — matches "floating... separate from main footer." Stays inside the locked cyan/navy palette, no new urgency-red color introduced, per the explicit color-governance instruction.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean; fresh production server; zero horizontal overflow at 1440/1920/390px across `/`, `/services`, `/solutions`, `/cyber-health` (footer + emergency CTA are site-wide, checked accordingly); zero non-pre-existing console errors; confirmed via DOM query that LinkedIn/Instagram/emergency `href`s all resolve to the exact values above, not placeholders left silently wrong.
- **Status:** Active. Two explicit flags carried forward, not resolved: Instagram has no real URL yet; the emergency contact number is a temporary placeholder pending a real Canadian line.
- **UPDATE (2026-08-15, D-045):** The placeholder Malaysian number itself removed — see D-045. The "no real Canadian line yet" flag is now moot (there's no phone number shown at all); Instagram's `href="#"` placeholder is untouched, still open.

---

### D-018 — Round 3: content-area headline moved into place, sidebar weight, panel shrunk; footer discrepancy investigated and ruled out as a code bug
- **Date:** 2026-08-13
- **Context:** Mohammad reviewed D-016/D-017 live. Items 1-3: the typography fix still didn't read as real hierarchy — no large headline lived in the actual content area (Challenge/Does/Get/Outcome side), only small labels next to body text. Sidebar read as a quiet side note next to the Bell reference. The visual panel was judged too large relative to the content it sat beside. Item 4: Mohammad's own screenshot showed neither Instagram nor the emergency CTA, contradicting commit b2a7717's message — flagged as a real discrepancy requiring investigation before re-claiming anything fixed, "the second round where 'verified' didn't match what Mohammad actually saw."
- **Decision — item 4, investigated first:** Confirmed via four independent methods, not just a re-run screenshot: (1) read the actual current `site-footer.tsx`/`emergency-cta.tsx`/`layout.tsx` source — both are unconditionally rendered, no hidden logic; (2) checked for stray/duplicate server processes on the machine — only one process was ever listening on port 3000; (3) fetched the raw server HTML via `curl` (zero browser/cache involved) and confirmed both "Instagram" and "Under attack" markers are present in the actual bytes the server sends; (4) loaded the page in a brand-new Playwright browser context (fresh cache/cookies) and confirmed both elements via `getBoundingClientRect`/computed style (non-zero size, `visibility: visible`, `opacity: 1`) plus a direct screenshot. Root cause is very likely a stale browser tab/cache on Mohammad's side (predating either the commit or a since-cleared build), not a defect in the shipped code — but this can't be verified with certainty without seeing his browser directly, so it's reported as the likely explanation, not a certainty. Killed all node processes, did a fully clean rebuild (`.next` cleared) and fresh `npm run start` before this round's screenshots, specifically to rule out any staleness on this end too.
- **Decision — item 1:** The row's headline (`H3`) lived only in the *visual* column, physically apart from the content column it was meant to anchor. Moved the accent eyebrow + headline into the content column, directly above the 4-field grid — same capability name already shown in the sidebar, nothing new written. Removed the now-redundant duplicate from the visual column (kept only the CTA button there). Three tiers are now unambiguous within one column: headline (`H3 size="lg"`, `text-4xl md:text-5xl font-bold`) > body (`Text size="base"`, 16px secondary) > category label (`Caption size="sm"`, 10px, wide-tracked, muted).
- **Decision — item 2:** Added a `size` variant to the shared `NavLink` component (same non-breaking opt-in pattern as `H1`/`H3`/`Caption`'s existing `size` props — default unchanged everywhere else it's used sitewide) — new `lg` tier (`text-base font-semibold`, up from `text-sm font-medium`), applied only in `CategoryNav`. Active-state color/border logic untouched, exactly as instructed.
- **Decision — item 3:** `CapabilitySpotlightVisual` capped at `max-w-md` (448px) within its grid column (was unconstrained, filling the full column). Row grid ratio adjusted from an even `md:grid-cols-2` to `md:grid-cols-[0.9fr_1.1fr]`, giving the content column real additional width rather than just leaving empty space in the visual's own column. No internal geometry/proportions of the panel itself touched, per "scale down a bit, don't redesign."
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean; fresh production server (`.next` cleared, all stray node processes killed first); zero horizontal overflow at 1440/1920/2560/768/375px; zero non-pre-existing console errors. Screenshots taken and reviewed directly (row hierarchy, sidebar close-up, full page at 1920px, mobile) before reporting — not claimed from code alone.
- **Status:** Active.

---

### D-019 — Round 4: sidebar/label weight reduced, CTA consistency fixed; headline font-family bug investigated and NOT found
- **Date:** 2026-08-13
- **Context:** Mohammad reviewed live and flagged four items: sidebar nav rendering bold (wants regular/medium), a suspected font-family bug where capability headlines look like the same face as body text just bigger/bolder, category labels rendering bold, and inconsistent CTA button text/href across the 4 live capability rows. Item 2 was framed explicitly as "the same category of miss as the responsive bug from Round 1 (looked fixed in code, wasn't fixed in the browser)" — an instruction to check the actual computed value, not just the source.
- **Decision — item 2, investigated, no bug found:** Checked the browser's actual computed `font-family` for every one of the 8 capability headlines (not just the one Mohammad named) on a fresh, cache-cleared production build. **Before: `"Space Grotesk", "Space Grotesk Fallback"` on all 8. After (no code changed): identical — `"Space Grotesk", "Space Grotesk Fallback"` on all 8, font-weight 700/600.** Body text on the same page computes to `Inter, "Inter Fallback"` — genuinely a different font-family, not a shared inherited value. `H3`'s source already carries `font-heading` (→ `--font-space-grotesk` per `tokens.css`) in every code path checked. No fix was made because no defect was found — inventing a change here would have been cosmetic noise, not a fix. Reported the actual computed values per the explicit instruction, rather than asserting "fixed." Most likely explanation for the visual complaint: Space Grotesk and Inter are both geometric/grotesque sans-serifs that can look closer than expected at a glance in bold weight, or — given this is the *second* round in a row where what Mohammad saw didn't match what the code/browser actually computes (see D-018's footer investigation) — a stale browser tab/cache remains a real possibility worth him ruling out with a hard refresh.
- **Decision — item 1:** `NavLink`'s `lg` size tier (added D-018 for `CategoryNav`) dropped from `font-semibold` to `font-medium` — computed weight confirmed 600→500 before/after. Active-state color (`text-accent`) and left-border indicator (in `CategoryNav`, unrelated to `NavLink` itself) untouched, exactly as instructed.
- **Decision — item 3:** `Caption`'s `sm` tier (used only by `ServiceField`'s category labels) dropped from the shared `font-medium` (baked into `Caption`'s base string, applying to every `Caption` usage sitewide) to a per-tier `font-normal` — required extracting weight out of the base string into each tier (same reasoning as `H3`/`NavLink`'s existing size variants: avoids two conflicting font-weight utility classes landing on the same element via `cn()`'s plain concatenation). Computed weight confirmed 500→400 before/after; `default` tier (every other `Caption` usage sitewide — page eyebrows, etc.) explicitly kept at `font-medium` in its own tier string, so nothing else changes.
- **Decision — item 4:** `LIVE_SERVICES[1]` (Risk Assessment & Compliance) was the one outlier among the 4 live rows — `{ label: "Get Your Cyber Health Score", href: "/cyber-health" }` while the other 3 already used `{ label: "Talk to Oragrol", href: "/contact" }`. Changed to match exactly, both label and href (leaving the href pointed at `/cyber-health` under new "Talk to Oragrol" text would have been a mismatched link — implies a contact conversation, actually launches an assessment). Confirmed via DOM query that all 4 rows now emit the identical `{ text: "Talk to Oragrol", href: "/contact" }` pair. Position (CTA directly below/beside its own row's visual, same margin) was already consistent per-column; the alternating left/right column swap between rows is the pre-existing, deliberately-approved editorial-row pattern from D-012/D-013 — not touched, flagged in the report in case that's what "position" also meant, rather than assumed.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean; fully clean rebuild (`.next` cleared, stray node processes killed) before any of this round's screenshots, given D-018's stale-cache lesson; zero horizontal overflow at 1440/1920/768/375px; zero non-pre-existing console errors; all four items confirmed via direct DOM/computed-style query, not just a screenshot glance; full-page screenshot showing all 8 capability panels captured and reviewed for the CTA-consistency check specifically, per instruction.
- **Status:** Active.

---

### D-020 — Round 5: CTA-alignment structural fix; sidebar weight genuinely split by active/inactive state
- **Date:** 2026-08-14
- **Context:** Mohammad reported the "Talk to Oragrol" CTA sits offset left of the schematic graphic panel on a 24" monitor but not on a 14" laptop, hypothesizing the button isn't nested inside the same container as the graphic — with an explicit instruction to check the code directly rather than assume. Also reported the sidebar still reads as uniformly bold, with a precise spec this time: active item medium/semi-bold (500-600) with its existing color/border kept, every inactive item regular (400) and muted gray — with an explicit instruction to confirm computed weight in-browser for both states, not just the source.
- **Decision — item 1, root cause confirmed by reading the code first:** The CTA *was* already a child of the same wrapping `<div>` as `CapabilitySpotlightVisual` — Mohammad's specific hypothesis wasn't quite right — but a real structural bug existed one level down: `mx-auto max-w-md` was applied only to the visual panel itself (self-centering it at 448px inside a grid column that's far wider than 448px on a 24" screen), while the `ButtonLink` sibling below it carried no matching width constraint and simply started at the column's own left edge. On a 14" laptop the column happens to be close enough to 448px wide that the gap isn't visible; on a 24" monitor the column is much wider, so the button visibly sits left of the centered panel. Fixed by making a single shared `<div className="mx-auto max-w-md">` the actual parent of both the visual and the CTA, moving the width/centering off the visual's own `className` — one constraint declared once, not two elements independently trusted to carry matching classes (which is how they drifted apart the first time). No viewport-relative margin used, per instruction.
- **Verified — item 1:** Playwright, real production server, actual 1440/1920/2560px viewports: `getBoundingClientRect()` on the row's panel and CTA confirms `leftDelta: 0` (exact left-edge alignment) at all three widths — not just at 1440. Screenshots reviewed directly (not just measured) at all three widths, particularly 2560px where the original bug was most visible.
- **Decision — item 2:** D-019's fix (`font-semibold`→`font-medium`) had applied one uniform weight to every sidebar item regardless of active/inactive state — which is why the list still read as "all bold": a uniformly-medium list is still uniformly heavier than surrounding body text, just less so than uniformly-semibold. Round 5 replaces the uniform weight with a genuine per-state split, scoped to `NavLink`'s `lg` tier only (CategoryNav's exclusive size — the `default` tier used by the site header/footer nav is untouched): active items get `font-semibold` (600, top of the requested 500-600 range) plus the existing `text-accent` color, unchanged; inactive items drop to `font-normal` (400) and switch from `text-text-secondary` (near-black in the White environment — not actually gray) to `text-text-muted` (the semantic muted-gray token, already WCAG-audited passing on White in D-010, ~4.84:1). `CategoryNav`'s own left-border active indicator is untouched, exactly as instructed.
- **Verified — item 2:** Playwright, real production server, computed styles read directly per item: active item ("Virtual CISO") — `font-weight: 600`, `color: rgb(1, 138, 190)` (accent). All 7 inactive items — `font-weight: 400`, `color: rgb(107, 114, 128)` (the muted-gray token's actual RGB value) — confirmed identical across every inactive item, not just spot-checked on one.
- **Verified overall:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean; fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`) before any measurement or screenshot; zero non-pre-existing console errors during the verification run.
- **Status:** Active.

---

### D-021 — Round 6: row top-alignment structural fix; Capability 02 dashboard-mockup prototype (v2)
- **Date:** 2026-08-14
- **Context:** Two-part instruction. (1) The capability headline/graphic don't share a common top edge on rows 02/03 — a real structural fix requested (align-items or explicit shared baseline on the row's own grid, not manual margins), verified across at least 3 rows. (2) Mohammad rejected the repeated glow-orb-in-card motif as generic/templated when identical across all 8 rows, citing known guidance that repeated abstract icon motifs read as templated. Direction: visuals should relate to what each capability actually does, favoring real interface/dashboard mockups over abstract icons — the original brief's own "mixed-chart dashboards, large-scale data typography" guidance, never actually built. Instructed to search 21st.dev/Magic MCP and the newly-installed `ui-ux-pro-max` skill for real dashboard/data-viz components, then prototype ONE new treatment for Capability 02 (naturally data/scoring-oriented) at `/services/prototype-v2`, without touching rows 3-8.
- **Decision — item 1, root cause:** The row's grid (`live-services.tsx`) used `md:items-center`, which vertically CENTERS the two columns relative to each other rather than aligning their top edges. Row 01's short one-line H3 happens to make both columns nearly equal height, so centering looked like alignment by coincidence; rows 02/03's longer capability names wrap the H3 to two lines, making the content column taller — `items-center` then shifted the shorter graphic column down to split the difference. Fixed by changing `md:items-center` → `md:items-start` on the row's own grid — both columns now begin flush at the row's top edge regardless of either column's height, a real shared-baseline fix on the grid itself, not a per-row margin patch.
- **Verified — item 1:** Playwright, real production server, `getBoundingClientRect()` on each row's two direct grid children across rows 01/02/03: `delta: 0` (exact top-edge match) on all three, not just one. Screenshots of all three rows reviewed directly.
- **Decision — item 2, research first:** Searched `21st.dev` (`mcp__21st__search`, "security risk score dashboard card dark data visualization") — top result close to the ask was "Financial Score Cards" (id 5409, half-circle progress + strength badge). Pulled its real code via `get_component` and found it's built on a separate "liquid glass" component system (turbulence-displaced `backdrop-filter`, shadcn `Badge`/`Button` primitives, HSL "strength" colors) — none of which exist in this codebase, and this session's own earlier `ui-ux-pro-max` style-domain search had already flagged that exact "Liquid Glass" category as moderate-poor performance and a contrast risk, not a strong choice here. Also ran `ui-ux-pro-max`'s chart-domain search ("risk score gauge radial progress compliance"), which recommended a Gauge/Bullet chart with a numerical value + tier label beside it for "Performance vs. Target" data — exactly the shape of a risk score. Rather than import 21st's unrelated dependency tree, reused a pattern **already built and approved in this exact codebase**: Cyber Health's `OutputShape` illustrative report card (score figure, `RiskBadge` tier pill, category risk-bar breakdown) — genuinely a dashboard-style mockup with real data typography, matching the brief's own ask, just never applied to Services. Content values (78/100, "Medium" tier, all four category names/tiers/fill percentages) are copied EXACTLY from `OutputShape`'s own already-approved illustrative example, not new numbers — same content-governance precedent Cyber Health itself established, with the same "Illustrative example — not a real result" label carried over.
- **Decision — build:** New `CapabilityDashboardVisual` (`capability-dashboard-visual.tsx`) — the score/badge/risk-breakdown content re-themed into Services' own dark `env-dark` elevated-panel shell (same border/radius/shadow-accent/`GlowEffect` language as `CapabilitySpotlightVisual`, for cohesion with the rows that keep the orb treatment), using only existing tokens/primitives (`RiskBadge`, `DataText`, `Caption`, `Text`, risk-tier colors) — no new dependencies. New `/services/prototype-v2` route (noindex, not linked from nav, same convention as D-013's original prototype route) shows the current treatment and this new one stacked with identical Capability 02 copy, so the visual treatment is the only variable. Does not modify or import from `live-services.tsx` — nothing live was touched. Rows 3-8 and `capability-spotlight.tsx` itself untouched.
- **Verified overall:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes, `/services/prototype-v2` included); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow on `/services` and `/services/prototype-v2` at 390px; zero non-pre-existing console errors; full-page screenshots of both routes reviewed directly.
- **Status:** Awaiting Mohammad's decision — approve Prototype B for rollout to Capability 02 (and possibly the analogous treatment for other data/assessment-shaped capabilities), keep the current treatment, or request a different direction. `/services/prototype-v2` stays until a decision is made, then gets deleted (D-014's own precedent for prototype routes).

---

### D-022 — Round 7: Capabilities 04-08 shell rethink, prototyped on Capability 04 (training-progress dashboard)
- **Date:** 2026-08-14
- **Context:** Mohammad disapproved the dark-panel + dot-grid + glow-orb SHELL itself for Capabilities 04-08 — not an icon-level swap like 02/03, the panel formula needs rethinking. Each of the 5 should get a treatment suited to what it actually does, explicitly not one new repeated formula in place of the old one (that would recreate the same problem). Instructed to research via `ui-ux-pro-max` (current B2B SaaS/security panel guidance) and `21st.dev`/Magic MCP (real component blocks matching that guidance) before building, re-reference Bell Business's structure (adapted, not its color skin), prototype ONE of the five first, and state the reasoning trail before building.
- **Research, stated before build (per instruction):**
  - `ui-ux-pro-max --domain style` ("modern B2B SaaS security marketing card panel 2025 bento") surfaced "Bento Grids"/"Bento Box Grid" (modular cells sized to their own content, not one repeated card shape — WCAG AA, "Excellent" performance, 10/10 Tailwind compatibility) and "Dimensional Layering" (elevation via shadow as the depth device) as the current, well-supported options. Explicitly did not reach for "Glassmorphism" (already flagged a contrast/performance risk earlier this session) or "Cyberpunk UI" (neon, glitch, scanlines, "Accessibility: Limited") — neither fits "premium, restrained, professional."
  - `mcp__21st__search` ("training progress completion checklist card team people") surfaced real, current components built around exactly this content shape — `ProjectProgressCard` (milestone timeline, completed/pending states), `Onboarding Checklist`, `Skills Progress Dashboard` (compact completion bars). Not imported wholesale (each carries its own shadcn/Framer Motion dependency tree not present in this codebase — same reasoning as D-021's research) — used to confirm "module checklist + completion indicator" is a real, current pattern for training content, then rebuilt with this codebase's own primitives (`Caption`/`Text`/`DataText`, `Icon`, existing `env-dark` panel/shadow/`GlowEffect` language).
  - Bell Business's service-panel page was re-fetched specifically for this round (not reused from memory) and turns out to be genuinely flat — title + one-line description, no visual variety between cards, no dashboard elements, hierarchy from indentation/grouping only. Confirms Bell is NOT the source for "vary the visual per capability" (that's Mohammad's own direction) — used only for its actual pattern (spacing/hierarchy discipline), same "structure only, not visual skin" precedent already applied when Bell informed the sidebar/footer.
- **Decision:** Built `CapabilityTrainingVisual` for Capability 04 (Security Awareness Training) — a module checklist (4 generic, non-proprietary training topics, each with a complete/in-progress/pending status icon) plus a single accent-colored completion ring with a centered percentage, replacing the orb's node-diagram + icon-badge + ghost-numeral formula entirely. The outer dark elevated-card container (rounded corners, border, `shadow-accent`, `GlowEffect`) is kept as the one through-line device across all 8 rows — reasoned as a judgment call, not asserted as the only right answer: a page with 8 unrelated card containers would read as inconsistent rather than premium, and "Dimensional Layering"'s own guidance is exactly elevation/shadow as the depth device, which this already uses. Flagged explicitly for Mohammad to confirm or override. Prototype shown at `/services/prototype-v2`-style route, `/services/prototype-v3` (noindex, not linked from nav), Capability 04's real copy on both variants so the visual treatment is the only variable. Capabilities 01-03 and 05-08, `capability-spotlight.tsx`, and `live-services.tsx` are all untouched — confirmed via DOM query that row 04 on the live `/services` page still renders its original orb panel.
- **Content governance:** the 84% completion figure and all four module names are NEW illustrative values — flagged explicitly as such, since (unlike Capability 02's score card, which reused Cyber Health's already-approved 78/100) no training-completion example exists anywhere in the project docs to reuse. Module names are generic, universally-recognized security-awareness topics, not Oragrol-specific curriculum.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (10 routes); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow on `/services/prototype-v3` at 390px; zero non-pre-existing console errors; confirmed via DOM query that `/services` itself (row 04's `.env-dark` orb panel) is unaffected.
- **Status:** Awaiting Mohammad's review — approve this direction for Capability 04 and proceed to 05-08 (each still needing its own suited treatment, not this same checklist-ring formula reused), request changes, or a different direction.

---

### D-023 — Capability 04 approved and shipped; /services/prototype-v3 removed
- **Date:** 2026-08-14
- **Context:** Mohammad reviewed D-022's `/services/prototype-v3` prototype (`CapabilityTrainingVisual` — module checklist + completion ring) live and approved it: "Approve CapabilityTrainingVisual for Capability 04 — make it live, replacing the orb. Remove /services/prototype-v3."
- **Decision:** `live-services.tsx` now renders a per-row visual via a small `CapabilityVisual({ service })` selector — Capability 04 renders `CapabilityTrainingVisual`, Capabilities 01-03 continue rendering `CapabilitySpotlightVisual` unchanged. `/services/prototype-v3` deleted, same lifecycle every prototype route in this project has followed (D-014, D-021's `/services/prototype-v2` still active pending its own separate decision).
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes, `/services/prototype-v3` confirmed 404 on a fresh production server); fully clean rebuild (stray node processes killed, `.next` cleared — this also regenerates Next's route-type-validator file, which had gone stale after the route deletion and caused a transient standalone-`tsc` error, resolved by the same rebuild); DOM query confirms rows 01-03 still render the orb panel's ghost-numeral marker, row 04 now renders the completion-ring SVG and nothing else; zero horizontal overflow at 390px; zero non-pre-existing console errors; screenshot of the live row reviewed directly.
- **Status:** Active. Capabilities 05-08 still pending their own individually-researched treatments (D-022's own note) — see [[D-024]] for Capability 05.

---

### D-024 — Round 8: Capability 05 (Managed Security Services / 24/7 MDR) shell prototype
- **Date:** 2026-08-14
- **Context:** Continuing D-022's one-capability-at-a-time process after Capability 04 shipped (D-023). Capability 05 next.
- **Research, stated before build (per instruction):**
  - `ui-ux-pro-max --domain chart` ("real-time monitoring live activity feed alerts status") recommended a real-time/streaming visualization for this data type, but explicitly flagged flashing elements as an accessibility risk needing restraint. Informs using one slow, subtle live-pulse (`motion-safe:` / `motion-reduce:` Tailwind variants — plain CSS, already respects `prefers-reduced-motion`, no client component needed) rather than a literal streaming ticker.
  - `mcp__21st__search` ("live activity feed alert log status indicator compact card dark") surfaced "Chrono Board" (its own description: "a concise activity timeline that surfaces recent system events... with clear status colors") as the closest real, current pattern — confirmed "a short list of recent, timestamped events with a live indicator" is the right shape for MDR content. Not imported directly (separate dependency tree, same reasoning every prior round's research used) — rebuilt with this codebase's own primitives.
- **A scope difference surfaced before building:** Capability 05 does NOT live in a full `LiveServices` row like 01-04 — it's one of 4 cards in `additional-capabilities.tsx`'s compact grid (D-007's "finalizing, not live yet" tier). Kept it in that format rather than promoting it to a full row — only the visual mark was asked to be redesigned, not the page architecture that distinguishes "live" from "finalizing" content — flagged explicitly for Mohammad in case he meant otherwise.
- **Decision:** Built `CapabilityMonitorMark` — a compact dark inset panel (same "dark mark inside a light card" convention `CapabilitySpotlightMark` already uses) with a live-pulse indicator, a small Radar icon, and two illustrative timestamped alert rows, replacing the plain icon-in-circle mark for Capability 05 only.
- **A real layout consequence found and flagged, not silently fixed on the live file:** the new mark is taller than the plain icon mark it replaces. Since `additional-capabilities.tsx`'s grid stretches all 4 cards to match the tallest by CSS Grid default, shipping this as-is would stretch Capabilities 06-08's still-plain cards to match, leaving large empty space in each. The prototype comparison route demonstrates this directly (both the "before" and "after" full 4-card rows, not just Capability 05 in isolation) and applies `items-start` only within the prototype's own second grid to show the corrected behavior — NOT applied to the live file yet, since that grid is shared with 06-08 and changing it now would be a live change ahead of approval. Flagged as a required follow-up if this direction is approved.
- **Content governance:** the two example alert rows are new illustrative values (generic, non-client-specific), same category as D-022's training figures — no existing precedent to reuse for this specific data type, explicitly labeled "Illustrative example."
- **Build:** `/services/prototype-v4` (noindex, not linked from nav) — shows the entire 4-card row twice (current vs. Capability-05-only swap) rather than a two-variant stack like D-021/D-022's routes, specifically so the in-context sibling-height question is visible, not hidden. Does not modify or import from `additional-capabilities.tsx`.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (10 routes); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow on `/services/prototype-v4` at 390px; zero non-pre-existing console errors; confirmed via DOM query that live `/services` Capability 05 card still renders the original `.h-14.w-14` orb mark with no alert-feed content.
- **Status:** Awaiting Mohammad's review — approve for Capability 05 (and apply the flagged `items-start` grid fix on ship), request changes, or a different direction. Capabilities 06-08 remain, each needing their own individually-researched treatment.

---

### D-025 — Capability 05 approved and shipped; grid-stretch fix verified; /services/prototype-v4 removed
- **Date:** 2026-08-14
- **Context:** Mohammad reviewed D-024's `/services/prototype-v4` prototype live and approved it, explicitly requiring the flagged `items-start` grid fix be applied and verified before confirming: "Approve Capability 05's new visual... and ship it, including the items-start grid fix so Capabilities 06-08 aren't stretched by the taller mark. Verify the grid fix doesn't break 06-08's current layout before confirming."
- **Decision:** `additional-capabilities.tsx`'s grid now carries `lg:items-start` alongside its existing `grid gap-6 sm:grid-cols-2 lg:grid-cols-4`. Capability 05's card renders `CapabilityMonitorMark` instead of `CapabilitySpotlightMark`; 06-08 unchanged. `/services/prototype-v4` deleted, same lifecycle as every prior prototype route.
- **Verified — the specific requirement (grid fix doesn't break 06-08):** Playwright, real production server, `getBoundingClientRect()` on each of the 4 finalizing cards' own height: Capability 05 — 450px (tall, as expected, the new mark). Capabilities 06/07/08 — 252px each, identical to their own natural unstretched height, NOT matching 05's 450px. Confirms `items-start` is working: the grid row no longer forces all 4 cards to match the tallest.
- **Verified overall:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes, `/services/prototype-v4` confirmed 404); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow at 1440/390px; zero non-pre-existing console errors; screenshot of the live finalizing-capabilities grid reviewed directly.
- **Status:** Active. Capabilities 06-08 remain, each needing their own individually-researched treatment — see [[D-026]] for Capability 06 (Penetration Testing).

---

### D-026 — Round 9: Capability 06 (Penetration Testing) shell prototype
- **Date:** 2026-08-14
- **Context:** Continuing the one-capability-at-a-time process after Capability 05 shipped (D-025). Capability 06 (Penetration Testing) next.
- **Research, stated before build (per instruction):**
  - `ui-ux-pro-max --domain chart` ("vulnerability findings severity list ranked report" / "categorical distribution severity tags") recommended "Compare Categories" data get a sorted-descending, labeled treatment — informed a ranked findings list ordered by severity, not an unordered icon grid or a single aggregate score.
  - `mcp__21st__search` ("vulnerability findings report card security scan results severity" / "exposed issues ranked list severity tag badge report card") mostly surfaced small severity-badge/chip primitives rather than one complete block — used as confirmation that a severity tag per row is the right atomic unit, then composed with this codebase's own `RiskBadge` (already the governance-approved component for exactly this kind of severity data per `tokens.css`'s own rule) rather than a new badge import.
- **Decision:** Built `CapabilityFindingsMark` — a ranked list of 4 illustrative example findings, each with a `RiskBadge` severity tag, sorted critical→low. Deliberately differentiated in composition from Capability 02's score-card (one aggregate number + evenly-weighted category bars) and Capability 05's activity feed (timestamped events, no severity labels) — a genuinely different shape each time, not a third re-skin. Same shared-grid context as Capability 05 (D-007 finalizing tier, not a full row) — kept in that format. The `items-start` grid fix already shipped with D-025 covers this capability too; confirmed no new grid change was needed.
- **A real bug found and fixed before presenting as final, not after:** the first pass's finding labels ("Outdated TLS Configuration," "Weak Password Policy") truncated illegibly next to the wider severity badges ("Critical"/"Medium" leave less horizontal room than "High"/"Low") — caught by reviewing the actual screenshot, not assumed fine from code. Shortened the labels first (a partial fix — two rows still truncated, since it's the badge width driving available space, not just label length) then replaced fixed single-line `truncate` with natural text wrapping (`items-start` on the row, no `truncate`) — a more robust fix for a narrow card than continuing to fight for single-line fit. Re-verified visually after each change, not just once.
- **Content governance:** the four example findings (SQL Injection, Outdated TLS, Weak Passwords, Verbose Errors) are new illustrative values — generic, widely-recognized vulnerability classes (OWASP-style technical categories), not a named framework/standard and not client-specific, explicitly labeled illustrative. No real Oragrol engagement findings exist in any project doc to reuse.
- **Build:** `/services/prototype-v5` (noindex, not linked from nav) — shows the whole 4-card row twice, Capability 05 already reflecting its shipped D-025 treatment in both so only Capability 06 differs between them. Does not modify or import from `additional-capabilities.tsx`.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (10 routes); fully clean rebuild after each fix (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow on `/services/prototype-v5` at 390px; zero non-pre-existing console errors; confirmed via DOM query that live `/services` Capability 06 card is unaffected; final screenshot reviewed directly confirming legible text on all 4 example findings.
- **Status:** Awaiting Mohammad's review. Capabilities 07-08 remain, each needing their own individually-researched treatment.

---

### D-027 — Capability 06 approved and shipped; /services/prototype-v5 removed
- **Date:** 2026-08-14
- **Context:** Mohammad reviewed D-026's `/services/prototype-v5` prototype live and approved it: "Approve Capability 06's new visual (severity-tagged ranked findings list) and ship it."
- **Decision:** `additional-capabilities.tsx` refactored its per-capability mark rendering from a single `n === "05"` ternary into a small `CapabilityMark({ service })` selector (same pattern as `LiveServices`' own `CapabilityVisual`) — Capability 05 renders `CapabilityMonitorMark`, Capability 06 renders `CapabilityFindingsMark`, 07-08 fall through to the default `CapabilitySpotlightMark` orb, pending their own treatments. `/services/prototype-v5` deleted, same lifecycle as every prior prototype route.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes, `/services/prototype-v5` confirmed 404); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); DOM measurement of all 4 finalizing cards' own height confirms 07/08 stay at 252px (their natural, unstretched height) while 05/06 are 450/456px respectively — the shared `items-start` grid fix from D-025 continues to hold for a second taller mark without any further change; zero horizontal overflow at 1440/390px; zero non-pre-existing console errors; screenshot of the live grid reviewed directly.
- **Status:** Active. Capabilities 07-08 remain — see [[D-028]] for Capability 07 (Endpoint Protection / EDR).

---

### D-028 — Round 10: Capability 07 (Endpoint Protection / EDR) shell prototype
- **Date:** 2026-08-14
- **Context:** Continuing the one-capability-at-a-time process after Capability 06 shipped (D-027). Capability 07 (Endpoint Protection / EDR) next.
- **Research, stated before build (per instruction):**
  - `ui-ux-pro-max --domain chart` — the first, specific query ("device fleet inventory coverage protected status count") returned no matches, so broadened to the underlying data shape ("part to whole coverage percentage protected total"): surfaced Waffle Chart/Pictogram, explicitly flagged as more accessible than a pie/donut for part-to-whole data ("a grid of units, a subset colored differently for the proportion"). Endpoints are literally discrete, countable devices, so a small grid of device icons — most in a protected state, a couple not — is a direct pictogram of fleet coverage, not an abstract stand-in.
  - `mcp__21st__search` ("device fleet inventory status list endpoint protected coverage card dark") didn't surface a device-specific match; "Card Status List" (per-item status-differentiated cells with animated indicators) confirmed the general per-item-status-cell pattern, applied to the pictogram's individual device cells rather than imported directly.
- **Decision:** Built `CapabilityFleetMark` — a 5x2 grid of device icons (Laptop/Monitor/Smartphone/Server, cycled for visual variety, matching this capability's own copy "every device on your network"), most in an accent-colored "protected" state, one in a muted "not yet" state, with a "9/10" coverage figure and an "Illustrative example" label. Deliberately a fourth, genuinely different mechanic among the capabilities addressed so far — not a ring (02/04), not a timestamped feed (05), not a ranked list (06) — a discrete-unit grid.
- **Shared-layout risk, checked and confirmed non-issue:** same shared grid as 05/06 (D-007 finalizing tier). The pictogram is compact (not taller than the plain orb mark), so no new height/stretch risk — confirmed via the in-context whole-row comparison screenshot, not assumed.
- **Content governance:** "9 of 10" and the device-type mix are new illustrative values — a plausible generic small-fleet example, not a real Oragrol client's device count (none exists in any project doc). Explicitly labeled illustrative.
- **Build:** `/services/prototype-v6` (noindex, not linked from nav) — whole 4-card row shown twice, Capabilities 05/06 already reflecting their shipped D-025/D-027 treatments in both so only Capability 07 differs. Does not modify or import from `additional-capabilities.tsx`.
- **Self-verified before reporting (per instruction):** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (10 routes); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow on `/services/prototype-v6` at 390px; zero non-pre-existing console errors; confirmed via DOM query that live `/services` Capability 07 card is unaffected; final full-page screenshot reviewed directly — pictogram legible, no truncation, no layout defects, confirming the fix pattern established in D-026 didn't need to be reapplied here.
- **Status:** Awaiting Mohammad's review. Capability 08 (Incident Response) remains, needing its own individually-researched treatment.

---

### D-029 — Capability 07 approved and shipped; /services/prototype-v6 removed
- **Date:** 2026-08-14
- **Context:** Mohammad reviewed D-028's `/services/prototype-v6` prototype live and approved it: "Approve Capability 07's device-grid visual as confirmed."
- **Decision:** `additional-capabilities.tsx`'s `CapabilityMark` selector gained a third branch — Capability 07 now renders `CapabilityFleetMark`. 08 still falls through to the default orb. `/services/prototype-v6` deleted, same lifecycle as every prior prototype route.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes, `/services/prototype-v6` confirmed 404); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); DOM measurement of all 4 finalizing cards confirms Capability 08 stays at 252px (unaffected) while 05/06/07 are 450/456/370px respectively — the shared `items-start` grid fix (D-025) continues to hold; zero horizontal overflow at 1440/390px; zero non-pre-existing console errors; screenshot of the live grid reviewed directly.
- **Status:** Active. Capability 08 (Incident Response) is the last of the 8 — see [[D-030]].

---

### D-030 — Round 11: Capability 08 (Incident Response) shell prototype — the LAST of the 8
- **Date:** 2026-08-14
- **Context:** Continuing the one-capability-at-a-time process after Capability 07 shipped (D-029). Capability 08 (Incident Response) is the last of the 8 to get its own researched treatment.
- **Research, stated before build (per instruction):**
  - `ui-ux-pro-max --domain chart` ("process sequence steps playbook stages workflow") returned Process Map/Sankey — both flagged with real accessibility caveats and built for many-node/many-path data, not a simple 4-step plan; not used. The UX-domain search ("step sequence timeline stages process indicator") confirmed the right-sized pattern instead: a numbered step-sequence indicator.
  - `mcp__21st__search` ("incident response playbook timeline stages process steps card dark") surfaced "How It Works" and "How We Do It — Process Overview," both real, current "connected step cards" patterns — confirmed a connected/numbered step sequence is the right pattern; not imported directly (same reasoning as every prior round), rebuilt reusing the "connecting spine between steps" TECHNIQUE Cyber Health's `Flow` component already established on this site (same idea, different scale — a compact 4-step card here vs. Cyber Health's full 7-step page section).
- **Decision:** Built `CapabilityPlaybookMark` — 4 connected phases ("Detect," "Contain," "Recover," "Review"), numbered dots joined by a vertical spine. Genuinely the 5th distinct mechanic among the capabilities addressed (not a ring: 02/04; not a feed: 05; not a ranked list: 06; not a grid: 07) — a connected linear sequence, matching what "a clear, practiced plan" (the row's own copy) actually calls for.
- **A risk considered and dropped before building, not caught after:** an early draft considered highlighting one phase as "in progress," to show the plan mid-execution. Dropped deliberately — a marketing page showing a live "currently responding" state risks reading as an actual ongoing incident rather than an illustrative plan overview. All four phases render as an equal-weight static sequence instead.
- **Content governance:** "Detect / Contain / Recover / Review" is a generic, plain-language description of what incident response conceptually involves — not a named framework/standard (not citing e.g. NIST SP 800-61 by name) and not asserted as Oragrol's own documented internal playbook, which isn't confirmed in any project doc. Labeled "Illustrative example."
- **Shared-layout risk, checked and confirmed non-issue:** same shared grid as 05-07 (D-007 finalizing tier). This mark's height is comparable to Capability 07's pictogram, not taller — confirmed via the in-context whole-row screenshot, no new grid change needed.
- **Build:** `/services/prototype-v7` (noindex, not linked from nav) — whole 4-card row shown twice, Capabilities 05/06/07 already reflecting their shipped D-025/D-027/D-029 treatments in both so only Capability 08 differs. Does not modify or import from `additional-capabilities.tsx`.
- **Self-verified before reporting (per instruction):** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (10 routes); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow on `/services/prototype-v7` at 390px; zero non-pre-existing console errors; confirmed via DOM query that live `/services` Capability 08 card is unaffected; final full-page screenshot reviewed directly — connecting line and numbered dots align correctly, all four phase labels legible, no truncation or layout defects found.
- **Status:** Awaiting Mohammad's review. If approved, all 8 capabilities will have their own individually-researched treatment — Services' visual-shell redesign work (the thread running from D-013 through this round) would be complete pending his final sign-off.

---

### D-031 — Capability 08 approved and shipped; all 8 capabilities now have individually-researched treatments; /services/prototype-v7 removed
- **Date:** 2026-08-14
- **Context:** Mohammad reviewed D-030's `/services/prototype-v7` prototype live and approved it: "Approve Capability 08's connected-phase sequence and ship it."
- **Decision:** `additional-capabilities.tsx`'s `CapabilityMark` selector gained a fourth branch — Capability 08 now renders `CapabilityPlaybookMark`. The `CapabilitySpotlightMark` orb fallback is now unreachable in practice (kept only for a hypothetical future 9th finalizing capability, not deleted). `/services/prototype-v7` deleted. This closes the "Capabilities 04-08 shell rethink" thread opened by Mohammad's Round 7 instruction: all 8 capabilities (01/03 keep the original approved D-013/D-014 orb; 02 has its own separate open prototype, D-021, not yet decided; 04-08 each got individually-researched replacements) now have a deliberate, reasoned visual treatment — no capability is still running on an unexamined default.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes, `/services/prototype-v7` confirmed 404); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); DOM measurement of all 4 finalizing cards shows each at its own distinct, natural height (05: 450px, 06: 456px, 07: 370px, 08: 424px) — no stretching, the `items-start` grid fix (D-025) held across all 4 replacements without further changes; zero horizontal overflow at 1440/1920/390px; zero non-pre-existing console errors; full-page screenshot at 1920px captured and reviewed for Mohammad's whole-page sign-off review.
- **Status:** Active. Only remaining open Services item: D-021 (Capability 02's dashboard-mockup prototype), still awaiting Mohammad's decision, and the D-015 "item 5" capability-regrouping question, held pending separate scope confirmation.

---

### D-032 — Capability 02 approved and shipped; /services/prototype-v2 removed
- **Date:** 2026-08-14
- **Context:** Mohammad reviewed D-021's `/services/prototype-v2` prototype (`CapabilityDashboardVisual` — score card + risk-tier breakdown, reusing Cyber Health's already-approved 78/100 illustrative example) and approved it: "Approve Capability 02's dashboard mockup and ship it too."
- **Decision:** `live-services.tsx`'s `CapabilityVisual` selector gained a second branch — Capability 02 now renders `CapabilityDashboardVisual`. Capability 04 (already shipped, D-022/D-023) is unaffected. `/services/prototype-v2` deleted, same lifecycle as every prior prototype route — this was the last prototype route in the project, none remain.
- **A factual discrepancy found and flagged, not silently resolved either way:** Mohammad's same message asked to "confirm Capability 03 still correctly shows the same dashboard treatment (it was approved as an extension of 02 earlier)." Checked `DECISIONS.md`/`PROJECT_MEMORY.md` directly and found no record of Capability 03 ever being included in D-021's scope — that prototype was explicitly built and approved for "ONE new treatment for Capability 02" only. Checked the live code and DOM directly (not assumed): Capability 03 has always rendered, and still renders, `CapabilitySpotlightVisual` (the orb) — confirmed via `getBoundingClientRect`/text-content checks on a fresh production server and a direct screenshot. Did not extend the dashboard treatment to Capability 03 based on an unconfirmed premise, and did not claim it "correctly shows" a treatment it has never had — flagged directly to Mohammad instead, per this project's own "if you still think a past decision is wrong, say so explicitly and ask" rule.
- **Verified (Capability 02 ship only):** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (8 routes — no prototype routes remain in the project); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); DOM/text-content check confirms row 02 renders the score card (no orb ghost-numeral marker) and row 03 still renders the orb (ghost-numeral marker present, no score-card content) — ground truth, not assumed either direction; zero horizontal overflow at 1440/390px; zero non-pre-existing console errors; screenshot of rows 02/03 reviewed directly side by side.
- **Status:** Capability 02 active/shipped. Capability 03's treatment is UNRESOLVED pending Mohammad's answer — the "final all 8 distinct" full-page screenshot he separately requested is held until this is resolved, since generating it now would either misrepresent 03 as already-decided or require guessing his intent.

---

### D-033 — Capability 03 extended to the dashboard treatment as a new, confirmed decision; all Services prototype work complete
- **Date:** 2026-08-14
- **Context:** Following D-032's flagged discrepancy (no record of Capability 03 ever being approved for the dashboard treatment), Mohammad confirmed via direct question: extend it to Capability 03 now, as a new decision — not a retroactive claim that it was already done.
- **Decision:** `live-services.tsx`'s `CapabilityVisual` selector now matches `n === "02" || n === "03"` for `CapabilityDashboardVisual`. The component takes no per-capability content props (its illustrative score/category copy is shared, hardcoded content) — reused as-is rather than forked into a near-duplicate, since the brief was to extend the same treatment and both rows are risk/vulnerability-assessment-shaped content.
- **Verified — ground truth for all 8 capabilities, checked individually, not assumed:** Playwright DOM/text-content query on a fresh production server confirms exactly one treatment per row with no overlaps: 01 → orb (ghost-numeral marker only), 02 → score card, 03 → score card, 04 → completion ring, 05 → activity feed, 06 → ranked findings, 07 → device pictogram, 08 → connected phase sequence. `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (8 routes, no prototype routes remain anywhere in the project); zero horizontal overflow at 1440/1920/390px; zero non-pre-existing console errors.
- **Status:** Active. This closes the entire "Capabilities 02/04-08 shell rethink" thread from Round 7 onward — every capability on `/services` now has a deliberate, individually-verified visual treatment (01 and 03 both intentionally still use the original D-013/D-014 orb — 01 because it was never in scope for this thread, 03 by this round's explicit decision to share 02's treatment). Full-page 1920px screenshot captured and sent to Mohammad for final sign-off review.

---

### D-034 — Services approved as complete by Mohammad; visual-redesign thread (D-013 through D-033) closed
- **Date:** 2026-08-14
- **Context:** Mohammad reviewed the final full-page `/services` screenshot at 1920px (all 8 capabilities individually treated, D-033) and gave explicit sign-off: "Services is approved and complete."
- **Decision:** This closes the entire multi-round Services Design Correction thread that began with D-011 (schematic-linework prototype) through D-013/D-014 (glow-orb treatment, all 8 capabilities), the container/typography/sidebar/footer rounds (D-015 through D-020), and the per-capability shell-rethink rounds (D-021 through D-033, Capabilities 02-08 each individually researched and shipped). No further Services visual/structural work is authorized without a new, explicit instruction from Mohammad. The one still-open, explicitly-deferred item from this whole thread is the D-015 "item 5" question (regrouping the 8 capabilities into categories, structure/copy only, benchmarked against Bell Business) — held pending a separate scope confirmation, not part of this sign-off, not to be started without Mohammad opening it again.
- **Final state, for the record:** Capability 01 (Virtual CISO) and Capability 03 (Vulnerability Assessment & Management, per D-033) — orb and score-card respectively, both deliberate; Capability 02 (Risk Assessment & Compliance) — score card (D-021/D-032); Capability 04 (Security Awareness Training) — training checklist + completion ring (D-022/D-023); Capability 05 (Managed Security Services/24-7 MDR) — live-pulse activity feed (D-024/D-025); Capability 06 (Penetration Testing) — ranked severity findings (D-026/D-027); Capability 07 (Endpoint Protection/EDR) — device-coverage pictogram (D-028/D-029); Capability 08 (Incident Response) — connected phase sequence (D-030/D-031). Container width, sidebar CategoryNav, footer, and CTA-alignment fixes from D-015 through D-020 all remain active and unchanged by this sign-off.
- **Status:** Services — SIGNED OFF, COMPLETE. Per the project's own page-by-page rollout rule (D-009: "new visual system builds on /services ONLY first; no other page touched until Services is reviewed and approved"), the next page in `PROJECT_MASTER.md`'s Build Sequence (Section 3) is now unblocked: **Step 8 — How We Work**, the next PENDING step (Steps 1-4/6/7 already complete/implemented before this thread began; Step 5 is this sign-off).

---

### D-035 — Step 8 (How We Work): primary hero visual prototyped at /how-we-work; page copy BLOCKED, not invented
- **Date:** 2026-08-14
- **Context:** Following D-034's Services sign-off, Mohammad instructed building the "How We Work" page from a supplied copy block, with an explicit visual direction: a connected, looping visual for the 4 locked stages (Understand/Prioritize/Protect/Improve) as the page's primary hero element, Improve visually connecting back to Understand (a repeating cycle, not a straight line) — referencing Capability 08's connected-phase spine (Services) for the general spirit, but at hero scale, not a small card. Prototype at `/how-we-work` before touching any other live route; report reasoning trail before building further.
- **Blocker found before any content was written:** the instruction's copy block was not actually present — where real page copy should have been, the message contained the literal placeholder text `[PASTE THE FULL COPY ABOVE HERE]`, not text. Per this project's own explicit content-governance rule ("never invent claims, add, or reword text not present") and the instruction's own emphatic version of that same rule, no page copy (headline, body, section structure) was written. Flagged directly to Mohammad rather than guessing or fabricating placeholder-quality copy to fill the gap.
- **What WAS built, since it didn't depend on the missing copy:** the requested primary hero visual, researched first per instruction:
  - `ui-ux-pro-max --domain chart` ("cyclical process loop repeating cycle diagram stages") returned Sankey/Process Mining — both flagged with real accessibility caveats and built for many-node/many-path data, the same category of mismatch already rejected for Capability 08's playbook. Not used.
  - `mcp__21st__search` ("cyclical process loop diagram circular stages methodology hero dark") did not surface a literal closed-loop component — closest were "Animated Roadmap" (a straight/curved path, not a closed loop) and "How We Do It" (linear stage cards). Neither is actually cyclical.
  - No external result matched "four nodes on a closed, directional loop" well enough to adapt. Built natively instead, reusing this codebase's own already-proven techniques: the polar-coordinate SVG math from `GaugeVisual` (Cyber Health) and the node/connector language from `HeroSchematicVisual`/`ServicesNetworkVisual` (Services) — both already-approved, site-native patterns for a hero-scale illustration in a nested dark, glow-lit panel.
- **Decision — built:** `HowWeWorkCycleVisual` — 4 numbered nodes at compass positions (top/right/bottom/left) connected by 4 directional accent-colored arc segments (with arrowhead markers) forming a closed loop, including the literal Improve(180°)→Understand(270°) segment Mohammad asked for — not just an implied closed circle. A single slow, subtle flowing-dash animation reinforces "this repeats," respecting `prefers-reduced-motion` via Tailwind's `motion-reduce:` variant (same mechanic as Capability 05's live-pulse dot) — the static ring/arcs/labels stay fully legible with motion off, verified in a dedicated `reducedMotion: 'reduce'` browser context. Stage labels/one-liners are the ALREADY-LOCKED copy from `home/how-we-work.tsx`/`home/approach.tsx` — reused verbatim, not new text.
- **A real bug caught before build, not after:** an early draft styled a stage-label `<p>` via the shared `Text` component with a `font-heading` className override — `Text`'s own base string already carries `font-body`, so this would have landed two conflicting font-family classes on one element via `cn()`'s plain concatenation, the same documented gotcha D-016/D-019 hit earlier. Fixed by using a plain `<p>` with explicit classes instead of overriding `Text`.
- **A verification miss caught and corrected within this same round:** an initial computed-style check used `document.querySelector('svg')`, which silently grabbed the site header's logo SVG (first in DOM order) instead of the diagram — reporting 0 paths/no marker and appearing to confirm a bug that didn't exist. Re-ran the check scoped to the actual diagram (`svg[viewBox="0 0 520 520"]`) and confirmed all 4 accent arcs, the arrowhead marker, and the spin animation are genuinely present and computing correctly. Reported here so the near-miss is on record, not silently corrected.
- **Route built:** `/how-we-work`, `noindex`'d, containing ONLY the confirmed-safe "How We Work" page label, the hero visual, and the CTA button reading exactly "Talk to Oragrol" (per instruction) → `/contact` (same label/href pairing used everywhere else on the site). No headline, body copy, or further page sections — all pending the actual copy block.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow at 1440/390px; zero non-pre-existing console errors; screenshots reviewed at 1440/390px plus a dedicated reduced-motion context; computed-style check (corrected mid-round, see above) confirms the diagram's arcs/marker/animation are real, not just visually assumed.
- **Status:** Visual prototype complete and self-verified. Page content — BLOCKED pending the actual copy block from Mohammad. Do not proceed to build the rest of the page (headline, body sections, structure beyond the hero) without it.

---

### D-036 — Step 8 (How We Work): full page built from supplied copy, byte-for-byte verified
- **Date:** 2026-08-14
- **Context:** Mohammad supplied the real "How We Work" page copy (hero headline/subhead, 4 full stage paragraphs, closing band headline+button), with an explicit "do not invent, add, or reword any of this" instruction, to be built around D-035's already-approved-pending hero visual.
- **Decision — structure:** Hero (headline/subhead + `HowWeWorkCycleVisual`, unchanged from D-035, White environment — kept for the reason stated in `hero.tsx`'s own comment: the visual is itself a nested dark elevated panel, and a White surrounding environment is what lets it read as elevated, same as every Services capability row) → Stage Sequence (4 full-paragraph stages, a page-level scale-up of `home/how-we-work.tsx`'s own vertical alternating spine+circle pattern — the same "reuse the established technique, scale it to full-page weight" move Cyber Health's `Flow` already made from `Approach.tsx`; vertical rather than `Flow`'s horizontal row, since a horizontal chip layout has no room for 3-sentence paragraphs) → Closing CTA (same visual family as `FinalCta`/Cyber Health's `ClosingCta` — ring accent, dark environment, centered H2 — but built as its own component, since the given headline and single-button copy don't match either existing shared component).
- **Verified — exact copy fidelity, not just "looks right":** wrote a Playwright script asserting byte-for-byte string equality between the supplied copy and the actual rendered DOM text for all 12 pieces of content (hero headline, hero subhead, 4 stage titles, 4 stage paragraphs, closing headline, closing button) — all 12 passed exactly. This is a stronger check than a visual screenshot glance, directly answering the "did I invent or reword anything" question with evidence rather than assertion.
- **A real screenshot-methodology false alarm caught and correctly diagnosed, not misreported:** the first mobile full-page screenshot appeared to show the Stage Sequence section entirely blank — a serious-looking defect. Investigated via direct computed-style query on the "Understand" heading rather than assuming: `opacity: 1`, no hiding transform, correct position, fully visible. Root cause: the screenshot script's 400ms wait per scroll increment wasn't enough for the section's staggered `Reveal` animations (delay `i * 0.08` plus transition duration) to complete before capture — the exact same category of false alarm this project's own history already has one precedent for (Cyber Health's `Flow`, last step "faded" in an under-waited screenshot). Re-captured with a longer wait (900ms) and confirmed all 4 stages render correctly and legibly on mobile. Reported here rather than either silently re-capturing without mentioning the near-miss, or reporting the false blank section as if it were real.
- **A judgment call flagged, not silently resolved:** `HowWeWorkCycleVisual`'s own short stage one-liners (from D-035, reused Home-teaser copy) now sit directly above the Stage Sequence's full paragraph versions of the same 4 stages — some content echo between the hero visual and the section immediately below it. Left the hero visual completely unchanged per "build the rest of the page around the hero visual already built" (not asked to revise it), but flagged this for Mohammad rather than silently deciding whether the repetition reads as intentional reinforcement (matching Services' sidebar-label + full-content-column precedent) or as redundant.
- **Verified overall:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow at 1440/390px; `noindex` removed from the route now that real content exists, matching every other built page.
- **Status:** Step 8 (How We Work) content-complete, self-verified, awaiting Mohammad's review.

---

### D-037 — Stage Sequence connector-line bug fixed; the cited reference component was found to have the identical bug
- **Date:** 2026-08-14
- **Context:** Mohammad reported the Stage Sequence's vertical connector doesn't actually pass through the alternating stage circles, and asked to check the component against `home/how-we-work.tsx`'s own alternating spine (described as handling this correctly) before fixing.
- **Investigated the cited reference before assuming it was correct:** measured `home/how-we-work.tsx`'s own live circle and spine positions on a fresh page load (not read from source alone). At 1440px: circles sit at x=340 (Understand/Protect) and x=1100 (Prioritize/Improve); the spine sits at x=719-720. The reference's own spine does not pass through any of its circles either — confirmed both by DOM measurement and a direct screenshot of that exact section. The premise that this reference "handles it correctly" doesn't hold; copying its technique verbatim would not have fixed the reported bug. Flagged this rather than silently building a fix that only superficially resembled the reference, or silently declining to mention the reference's own defect.
- **Root cause (present in both components):** a spine fixed at a static x-position (`left-1/2 -translate-x-1/2`) combined with `sm:flex-row-reverse` alternation, which packs each row's circle+content group toward that row's flex-start edge — left edge for a normal row, right edge for a reversed row — never toward the actual center where the spine sits. The circle's position was never structurally tied to the spine's position; it only worked when the layout happened to not alternate.
- **Decision — real fix, not a reference-matching patch:** restructured the `sm:` and up layout as CSS Grid (`sm:grid-cols-[1fr_auto_1fr]`) with the circle always explicitly placed in the center column (`sm:col-start-2`) — the same column the spine's `left-1/2` targets — and stage content placed in the left or right column depending on the stage, alone on its side, text-aligned toward the spine. This guarantees the circle sits on the spine by construction (grid track placement), not by incidental row sizing. Mobile (below `sm`) was already correct — single left-aligned column, no reversal applies there — and is untouched by this fix.
- **Verified — the specific check requested:** Playwright, fresh production server, both required widths: at 1920px, all 4 circles' center-x exactly equals the spine's center-x (delta 0px, all four). At 390px, delta is 1px for all four (sub-pixel rounding, visually indistinguishable, well within tolerance). Screenshots at both widths reviewed directly and confirm the line visibly threading through every circle. `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes); fresh clean rebuild; zero horizontal overflow at 1920/390px.
- **Follow-up worth noting, not actioned without being asked:** `home/how-we-work.tsx` (the Home page teaser) still has this same unfixed bug — out of scope for this instruction (which was about the How We Work page specifically), flagged for Mohammad in case he wants it fixed there too using the same technique.

---

### D-038 — Home teaser's matching connector-line bug fixed
- **Date:** 2026-08-14
- **Context:** Mohammad confirmed: "Yes, fix it on Home too" — the same connector-line bug D-037 fixed on the new How We Work page also exists in `home/how-we-work.tsx` (the Home page's own teaser, flagged as a finding during D-037's investigation).
- **Decision:** Applied the identical real fix from D-037 — circle explicitly placed in a CSS Grid center column (`sm:col-start-2`) that the spine's `left-1/2` already targets, content alternating into the left/right column with text-align toward the spine. Mobile (already correct) untouched. Same component otherwise unchanged — copy, spacing, circle size (`h-10 w-10`, smaller than the new page's `h-12 w-12`, left as-is) all preserved.
- **Verified:** Playwright, fresh production server, both widths Mohammad specified for the original bug report: at 1920px, all 4 circles' center-x exactly equals the spine's center-x (0px delta). At 390px, also exactly 0px delta (this component's mobile geometry aligned slightly more precisely than the new page's 1px sub-pixel gap). Screenshots at both widths reviewed directly, confirming the line visibly threading through every circle. `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes); fresh clean rebuild (stray node processes killed, `.next` cleared); zero horizontal overflow at 1920/390px. Confirmed no regression to the rest of the Home page (Approach's separate horizontal 4-stage section, using the same stage labels but a structurally unrelated layout, is untouched).
- **Status:** Both instances of this bug (How We Work page, D-037; Home teaser, D-038) are now fixed with the same real, construction-guaranteed technique.

---

### D-039 — Solutions hero visual reopened, prototyped: Kinetic Grid replacing the isometric strata visual
- **Date:** 2026-08-14
- **Context:** Mohammad explicitly reopened Solutions' already-approved D-008 hero visual (isometric ascending strata, `StrataVisual`) — an intentional, deliberate replacement decision, not a fix to something broken. Instructed to integrate a specific component ("Kinetic Grid," sourced via 21st.dev) as the new hero background, with 5 required modifications, prototyped at `/solutions/prototype` before touching the live page.
- **Source-code gap, resolved without blocking a second time:** the instruction's own code block came through empty — the same literal-placeholder failure mode as the earlier How We Work copy round (this time no placeholder text was even visible, just an absent block). Rather than stop and ask again, searched `21st.dev` directly (`mcp__21st__search`, "kinetic grid canvas mouse warp ripple background") and found an exact match: "Kinetic Grid" by `satoriui`, id 18254 — name, description ("warping dot grid that bends toward the cursor and ripples outward on click"), and fetched source all matched the instruction's own described details precisely (a `globalColor: "default" | "monochrome"` theme prop, `fixed inset-0` canvas, `min-h-screen` wrapper, `lineActive`/`nodeActive`/`glow`/`ripple` theme keys) — confirmed via `get_component` before treating it as the real source, not guessed or fabricated.
- **All 5 required modifications, each verified with direct evidence, not a visual glance:**
  1. **Scoped to hero, not viewport** — canvas changed from `fixed inset-0`/`min-h-screen` (viewport-relative) to `absolute inset-0` inside a wrapper sized to its own content, using `ResizeObserver` on the wrapper (not `window.innerWidth/Height`) for sizing, and mouse/click listeners attached to the wrapper element with coordinates relative to its own bounding rect (not `window`/`e.clientX`). Verified: canvas measured 1920×520px on a 2380px-tall page at 1920×1200 viewport — well short of full page or viewport height, confirming it's genuinely scoped to the hero section.
  2. **Color remap** — theme reduced to one fixed object built from `tokens.css`'s own raw values, not invented: background `#0a0a0a` (`--palette-black-900`, the same value `.env-dark`'s `--background` resolves to), active line/node/glow/ripple `#018abe` (`--palette-blue-600`, Oragrol Cyan). The `globalColor` prop and "monochrome" variant are removed entirely, not just defaulted away. Verified: sampled the canvas's own background pixel via `getImageData` — exactly `rgb(10,10,10)` (`#0a0a0a`), not assumed from the source alone.
  3. **Reduced-motion support** — reuses the codebase's existing `usePrefersReducedMotion` hook (same one Capability 05's live-pulse indicator uses) rather than a new check. When active: no `requestAnimationFrame` loop starts, no mouse/click listeners attach, one static frame is drawn (mouse forced permanently off-canvas, no ripples — the existing `draw()` function naturally renders the resting/un-warped grid through this, no forked drawing logic). Verified: sampled the same canvas region twice, 600ms apart, in a `reducedMotion: 'reduce'` browser context — byte-identical `ImageData`, direct proof no animation is running, not inferred from a screenshot.
  4. **Performance** — measured via a real `requestAnimationFrame` sampling script, not assumed: 60fps at 1920px under normal CPU; 56fps at 390px under a CDP-simulated 4x CPU throttle (approximating a mid-range mobile device). No dropped-frame or jank concerns found; the animation loop itself is unchanged from the sourced component, only the sizing/coordinate/color/motion-gating logic around it changed.
  5. **Content preserved** — `hero.tsx`'s existing Caption/H1/Text (Solutions eyebrow, the locked "Three levels of protection, one clear path forward." headline, and subhead) are unchanged, copied verbatim into the prototype's own component, rendered as `children` inside the new background layer.
- **A real lint error found and fixed before this was ever reported as done:** the sourced component's own recursive `requestAnimationFrame(animate)` call, wrapped in a `const animate = useCallback(...)`, tripped this project's `react-hooks` lint rule (self-reference before the `const` declaration completes). Fixed by moving `animate` into a hoisted `function` declaration local to the one `useEffect` that uses it, which can safely self-reference — not a functional bug, but a real gate that would have blocked "clean lint" verification.
- **Two screenshot-timing false alarms caught and correctly diagnosed, not misreported:** both the first normal-motion and first reduced-motion full-page screenshots appeared to show the prototype's headline/subhead entirely missing — looked like a serious rendering defect. Same category of false alarm this project has now hit multiple times (Cyber Health's `Flow`, How We Work's Stage Sequence): the capture script took a single screenshot without scrolling through the page first, so the second (lower) hero's `Reveal` entrance animations never triggered before capture. Confirmed via direct computed-style query (`opacity: 1`, no hiding transform, correct position) before re-capturing with proper incremental scroll-through, which showed the content rendering correctly in both states.
- **Prototype:** `/solutions/prototype` (noindex, not linked from nav) shows the current live hero (`SolutionsHero`, `StrataVisual`, unchanged) stacked above the new prototype (`SolutionsHeroKineticPrototype`, `KineticGrid`) with identical copy in both. `hero.tsx` and `StrataVisual` are completely untouched — confirmed via DOM query that the live `/solutions` page has no `<canvas>` element at all.
- **Verified overall:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (10 routes); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow at 1920/390px.
- **Status:** Prototype complete, all 5 required items verified with direct evidence. Awaiting Mohammad's approval before shipping to the live `/solutions` page.

---

### D-040 — Kinetic Grid approved and shipped, replacing D-008's isometric strata hero on Solutions; /solutions/prototype removed
- **Date:** 2026-08-14
- **Context:** Mohammad reviewed D-039's `/solutions/prototype` and gave explicit approval: "Approve the Kinetic Grid hero for Solutions. Ship it to the live /solutions page, replacing the strata visual." Logged here per his explicit instruction as an intentional, deliberate replacement of D-008's original hero decision — not a defect fix. D-008 itself (Solutions' "ascending stacked planes" motif, and the other 8 pages' own per-page visual directions) remains the record of what was originally decided and why; this entry documents that Solutions' specific piece of it has now been superseded by direct instruction.
- **Decision:** `hero.tsx` now imports and renders `KineticGrid` in place of `StrataVisual`, wrapping the same unchanged Caption/H1/Text copy as its `children` (background-and-content pattern, not a separate visual block beside the text). `StrataVisual`'s own file is left in the codebase, unused — not deleted, in case this is ever reverted. `/solutions/prototype` and its prototype-only `SolutionsHeroKineticPrototype` component are both deleted, same lifecycle as every prior prototype route in this project.
- **Verified — same rigor as the prototype round, re-run against the live page, not assumed to still hold:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes, `/solutions/prototype` confirmed 404); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`). Canvas scoping re-measured on the live page: 1920×520px on a 2530px-tall page (page height differs slightly from the prototype's 2380px since the prototype route had extra comparison-UI chrome around it — expected, not a regression). Background pixel re-sampled: exactly `rgb(10,10,10)`. Reduced-motion frame re-sampled twice 600ms apart on the live page: byte-identical, confirming no animation runs. Zero horizontal overflow at 1920/390px. Confirmed via DOM query that the live hero's section contains a `<canvas>` and no `<svg>` — no `StrataVisual` remnant. Full-page screenshot reviewed directly.
- **Status:** Active. Solutions' hero is now `KineticGrid`. Two flags carried forward, unrelated to this change: `StrataVisual`'s file remains in the codebase unused (available for revert); SiteHeader's dark-environment-on-entry assumption (noted in `hero.tsx`'s own comment since D-006/Solutions Step 6) is still an open, not-yet-scheduled follow-up.

---

### D-041 — Cyber Health "Processing Screen" loader prototyped natively; two premise checks surfaced, not silently accepted or ignored
- **Date:** 2026-08-14
- **Context:** Mohammad instructed integrating an external "ai-loader" component as Cyber Health's processing-screen animation, cycling through 5 named steps (Calculate Score, Assign Risk Tier, Generate AI Analysis, Generate PDF, Update HubSpot), with 5 required fixes (invalid JSX, missing base CSS, color remap, dynamic real content, reduced-motion support), described as "filling an already-documented gap" from a "Cyber Health Platform spec (Processing Screen section)."
- **Source-code gap:** the instruction's own code block came through empty, same failure mode as the two prior rounds. Searched `21st.dev` first (same recovery method that found Kinetic Grid's exact match last round) — no result matched the instruction's specific described bugs (raw `class` attributes, missing base CSS with only keyframes provided, the exact purple/pink palette `#ad5fff`/`#471eec`/`#d60a47`/`#311e80`, a dead `useState` counter) closely enough to treat as confirmed; those details read as a raw HTML/CSS snippet (e.g. Uiverse.io-style), not a 21st.dev React component, and this project has no search access to that kind of source. Rather than guess at an external source and present a fabricated adaptation as "the fixed version of your code," built a NEW, native implementation of the effect AS DESCRIBED (rotating ring with layered inset shadows + letter-by-letter fade text), using this codebase's own established techniques — flagged explicitly, not silently substituted.
- **Two premise checks, done before building further, neither silently accepted nor silently ignored:**
  1. Grepped this repo's own docs (`PROJECT_MASTER.md`, `DECISIONS.md`, `PROJECT_MEMORY.md`) for "Processing Screen" and all 5 named steps — zero hits anywhere. No such spec section exists in this project's own record; if it exists, it's in a document outside this repo that hasn't been shared here.
  2. More materially: this codebase has no in-app processing pipeline to attach a "processing screen" to in the first place. `PROJECT_MASTER.md` Step 7 — an existing, already-established decision from earlier in this project — is explicit that the real Assessment/Submit/Score-Calculation/AI-Analysis/Report/Email/CRM-Lead pipeline runs entirely on the external, already-live Tally MVP (`https://tally.so/r/2EzROb`); every Cyber Health CTA links out to it in a new tab. Nothing about that pipeline is rendered or executed by this Next.js app. Displaying a polished "Calculate Score → ... → Update HubSpot" progress animation as if this app were performing those steps, when it structurally cannot (it has no visibility into or control over Tally's backend), would misrepresent how the product actually works — a real Honesty Rule (CLAUDE.md §8) concern, not just a styling nitpick.
- **Decision — proceeded with the requested prototype anyway, deferred the live-wiring question:** the instruction itself explicitly scoped this round to "prototype at `/cyber-health/prototype-loader` before wiring it into the live assessment flow" — a two-phase approach that already defers the "where does this attach" question to a later step. Built the component in full per all 5 required fixes, without resolving or guessing where (or whether) it should attach to the live page, since that depends on an answer only Mohammad has (e.g., is this meant as a purely illustrative/marketing flourish shown briefly before the Tally redirect, or does he have a different real pipeline in mind that isn't reflected in this repo's docs).
- **All 5 required fixes, built into the native implementation:**
  1. Valid JSX throughout (`className`, not `class` — moot for a from-scratch build, but confirmed no such error exists).
  2. Full base styles built out: a fixed-size ring (`h-24 w-24`), a rotating conic-gradient sweep masked into a thin ring (reusing Tailwind's built-in `animate-spin`, already proven working this session), a static inset-shadow core for depth, and per-letter `<span>`s with staggered `animation-delay` for the fade-up text — not just keyframes with no shapes to apply them to.
  3. Color remap: no hardcoded hex at all — every color reads `var(--color-accent)`/`var(--color-background)` directly (the semantic tokens, per `tokens.css`'s own "never reference raw hex, always the semantic layer" rule — stricter than Kinetic Grid's canvas-context hex, since this is normal DOM/CSS with full access to the cascade). No unused state: the single `useState` (`stepIndex`) is load-bearing, actively driving both the displayed text and the `key`-triggered re-animation — no dead code to strip.
  4. Dynamic real content: the exact 5 step names given, in the given order, advancing on a `setInterval`, verified via `waitForFunction` that each of the 5 states renders the exact expected text in sequence — not a static "Generating..." placeholder.
  5. Reduced-motion: `usePrefersReducedMotion` (the same hook already used elsewhere in this codebase) gates the entire animated branch — reduced motion renders ONLY a plain static text label, no ring element at all in the DOM, confirmed via a precisely-scoped query (an initial broader check produced a false positive by matching an unrelated `aria-hidden` element elsewhere on the page — caught and re-verified with a scoped selector before concluding anything). Content still advances through the real 5 steps every interval regardless of motion preference (confirmed: text changed from "Calculate Score" to "Assign Risk Tier" after one tick) — only the visual animation is gated, not the underlying progress information.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (10 routes); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow; all 5 step states individually screenshotted and confirmed via exact DOM text match, not assumed from timing alone; reduced-motion state screenshotted and confirmed to contain no ring/animation elements; confirmed via DOM query that the live `/cyber-health` page has zero loader-related text — nothing wired in.
- **Status:** Prototype complete at `/cyber-health/prototype-loader`, all 5 required fixes verified. NOT wired into any live flow — that decision needs Mohammad's input on where (if anywhere) it should attach, given the architecture finding above.

---

### D-042 — AiLoader repurposed to a generic transition, wired live into both Cyber Health CTAs; prototype route removed
- **Date:** 2026-08-14
- **Context:** Mohammad confirmed D-041's architecture finding was correct: this codebase has no visibility into the real Assessment/Score/AI-Analysis/CRM pipeline (it runs entirely on the external Tally form), so narrating 5 named backend steps ("Calculate Score," "Assign Risk Tier," etc.) would imply real-time computation the frontend can't actually observe — a real Honesty Rule concern, not acceptable to ship as-is. Instructed to repurpose the loader as a brief, generic, honest transition ("Preparing your assessment...") shown during the click-to-redirect to Tally, keep the color remap/reduced-motion/visual-treatment work already done, and wire it into the live CTAs rather than leaving it a standalone prototype.
- **Decision — `AiLoader` simplified:** removed the `STEPS` array, `stepIndex` state, and `setInterval`-based step-advancement entirely. Replaced with one static `MESSAGE = "Preparing your assessment..."` constant. Visual treatment (rotating conic-gradient ring masked into a thin ring, layered inset-shadow core, letter-by-letter fade-up text) and the `usePrefersReducedMotion`-gated static-label-only fallback are otherwise unchanged from D-041.
- **Decision — new `AssessmentCta` wrapper component**, not a change to navigation mechanics: rather than intercepting the click and calling `window.open()` from a delayed callback (which loses the synchronous-user-gesture context browsers require and would very likely get silently popup-blocked on a real, live, revenue-critical path), the underlying element stays a genuine `<a target="_blank" rel="noopener noreferrer">` with no `preventDefault` — native browser navigation (including middle-click, right-click "open in new tab," keyboard activation) is completely unmodified. The `onClick` handler runs alongside that native navigation purely to show a fixed full-screen overlay containing `AiLoader` on the page the user is still looking at, for `TRANSITION_DURATION_MS = 2600`ms, then auto-hides. Both of Cyber Health's "Get Your Cyber Health Score" CTAs (`hero.tsx`, `closing-cta.tsx`) now route through `AssessmentCta` instead of a bare `ButtonLink`; the secondary "Talk to Oragrol" buttons (internal `/contact` links) are untouched.
- **`/cyber-health/prototype-loader` removed**, same lifecycle as every prior prototype route this session (Kinetic Grid's `/solutions/prototype`, etc.).
- **A locator mistake caught and corrected during verification, not misreported:** the first verification pass used `getByRole("link", { name: "Get Your Cyber Health Score" }).first()`, which matched SiteHeader's own nav CTA (an internal `/cyber-health` link, unrelated to this change, no `target`) instead of the hero's `AssessmentCta` — since both share the same accessible name. Confirmed via `outerHTML` inspection before concluding anything was broken; re-scoped the locator to `a[href*="tally.so"]` (the real distinguishing attribute) and re-ran. A one-off timing flake also surfaced in that same first run (overlay screenshot appeared empty because the check landed unusually late relative to that particular run's new-tab-open latency); re-ran twice more, both fully passing, before treating the flake as noise rather than a bug — the isolated timing trace (`overlay count` sampled at 9 checkpoints from t+50ms to t+2864ms) independently confirmed the overlay appears immediately and dismisses at the expected ~2.6s mark with no flakiness in the component itself.
- **Verified:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (9 routes, `/cyber-health/prototype-loader` confirmed gone from the route table); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`). Playwright, scoped to `a[href*="tally.so"]`: hero CTA (normal motion) — overlay appears immediately on click, screenshotted showing the ring + "PREPARING YOUR ASSESSMENT..." fade-in; overlay count confirmed 0 after 2.7s (auto-dismiss). Closing-cta (`reducedMotion: 'reduce'` browser context) — overlay appears immediately, screenshotted showing only the static label with zero `.animate-spin` elements present (no ring in reduced motion). Both cases: `context.waitForEvent('page')` confirmed a real new tab opens to `https://tally.so/r/2EzROb` — the actual navigation is unaffected by the overlay. Zero horizontal overflow at 1440px in both contexts. Console-error check performed: 4-8 `404` entries present in both runs, traced to pre-existing RSC prefetch requests for not-yet-built nav routes (`/industries`, `/insights`, `/company`, `/contact`) — confirmed via a clean pageload with no clicks at all, unrelated to this change, not a regression.
- **Status:** Active, Mohammad-confirmed. `AssessmentCta`/`AiLoader` are live on both Cyber Health CTAs. `/cyber-health/prototype-loader` removed. D-041's 5-step version is fully superseded — no step-cycling, no implied backend visibility, remains. Mohammad confirmed in-browser after Claude's own Playwright verification: "Confirmed working live — loader shows correctly, colors match, Tally opens in new tab as expected. Approved."

---

### D-043 — Cyber Health hero visual reopened, prototyped: ambient rotating-ring loader replacing D-008's instrument-panel gauge
- **Date:** 2026-08-14
- **Context:** Mohammad explicitly reopened Cyber Health's already-approved D-008 hero visual (`GaugeVisual`, the instrument-panel arc gauge) — an intentional, deliberate replacement decision, not a bug fix. Instructed to replace it with a persistent, ambient animated visual based on the rotating-ring loader already built for the assessment click overlay (`AiLoader`/D-042), reusing/extending rather than duplicating that component, with 5 required changes for the different (permanent, page-lifetime) use case, prototyped at `/cyber-health/prototype-hero` before touching the live page.
- **Decision — refactored `ai-loader.tsx` to expose shared building blocks** rather than copy-pasting: extracted `RotatingRing` (the conic-gradient ring + inset-shadow core, now with a `spinning` prop so a caller can pause the CSS animation without unmounting it) and `FadeText` (the letter-by-letter fade, now takes a `wordKey` so a caller can replay the fade whenever the word changes) out of `AiLoader`, which now composes them internally with byte-identical output to before (`RotatingRing size={96}` matches the original `h-24 w-24`/`inset-3` math exactly — verified, not just assumed).
- **New `HeroAmbientLoader` component**, built on those shared pieces, with the 5 required changes:
  1. **No "Generating"/action language** — cycles through the exact example words Mohammad gave ("Assess." / "Prioritize." / "Protect.") every 2.2s, decorative brand words rather than any implied real-time process, honoring the same honesty-rule reasoning as D-042.
  2. **Reduced motion holds for the full page lifetime**, not a brief window — the reduced-motion branch never starts the word-cycle `setInterval` at all (the check is inside the effect body, called unconditionally per React's rules-of-hooks, but the interval itself is never created when `reducedMotion` is true). Verified: sampled DOM state at t=0s and again at t=8s (well past 3× the normal 2.2s cycle) in a `reducedMotion: 'reduce'` context — zero `.animate-spin` elements and the exact same static text present at both points, not just checked once.
  3. **Performance verified, not assumed** — this is a CSS-only animation (Tailwind's `animate-spin`, GPU-compositor-driven, no canvas/JS render loop), so the a priori risk was low, but measured anyway: 59fps sampled on desktop at normal CPU, 61fps sampled at a 390px mobile viewport under CDP 4x CPU throttling — no meaningful degradation either case. **Throttling implemented anyway, not just flagged**: an `IntersectionObserver` on the visual's wrapper pauses both the ring's CSS animation (toggling the `animate-spin` class via the new `spinning` prop) and the word-cycle interval whenever the hero visual scrolls out of view, since this now runs indefinitely for every visitor rather than AiLoader's brief 2.6s. Verified: `animate-spin` count is 1 while the visual is in view, 0 once scrolled away.
  4. **Color** — no new tokens, reuses `RotatingRing`'s existing `var(--color-accent)`/`var(--color-background)` references, same locked palette as the approved overlay version.
  5. **Preserved** — headline, subhead, and both CTA buttons copied verbatim into the prototype-only `CyberHealthHeroAmbientPrototype` component; `hero.tsx` itself is completely untouched.
- **A wording note flagged, not silently resolved:** Mohammad's example words ("Assess.", "Prioritize.", "Protect.") partially, not fully, match the already-locked How We Work 4-stage vocabulary ("Understand, Prioritize, Protect, Improve" — D-035/D-036). Two of three match exactly; "Assess" isn't one of the four locked stage names. Built the prototype with Mohammad's words exactly as given (not silently substituted), per his own explicit "confirm with me if you want different wording" — flagged for his decision before shipping, not resolved unilaterally either way.
- **Accessibility:** the entire component (both branches) is `aria-hidden="true"` on its outer wrapper — same treatment `GaugeVisual` itself documented ("pure decoration... the actual content is rendered as real text elsewhere"), so the cycling words are never announced to assistive tech, consistent with prior practice rather than a new pattern.
- **Prototype:** `/cyber-health/prototype-hero` (noindex) shows the current live hero (`CyberHealthHero`, `GaugeVisual`, unchanged) stacked above the new prototype (`CyberHealthHeroAmbientPrototype`, `HeroAmbientLoader`) with identical copy in both — same comparison pattern as D-039. `hero.tsx` and `GaugeVisual` are completely untouched.
- **Verified overall:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (10 routes); fully clean rebuild (stray node processes killed, `.next` cleared, fresh `npm run start`); zero horizontal overflow at 1920/390px; screenshots reviewed directly for both normal-motion (ring spinning, word visible) and reduced-motion (static label, no ring) states.
- **Status:** Prototype complete at `/cyber-health/prototype-hero`, all 5 required changes verified with direct evidence. NOT wired into the live page — awaiting Mohammad's review/approval and his decision on the wording flag above.
- **SHIPPED LIVE (2026-08-15):** Mohammad approved both the visual and the wording flag — explicitly re-specified "Assess." / "Prioritize." / "Protect." verbatim, resolving the How We Work vocabulary mismatch note above in favor of keeping his original words as given (not silently reconciled with the locked 4-stage names either way). `hero.tsx` now imports `HeroAmbientLoader` directly in place of `GaugeVisual`; `/cyber-health/prototype-hero` and the prototype-only `CyberHealthHeroAmbientPrototype` component removed per the same prototype-lifecycle convention as D-039/D-040 and D-041/D-042. `gauge-visual.tsx` itself left in place, unused, available for revert (same convention as `StrataVisual` after D-039/D-040). Re-verified on the live page, not just re-assumed from the prototype: `npx tsc --noEmit`/`npm run lint`/`npm run build` all clean; a Playwright script (per CLAUDE.md §7's documented visual-check method — the Chrome extension tab in this environment renders `document.hidden === true` regardless of focus, which throttles/freezes the CSS fade and produced misleading partial-letter screenshots, so Playwright was used instead of the extension for the proof screenshots) captured the live `/cyber-health` hero with "PRIORITIZE." fully opaque and the ring mid-spin, and separately confirmed the reduced-motion branch (0 `.animate-spin` elements, static "Assess. Prioritize. Protect." label) and the IntersectionObserver pause/resume (`.animate-spin` count: 1 in view → 0 scrolled to the page bottom → 1 back in view).

### D-044 — Industries page built: sidebar/tabs ARIA-tabs pattern, all 9 industries, real Canadian-sourced content
- **Date:** 2026-08-15
- **Context:** Step 9 (Industries) was PENDING per `PROJECT_MASTER.md`, blocked on the real per-industry content brief. Mohammad supplied it (`ORAGROL_INDUSTRIES_CONTENT_CANADA.md`, aligned with Canadian Centre for Cyber Security / OSFI guidance) along with an explicit structure spec: desktop left sidebar / mobile horizontal scrollable tabs, a right detail panel with 4 fields (Risk/Priorities/Approach/Next Step), instant swap on click (no reload, no accordion), active/inactive styling reusing Services' existing weight-600/weight-400-muted convention, content as a typed data array separate from component logic.
- **Research performed first** (same discipline as D-035/Services' `CategoryNav`): `ui-ux-pro-max`'s `ux` domain (keyboard nav / focus-state / tab-order guidance) and a `21st.dev` component search for "sidebar tabs detail panel selector" — both confirmed this is the WAI-ARIA "Tabs" pattern (a `tablist` of `tab`s driving one `tabpanel` via state), not a same-page anchor-nav list like `CategoryNav` (which scroll-spies real hrefs to always-rendered sections) — genuinely different semantics since Industries only ever shows one panel at a time.
- **Built `IndustriesExplorer`** (`app/components/sections/industries/industries-explorer.tsx`): a real `role="tablist"`/`role="tab"`/`role="tabpanel"` implementation on `<button>`s (not `NavLink`, which renders a real `<Link>` — inappropriate here since nothing navigates), with full keyboard support (Arrow keys/Home/End move focus+selection, roving `tabIndex`). The active/inactive visual rule reimplements `NavLink`'s locked `size="lg"` split (D-019: active `font-semibold text-accent`, inactive `font-normal text-text-muted hover:text-text-primary`) rather than inventing a new one. Desktop renders a sticky vertical tablist (`lg:block`); mobile renders the identical tab set horizontally with `overflow-x-auto`/`whitespace-nowrap` (`lg:hidden`) — verified zero horizontal page overflow at 390px width. Panel content swaps via React state with no transition/delay, per the explicit "instant, not an accordion" requirement.
- **Content** (`industries-data.ts`): a typed `Industry[]` array, all 9 industries copied verbatim from the supplied brief — **verified byte-for-byte via a Node script diffing the parsed source `.md` against the built array**, not a visual read-through (all 9 industries × 4 fields matched exactly on the first pass). `nextStep.kind` (`"assessment" | "contact"`) is UI routing metadata only, not new content: "Get Your Cyber Health Score" routes through the existing `AssessmentCta`/`TALLY_ASSESSMENT_URL` (same real Tally link as Cyber Health/Services); every "Talk to Oragrol About X" variant routes to `/contact`, the same target every other "Talk to Oragrol" CTA on the site already uses — there is no per-industry contact page to invent one for.
- **No abstract line-motif visual was built for the hero**, despite D-008's original "distinct abstract line-motif per sector" direction — Mohammad's structure spec for this round didn't call for one, and the interactive explorer itself is the page's signature "revealed via interaction/selection" element per that same D-008 note; adding a separate static hero graphic wasn't requested and risked competing with it. Flagged here rather than silently decided either way — a future round could still add one without conflicting with anything built now.
- **Page assembly**: Hero (dark, single-column — no paired visual, see above) → `IndustriesExplorer` (white) → `FinalCta` (reused from Home, same as Solutions — not requested explicitly but every other shipped page ends with one; flagged as a consistency choice, not a content invention).
- **Verified:** `npx tsc --noEmit`/`npm run lint`/`npm run build` all clean (11 routes, `/industries` now static). Playwright (per CLAUDE.md §7's documented method): clicked through 3 industries (Professional Services → Healthcare → Technology) confirming the panel swaps to the correct real content every time (checked via `innerText` prefix, not just a visual glance); keyboard `ArrowDown` from the first tab correctly moved selection to the second; zero horizontal overflow at 390px. Screenshots sent to the user showing two different industries selected with real content and the mobile tab bar.
- **Known pre-existing, out-of-scope issue surfaced, not fixed:** the site-wide floating "Under attack?" widget (fixed bottom-right) visually overlaps the panel's "Next Step" CTA button at some scroll positions/viewport heights on this page — a sitewide floating-element z-index/layout issue that predates this page and isn't specific to Industries; flagged for a future pass rather than fixed here (out of scope for this instruction).

### D-045 — Placeholder Malaysian emergency number removed, "Under attack?" pill now text-only, links to /contact
- **Date:** 2026-08-15
- **Context:** D-017's temporary Malaysian placeholder number (flagged at the time as needing replacement with a real Canadian line before public launch — no real line has been supplied since) was explicitly instructed to be removed site-wide, not replaced with another number: the pill now reads "Under attack? Contact us now" and links to `/contact` instead of a `tel:` link.
- **Investigation before editing:** Grepped the whole repo for the number and its `tel:`-formatted variant — found exactly one component (`EmergencyCta`, rendered once from `RootLayout`, so already site-wide including the footer area) plus historical mentions in `DECISIONS.md`/`PROJECT_MEMORY.md` (left untouched — those are the permanent record, not live site content, same treatment as every other superseded decision in this file). No separate hardcoded instance existed anywhere else (footer itself never had its own copy — `EmergencyCta` was always the single source, per D-017).
- **Decision:** `EmergencyCta` now renders a `next/link` `<Link href="/contact">` instead of an `<a href="tel:...">`; the phone-number constants are deleted, not just hidden. Icon (`PhoneCall`), pill styling, and fixed bottom-right position all unchanged, per explicit instruction. Also removed the literal digits from the component's own code comment (not just the rendered UI) — the requirement was "no longer appears anywhere," so the historical note now describes the number in prose and points to this decision instead of repeating it in source.
- **`/contact` doesn't exist as a built page yet** (Step 12, still PENDING) — same as every other "Talk to Oragrol" CTA already linking there site-wide (Services, Cyber Health, Industries' D-044 "Talk to Oragrol About X" CTAs), so this isn't a new gap, just the existing sitewide convention applied here too.
- **Verified:** `npx tsc --noEmit`/`npm run lint`/`npm run build` all clean. Grepped the number's exact digit sequence across the repo post-edit — zero hits outside `DECISIONS.md`/`PROJECT_MEMORY.md`. Playwright-confirmed on `/`, `/cyber-health`, and `/industries` that the pill's text reads exactly "Under attack? Contact us now" and its `href` resolves to `/contact` on all three (single global component, checked on more than one page rather than assumed).
- **Status:** Live. D-017's "temporary placeholder, needs a real Canadian line" flag is now moot — there's no number shown at all pending a real line; Instagram's separate `href="#"` placeholder from the same D-017 round is untouched, still open.

### D-046 — Contact page built (two-path, not a generic form); Instagram placeholder replaced with the real URL
- **Date:** 2026-08-15
- **Context:** Step 12 (Contact) was PENDING in `PROJECT_MASTER.md`, with an earlier placeholder outline (generic Name/Business email/Company/Company size/Message form + a "Book a consultation" CTA — no real destination for that CTA exists anywhere in the project). Mohammad's actual instruction for this build superseded that outline with a specific two-path structure (`Oragrol_Contact_Page_Content.md`): Path 1 "New to Oragrol" → the Cyber Health assessment (self-serve), Path 2 "Existing client / urgent" → direct phone/email plus the "Under attack?" framing, kept visually and functionally separate — explicitly NOT merged into one generic form. The hero headline itself ("Let's make security clearer.") IS the one piece of the original outline reused, since it's still the same locked line, not new copy.
- **"Book a consultation" intentionally dropped, not built:** no booking system/Calendly link/real destination is confirmed anywhere in this project's docs — same "flag, don't invent a destination" discipline as Instagram's prior placeholder and the emergency-CTA number (D-017/D-045). If a real booking flow exists, it needs to be supplied before this CTA is added.
- **Content** (`locations-data.ts`): a typed `Location[]` array, Thunder Bay's address/phone/email/hours copied verbatim from the brief (all real, confirmed values). Every Toronto field is the literal `[pending]` marker from the source — rendered per-field as a "Coming soon" `Badge`, not left blank (which would read as broken) and not backfilled with Thunder Bay's values. No unverified legacy-WordPress stats ("+14 Years," "+3 Countries," "+150 Projects") carried over, per the brief's explicit instruction.
- **Toronto is visually primary** (larger card, `border-accent`/subtle accent-tint background, bigger heading), explicitly labeled "Operations"; Thunder Bay renders smaller/secondary, explicitly labeled "Registered Office" — never reading as the main "call us" number, per the brief.
- **A component-conflict pitfall avoided, not just noticed:** the location cards needed a heading size between `H3`'s two existing tiers (`default`/`lg`). Overriding `H3`'s size via a `className` prop would have emitted two conflicting `text-*` utilities for the same element — the exact plain-`cn()`-concatenation gotcha `cn.ts` and `typography.tsx`'s own `H3Size` comment both already document (later-in-generated-CSS wins, not later-in-the-className-string). Used a plain `<h3>` with one complete, non-conflicting class string instead of fighting the shared component.
- **Path 2's "Under attack?" CTA is a real `tel:` link to Thunder Bay's confirmed number**, not a link back to `/contact` (which would be circular now that this page IS `/contact`, and the sitewide floating pill — D-045 — already points here). Email is the secondary action underneath, `mailto:` to the confirmed HQ address. Path 1 reuses the exact same `AssessmentCta`/Tally destination used everywhere else — no new CTA target invented.
- **No closing `FinalCta`** on this page (every other shipped page has one) — flagged deliberately, not an oversight: `FinalCta` itself points at `/contact`, so including it here would point the page at itself.
- **Instagram fix (separate item, same session):** `SiteFooter`'s Instagram icon previously linked `href="#"` (D-017's explicit "no real URL yet" placeholder). Grepped the whole repo for `instagram` first — confirmed exactly one live `href` existed (no separate hardcoded instance on any other page, including this new Contact page, which doesn't render social icons at all). Updated to `https://www.instagram.com/oragrolglobal`, `target="_blank"`/`rel="noopener noreferrer"` added (matching the LinkedIn icon's existing pattern), the old "(coming soon)" `aria-label` suffix removed since it's no longer accurate.
- **Verified:** `npx tsc --noEmit`/`npm run lint`/`npm run build` all clean (`/contact` now static, 12 routes). Playwright: confirmed Path 1's CTA resolves to the real Tally URL (scoped to `<main>`, since the header nav has its own same-text CTA that a naive query would wrongly match instead), Path 2's CTA resolves to `tel:+16133150328`, the email link resolves to `mailto:info@oragrolglobal.com`, exactly 4 "Coming soon" badges render (one per Toronto field), and both "Registered Office"/"Operations" labels are present. Post-edit grep for `href="#"` and the old Instagram placeholder across every `.tsx` file: zero hits; grep for the real Instagram URL: exactly one hit, the fixed footer link. Screenshots sent to the user showing both location cards (including the pending-state styling) and the footer.
- **Status:** Live. Contact page awaiting Mohammad's review, same as every other recently-shipped page this session.

### D-047 — General Inquiry form added to Contact page, real email send via Resend
- **Date:** 2026-08-15
- **Context:** Added a third entry point to `/contact` (D-046), above the Locations section: a "General Inquiry" form for anything the two funneled paths (assessment / direct urgent contact) don't cover — partnerships, media, vendor questions, etc. Explicit instructions: real email send (Resend or SendGrid, whichever is simpler), no n8n/HubSpot, a real success/error state (no faked success), and if an API key is needed, say exactly what to set up rather than hardcoding anything.
- **Resend chosen over SendGrid:** one API key, one `resend.emails.send()` call, and Resend's own `onboarding@resend.dev` sandbox sender can send a real test email immediately with just the key — no separate sender-identity verification step required to get a first working send, unlike SendGrid's mandatory sender-authentication flow. Matches the "minimal config" instruction directly.
- **Reused, not added, for form handling:** `react-hook-form` and `zod` were already project dependencies (installed, unused anywhere until now) — used instead of hand-rolling form state or pulling in a different library. Added `@hookform/resolvers` (the standard, no-config RHF↔zod adapter) and `resend` — the only two new dependencies.
- **One shared `zod` schema** (`app/lib/contact-schema.ts`) validates both client-side (`GeneralInquiry`, via `zodResolver`) and server-side (`/api/contact`'s authoritative re-check — client-side validation is never trusted alone). Hit and fixed a real zod+RHF typing pitfall before it shipped: an initial `.transform()` on the optional `company` field made zod's inferred type diverge between the schema's input and output shapes, which `useForm<GeneralInquiryInput>`'s single generic can't express — removed the transform, handled the blank-vs-provided case at render time in the API route instead (`company || "—"`), keeping input/output types identical.
- **`POST /api/contact`** (`app/api/contact/route.ts`) is the only place the real send happens — the client never talks to Resend directly (no API key in the browser bundle). Required env var `RESEND_API_KEY` (secret) is read via `process.env` only, never hardcoded; `.env.local.example` (newly tracked — added `!.env*.example` to `.gitignore`, which previously blanket-ignored every `.env*` file including example ones) documents it plus two optional overrides (`CONTACT_TO_EMAIL`, `CONTACT_FROM_EMAIL`). A missing key is treated as a real, reportable failure (logged server-side with the exact fix, returned to the client as a real error pointing to the direct email address) — never a faked success, per the explicit instruction.
- **Verified the "no fake success" behavior directly, not just claimed it:** submitted the live form with `RESEND_API_KEY` unset (this environment has no real key yet) — confirmed via Playwright and the dev-server log that it correctly surfaces `"Email sending isn't configured yet. Please email info@oragrolglobal.com directly."` as a real error, with the exact diagnostic (`RESEND_API_KEY is not set...`) logged server-side, not a silently-swallowed failure.
- **Visual consistency:** the form sits in the same `Card` treatment as `TwoPath`'s two path-cards (bordered, `p-8`, same `Caption`/`H2` pattern) so it reads as a third option in the same family — not a bolted-on generic form. Error text reuses the site's existing form-error convention (`text-risk-critical`, already used by `FieldChrome` for required-field asterisks and inline field errors) rather than inventing a new error color.
- **Status:** Code complete, verified end-to-end except the actual outbound send (blocked on a real `RESEND_API_KEY`, which only Mohammad can supply — see PROJECT_MEMORY.md for the exact ask). Once supplied, a real test send still needs to be run and proven (screenshot of the received email or a server log showing success) before this is fully closed.

---

### D-048 — Company / About page built (Step 11); real content brief supersedes the original outline; Founder Bio opens Dark, not "primarily light"
- **Date:** 2026-08-17
- **Context:** Built `/company` from `ORAGROL_ABOUT_PAGE_CONTENT_FINAL.md` (4 sections: Founder Bio, Company Mission/Story, Team, Values/Differentiators — "already through content and compliance review," use exactly as written) plus a real founder photo. Explicit instructions: research premium About/Company patterns via `ui-ux-pro-max` + `21st.dev` first, don't default to generic "photo + paragraph," give the photo real editorial weight (not a small avatar), and give each of the 4 sections its own distinct visual treatment.
- **This real, final content brief supersedes PROJECT_MASTER.md's original Step 11 outline** (Hero / Who Oragrol Is / Company Story / Philosophy / Experience & Expertise / Partners & Ecosystem / Approach / CTA, "primarily light section") — the same "real brief supersedes placeholder outline" pattern D-046 established for Contact. The new structure is the one built; the old outline is historical record only.
- **Research done before designing, per instruction:** `ui-ux-pro-max` (`--domain landing` for about/founder-story patterns, `--domain ux` for editorial-photo layout guidance) and `21st.dev` (`search` for "about page founder story bio section," "team section values grid company mission"). Neither surfaced a strong pre-built match for a premium editorial founder-bio pattern specifically — the catalog results were generic SaaS "About Page" templates (stat counters, logo grids, card carousels), not the large-photo/pull-statement editorial treatment this brief called for. Consistent with this project's own repeated precedent (D-035 Cycle Visual, D-041 AiLoader, D-043 ambient hero) of building natively from researched principles, flagged as such, rather than force-fitting a mismatched import — done again here rather than silently claiming a 21st.dev source that isn't real.
- **Founder photo:** arrived saved to the repo root as a mangled filename (`publicimagesfounder-mohammad.jpg.jpeg` — path separators dropped, wrong extension appended) instead of the stated `public/images/founder-mohammad.jpg`. Verified it was a real, valid, high-resolution (3024×4032, exact 3:4 portrait) photo before moving/renaming it to the correct path and deleting the stray root-level file.
- **Four distinct section treatments (not four stacked identical text blocks):**
  - **Founder Bio** — Dark environment, large 2-column layout: the real photo at genuine editorial scale (not a small avatar) with a soft ambient `GlowEffect` halo behind its frame (same accent-family "light-based depth" material as Services' `CapabilitySpotlight`, D-013 — genuine reuse of an already-approved premium treatment), paired with name/title/bio. The origin-story paragraph renders at a larger "lede" size — a standard editorial-profile convention, purely typographic, the text itself untouched/unreworded/verbatim.
  - **Mission/Story** — Light-blue environment, single-column manifesto treatment: the two-sentence opening ("Oragrol was built on a simple idea...") set at display scale as the section's dominant visual (a natural pull-line already, given as its own two short sentences in the source — enlarged, not reworded), explanatory paragraph below, closing trajectory line set apart in accent color. A soft ambient glow reaches for D-008's original "architectural/light-study abstraction" motif for Company, filling the role the founder photo plays in the prior section.
  - **Team** — White environment, deliberately minimal (a single bordered band, not a padded-out grid) — matches both the content brief's own two-sentence brevity and the explicit instruction that this section "can be minimal given it's founder-led today."
  - **Values/Differentiators** — White environment, 3-item icon-led grid (icon badge reuses the exact radial-gradient "material" style from `CapabilitySpotlight`, D-013) plus the one-line summary rendered as its own nested `env-dark` elevated closing panel (the same "dark panel floating on a White section" technique Services' capability rows use) — giving the section a clear beginning/end.
- **No separate hero was built.** No hero headline/subhead was supplied in the content brief (unlike every other built page) — per CLAUDE.md §8's "never invent... flag it instead of guessing," nothing was added above Founder Bio; it doubles as the page's opening.
- **Founder Bio deliberately opens Dark, not the brief's "primarily light" framing — flagged, not silent.** SiteHeader (`site-header.tsx`) is hardcoded `env-dark` with only a 30%-opacity dark wash before ~140px of scroll — its own comment already says "once light-first pages exist, this should read the entry section's environment instead of assuming Dark," not yet done. Opening Company on White/Light-blue would put the header's white nav text on a near-white page background at scroll position 0 — every page built so far (Home, Services, Solutions, Cyber Health, How We Work, Industries, Contact) opens Dark for exactly this reason (see `services/hero.tsx`'s identical reasoning). Rather than fix SiteHeader itself (out of scope, shared component, risks regressions on every already-approved page) or silently open the page in a way that's illegible at load, Founder Bio stays Dark; the other 3 of 4 sections (Mission, Team, Values) are White/Light-blue, honoring the "primarily light" spirit everywhere it's safe to.
- **A plain `<h2>`, not the shared `H2` component, carries Mission's display-scale statement** — `H2` has no `size` variant (unlike `H1`/`H3`/`Caption`), and overriding its size via `className` would emit two conflicting `text-*` utilities at once, the same `cn()` gotcha D-018 hit and fixed the identical way.
- **Fixed one real accessibility issue before shipping, not after:** Values' 3 card titles were initially built as `H4` under an `H2` section heading, skipping a level (and breaking this project's own established convention — `H3` is what every other card/item title site-wide uses, confirmed via grep across Contact/How We Work/Services). Caught via a direct heading-order DOM query, fixed to `H3`, re-verified clean.
- **A real mobile horizontal-overflow bug found and fixed:** Founder Bio's photo-frame `GlowEffect` halo used a `-inset-8` negative-inset wrapper with no `overflow-hidden` ancestor, bleeding 8px past the 390px viewport edge. Fixed by adding `overflow-hidden` to the section (the same pattern `FinalCta`'s `OragrolRing` bleed and Mission's own glow already use) — re-verified `0px` overflow at both 1440px and 390px.
- **A screenshot-methodology false alarm correctly diagnosed, not reported as a bug:** a `fullPage: true` Playwright mobile screenshot appeared to show the founder photo missing entirely and the site-wide `EmergencyCta` pill overlapping the H1 name. Investigated via direct DOM/computed-style query before concluding anything (same discipline as D-036/D-041/D-043's prior false-alarm catches): the photo was fully loaded, visible, opacity 1, correctly positioned; the pill's parent `<div>` is genuinely `position: fixed` (confirmed in `emergency-cta.tsx`) — both artifacts were Playwright's own known limitation of compositing `position: fixed` elements once, at one scroll-relative position, into a tall stitched `fullPage` image, not a live rendering defect. Re-captured with per-viewport (non-stitched) screenshots instead, which show both elements rendering correctly. The pill's real, live overlap with Values' third card at one specific scroll depth on mobile is the same already-known, already-flagged, out-of-scope site-wide issue D-044 logged for Industries — not new, not fixed here, not silently ignored either.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean (`/company` now a static route, 11 total). Server-rendered HTML confirmed (via direct `curl`, not just a browser render) to contain all 4 section headings verbatim. Playwright: zero horizontal overflow at 1440px/390px; reduced-motion context confirms all `Reveal`-wrapped content renders at `opacity: 1` (final state, not stuck) at every heading checked; alt text confirmed present and meaningful (`"Mohammad Chelouy Tabrizi, Founder & CEO of Oragrol Global"`); heading order confirmed correct (one `H1`, then `H2`s, then `H3`s, no skipped levels) after the fix above. Full-page and per-section screenshots at both widths reviewed directly, not assumed from code.
- **Status:** Built, verified, awaiting Mohammad's review. Same standing flags as every other recently-built page: the sitewide `EmergencyCta` pill's overlap-at-certain-scroll-positions issue (D-044) remains open, out of scope here; SiteHeader's Dark-entry-section assumption (noted in its own comment, reaffirmed by this decision) remains open for whenever a genuinely light-first page is wanted.

---

### D-049 — Founder Bio restructured: small top identifier → narrative bio → large closing photo
- **Date:** 2026-08-17
- **Context:** Mohammad asked to restructure Founder Bio (D-048) from its original 2-column "large photo | name+bio" layout: a small photo identifier (new headshot, `public/images/founder-mohammad-headshot.jpg`) alongside name + "Founder & CEO" at the top, bio text reading through the middle narrative-style, then the original large-format photo (`public/images/founder-mohammad.jpg`) reappearing as a closing visual at the end. Text content explicitly unchanged — layout/structure only.
- **Headshot file arrived with the same mis-saved-filename pattern as the original founder photo (D-048)** — saved to `public/images/founder-mohammad-headshot.jpg.png` instead of the stated `.jpg` path. Verified it was a real, valid, high-resolution front-facing headshot before renaming to the correct path.
- **New single-column structure, one Reveal-staggered narrative flow instead of the old 2-column grid:** (1) small circular photo (h-20/24, `rounded-full`, `object-cover`) + `H1` name + `Caption` title, centered; (2) the same 3 bio paragraphs (byte-identical, including the lede-sized first paragraph — unchanged from D-048), in a `max-w-2xl` centered column; (3) the large-format photo (same `GlowEffect`-halo + bordered-frame treatment as before) at the end, widened from D-048's ~45%-of-1280px column to `max-w-2xl` (672px) centered, matching the text column's width for a continuous single-measure editorial feel and giving it real "closing moment" weight rather than splitting the layout with text beside it.
- **Alt text moved, not duplicated:** the small top headshot is `alt=""` (decorative) since the adjacent `H1` already announces the name — repeating the same description on both images in one section would double-announce the same information to screen readers, not add information. The large closing photo keeps the full descriptive alt text (`"Mohammad Chelouy Tabrizi, Founder & CEO of Oragrol Global"`), since it's now the section's own visual statement.
- **`priority` moved from the large photo to the small headshot** — a real, correct-by-construction side effect of the restructure, not a separate ask: the small headshot is now the above-the-fold image (LCP candidate), the large photo is now below the fold, so eager-loading the large photo would no longer be the right call.
- **Dark environment, container width, and every other structural decision from D-048 (why Founder Bio opens Dark, not the brief's "primarily light" framing) are unchanged** — this instruction was scoped to layout within the section, not a reason to revisit that separate call.
- **A real, new mobile-only issue found and verified before reporting either way — not fixed:** the wider closing photo (now `max-w-2xl` vs. the prior ~`max-w-md` column) makes the sitewide fixed `EmergencyCta` pill visibly overlap the photo itself at real single-viewport scroll positions on mobile (confirmed with actual per-frame screenshots at fixed scroll offsets — not the `fullPage`+`position:fixed` stitching artifact D-048 diagnosed and ruled out for the previous layout; this is a genuine on-screen overlap a mobile visitor would see, verified at scroll≈0px, ≈400px, and ≈800px). This is the same already-flagged, out-of-scope, sitewide `EmergencyCta` issue as D-044/D-048 — but now directly obscures the photo this exact task asked to make a "strong closing visual moment," which is a more material instance of the same standing issue than its prior occurrences (a card's tail-end text, not a person's face). Not fixed here — `EmergencyCta` is a single global instance rendered from `RootLayout` on every page, and repositioning or hiding it per-route would be a bigger, unrequested change to a shared component. Flagged explicitly, with evidence, for Mohammad's call — desktop was checked at the same real scroll positions and has no overlap (the centered photo column doesn't reach the pill's bottom-right corner at 1440px).
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean. Zero horizontal overflow at 1440px/390px. Both images confirmed correctly wired via DOM query (`founder-mohammad-headshot.jpg` at the top with `alt=""`, `founder-mohammad.jpg` at the close with the full descriptive alt). Real (non-stitched) screenshots at both widths, scrolled through incrementally, reviewed directly.
- **Status:** Built, verified, awaiting Mohammad's review — including his call on the new mobile pill/photo overlap flagged above.

---

### D-050 — Founder Bio real design fixes: headshot crop corrected, closing photo shrunk + redesigned as photo-beside-pull-quote
- **Date:** 2026-08-17
- **Context:** Mohammad reviewed D-049's restructure and found two real design problems, not a quick-tweak request: (1) the top circular headshot cropped awkwardly, face not centered/filled; (2) the closing photo was still far too large, needing to shrink to roughly one-third its size and read as a tasteful accent, not a dominant block; (3) general instruction to research (`ui-ux-pro-max` + `21st.dev`) clean high-end ways to pair a smaller closing photo with text, e.g. photo-beside-pull-quote rather than a stacked block.
- **Root cause of the crop bug, investigated before fixing:** `public/images/founder-mohammad-headshot.jpg` (added in D-049) turned out, on close pixel-level inspection, to actually be a 3/4-body composition (1086×1448, `xxd`-confirmed real content: same "arms crossed, leaning on table" framing family as the main photo) rather than a tight face crop — despite its filename. `object-cover`'s default center-center crop on a photo where the face occupies only the top ~30% of the frame will always show mostly torso/shirt in a small square window; that's a source-image/CSS-fit mismatch, not a bug in the original object-cover call. Also found and fixed a real asset-hygiene bug from D-049: the file's actual bytes are PNG (`xxd` confirms the `89 50 4E 47` PNG magic number — its original upload name was `founder-mohammad-headshot.jpg.png`, and D-049 only renamed the visible extension to `.jpg` without noticing the real format underneath) — renamed to the correct `founder-mohammad-headshot.png` and the reference updated, rather than leaving a JPEG-named PNG being served with a mismatched `Content-Type`.
- **Crop fix, tuned empirically against the real image, not guessed once and shipped:** built a standalone 4-way comparison test harness (`crop-test.html`, scratchpad-only, not part of the app) rendering the actual source file at a legible size with different `object-position`/`transform-origin`/`scale` combinations, screenshotted via Playwright, and iterated twice — the first attempt (object-position bias + scale from default center) was visibly wrong (mouth/chin dominant, eyes cut off at the top edge) because a plain `object-position` shift plus a `scale()` from the box's default 50%/50% origin zooms into whatever sits at the object-positioned window's own center — which, worked out precisely from the source image's real proportions (face center at ~y=235 of 1448, but the cover-fit window is 1086px tall so its center can never be higher than source y=543, well into the shirt/collar), can never land on the face no matter what position value is chosen. Fixed by decoupling the two: `object-position: 50% 0%` (top-anchored source window) + an explicit `transform-origin` placed at the face's actual position within that window (~25% down) + `scale(2)` to zoom in around that anchor. Verified against the real rendered 96px/80px circle on the live page (not just the larger test harness), not assumed to transfer 1:1.
- **Closing photo, redesigned per the research finding, not just resized:** `ui-ux-pro-max` (`--domain landing`, `--domain ux`) returned 0 direct pattern matches for this specific need — flagged rather than silently falling back. `21st.dev search` surfaced a strong match: **"Editorial Testimonial"** (jatin-yadav05, id 9637) — a small ringed circular photo beside a bold pull-quote line, oversized faint index numeral, generous whitespace. Its carousel/navigation state machinery doesn't apply (one founder, not a rotating testimonial list), but its core pairing — small photo beside a pull-quote, not a large stacked photo — is exactly the pattern asked for, so that DNA was adapted into a static closing beat rather than importing the carousel wholesale.
- **The pull-quote is a verbatim excerpt of the bio's own first sentence** ("Running that business, rather than just studying the industry, is where the practical, business first approach behind Oragrol actually comes from."), not new copy — a pull-quote by definition re-displays existing text prominently; this is the same "excerpt, don't reword" principle already used for Mission's display-scale statement (D-048). The sentence appearing twice in the section (once in-line in the bio paragraph, once as the enlarged closing pull-quote) is intentional and standard for the pattern, not accidental duplication.
- **Sizing:** the photo shrank from D-049's `max-w-2xl` (672px) stacked block to `w-40 sm:w-56` (160px mobile / 224px desktop — 224px is exactly one-third of 672px, matching the "roughly one-third" instruction on desktop specifically). A `border-t` divider was added above the closing beat (new — D-049 had none) to give the redesigned section clearer rhythm between its three beats (identifier / bio / closing), addressing the "framing and text placement look unpolished" part of the complaint, not just the two explicit sizing/crop items.
- **A genuine side-effect improvement, verified rather than assumed:** D-049 logged a real mobile issue where the (then much wider) closing photo visibly overlapped the sitewide `EmergencyCta` pill, obscuring the founder's face. Re-checked at the same real scroll positions with the new, much smaller photo: the pill no longer overlaps the photo or the subject's face at all; it very slightly brushes the top-left corner of the photo frame at one scroll position (~300px) — a much smaller, more typical instance of the same standing D-044-class sitewide issue, not the "obscures the person's face" severity D-049 flagged. Still not fixed here (same reasoning as D-049: `EmergencyCta` is one global `RootLayout` instance, out of scope for a page-level task) but materially improved as a byproduct of the resize.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean. Zero horizontal overflow at 1440px/390px. Both images confirmed correctly wired via DOM query. Full-section screenshots (via `locator("section").screenshot()`, not `fullPage`) reviewed directly at both widths; the tuned crop additionally verified at its real rendered pixel size (not just the larger test-harness render) before being called done.
- **Status:** Superseded by D-051 — the top headshot this decision fixed was removed entirely the same session.

---

### D-051 — Top headshot removed; founder photo now appears exactly once, at the close
- **Date:** 2026-08-17
- **Context:** Mohammad instructed removing the small circular headshot from the top of Founder Bio entirely — name + "Founder & CEO" only there, no photo. The founder's photo now appears exactly once on the page: the closing placement (`public/images/founder-mohammad.jpg`) established by D-050, unchanged in size/treatment.
- **Decision:** Removed the top photo `<div>` (circle frame, `Image`, crop styling) from `founder-bio.tsx` entirely — not hidden/commented out. The remaining top block is just `H1` name + `Caption` title, centered, matching the same vertical rhythm as before minus the photo.
- **`founder-mohammad-headshot.png` (D-050's corrected asset) is no longer referenced anywhere** — confirmed via a repo-wide grep. Left in place on disk, unused, rather than deleted — same "kept for a possible revert" convention this project already applies to superseded visual assets (`StrataVisual`, `SchematicVisual` after their own supersessions).
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean. Zero horizontal overflow at 1440px/390px. DOM query confirms exactly one `<img>` inside the Founder Bio `<section>`, and its `src` resolves to `founder-mohammad.jpg` (the closing photo) — not the removed headshot. Screenshots at both widths reviewed directly, showing the top of the section with no photo present.
- **Status:** Built, verified, awaiting Mohammad's review.

---

### D-052 — Resources page built (Step 10): 6 articles, faceted filters, article template; nav re-pointed from `/insights` to `/resources`
- **Date:** 2026-08-17
- **Context:** Built `/resources` from the real content brief (`ORAGROL_RESOURCES_ALL_ARTICLES_FINAL.md`, 6 articles — Article 1 "What Is a Cyber Health Score?" marked Featured: Yes). Explicit requirements: research first (`ui-ux-pro-max` + `21st.dev`), a content grid (type/topic/summary/reading-time per card), faceted filters by Industry/Topic/Content Type that hide zero-match options, an article detail template (breadcrumb, meta, body with H2/H3 hierarchy, "One next action," the exact per-article CTA from the source file's mapping table), and no invented stats/claims/author bios.
- **Route mismatch found and resolved, not silently worked around:** `SiteHeader`/`SiteFooter` already had a "Resources" nav item, but it pointed to `/insights` (an unbuilt placeholder target, D-017) — the content brief's own `Slug` fields are all `/resources/<slug>`. Updated both nav files to `/resources` rather than building at `/insights` and leaving the slugs inconsistent with the real content brief; `home/insights.tsx`'s own "coming soon" teaser section was deliberately left untouched (out of scope for this instruction — flagged, not silently updated or removed) and now reads slightly stale next to a live `/resources` page.
- **Research done first, per instruction:** `ui-ux-pro-max` (`--domain landing` for "cybersecurity knowledge center resource hub blog" — 0 matches, flagged rather than silently falling back; `--domain ux` for filter/faceted-grid guidance, which surfaced real, applied findings: chip-collection reflow — must wrap, never clip, overflow filter chips — and lazy-loading below-fold content). `21st.dev search` surfaced three real matches, one of which (`get_component` on "Filter Grid," id 23525 — segmented radio chips with live per-filter counts) hit this session's 21st.dev daily retrieval-limit before its code could be pulled; its *pattern* (not literal code) was rebuilt natively and is flagged as such in `resources-explorer.tsx`'s own comment, not presented as sourced. "Blog Posts" (id 5622, hero-style asymmetric grid — one large primary card, smaller secondary cards) and "Link Breadcrumb" (id 18132) were successfully retrieved and informed the featured-card layout and breadcrumb structure respectively — both real code reviewed, neither imported wholesale (no photography in the source data to drive an image-background card like the reference's own demo; adapted to this site's established dark-panel/`GlowEffect` material instead, D-013).
- **Content transcribed verbatim, then verified byte-for-byte — not just claimed accurate:** all 6 articles' full bodies, all metadata fields (title/slug/topic/industry/SEO title/SEO description/summary/featured), and every "One next action" line were typed into `articles-data.ts` by hand from the source file, then checked by three separate Node scripts run against the actual source `.md` (not re-read by eye a second time): one diffing normalized body text (strips only markdown syntax — `**`, `-`, heading markers — then compares full text), one diffing every metadata field per article, one specifically checking heading-level fidelity. The heading-level script caught a real transcription mistake before shipping: Article 1's four subheadings had been typed as `h2` while Articles 2–6's were `h3`, not because the source varies (it doesn't — every article uses `###` uniformly for its one tier of subheadings) but because of an initial inconsistent guess. Resolved by making all 6 articles' subheadings `h2` uniformly — the semantically correct choice for the rendered page (`H1` title → `H2` subsections, no skipped level; there's no second heading tier in any article to justify `H3`), not a literal mirror of the source `.md`'s own internal `##`/`###` document-structure markers, which aren't a hierarchy signal to preserve on the rendered page. All three scripts passed clean after the fix.
- **CTA text/hrefs** come directly from the source file's own "Article-to-Oragrol Mapping" table, verified via a live Playwright check of the rendered `<a>` element's text and `href` (MFA's article confirmed to render exactly `"Talk to Oragrol About Your Security Controls"` linking to `/contact`), not just copied into the data file and assumed correct. "Get Your Cyber Health Score" routes to `/cyber-health` (the internal page every other such CTA site-wide uses — FinalCta, etc.), not the external Tally URL directly; every other CTA is a "talk to us" variant with no other real destination, routed to `/contact` (matching Services'/Industries' "Talk to Oragrol" convention).
- **Faceted filtering, not independent per-dimension filtering:** each of the three filter groups (Content Type / Topic / Industry) computes its own visible chips from the OTHER two active filters, so a chip whose combination would currently match zero articles is never rendered — verified live (selecting Industry: Professional Services immediately hides "Article" from Content Type and every topic except "SMB Security / Industry," since the one Professional-Services piece is the sole Guide) rather than just asserted from source. Single-select per dimension (not multi-select checkboxes), matching the instruction's "by Industry..., by Topic, by Content Type" framing as three independent questions.
- **Two real `cn()`-conflict bugs caught and fixed before shipping** (this project's own documented gotcha — a component's own base classes plus a conflicting `className` override emit two competing same-property utilities with unpredictable ordering, see typography.tsx's own H3/Caption comments and D-018/D-048's prior instances of the identical mistake): `ArticleCard`'s title initially passed `text-xl md:text-xl` to the shared `H3` (which already has its own `text-2xl.../md:text-3xl` base), and `ArticleDetail`'s body-heading renderer initially passed `md:text-3xl` to the shared `H2`. Both fixed by dropping the conflicting size override and keeping only non-conflicting classes (spacing), same resolution D-018/D-048 already used for this exact class of bug.
- **Reading time calculated from real word count** (`app/lib/reading-time.ts`, 200 wpm, rounded up, minimum 1 minute) — not a hardcoded per-article guess, per the explicit instruction.
- **No `FinalCta` on either the grid or article-detail pages** — the grid's whole purpose is surfacing a next step (one of the 6 articles or their filters), and each article page already has its own specific, per-article-mapped CTA (the "One next action" panel); stacking the generic site-wide CTA on top would compete with/dilute the specific one, same reasoning Contact used (D-046) for skipping `FinalCta` there.
- **No hero copy was supplied** for the `/resources` listing page (the content brief is per-article only) — flagged rather than invented marketing language; the hero subhead is grounded in the source file's own explicit "Positioning" note ("Cybersecurity-first for Canadian SMBs") rather than new claims.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean (`/resources` static, all 6 `/resources/<slug>` pages statically generated via `generateStaticParams`, an unknown slug confirmed to 404). Server-rendered HTML fetched directly via `curl` confirms all 6 titles present verbatim. Playwright: zero horizontal overflow at 1440px/390px; zero console errors on both the grid and an article page; heading order on an article page confirmed correct (one `H1`, then `H2`s, no skips); reduced-motion context confirms `Reveal`-wrapped content renders at `opacity: 1`. Full-page and filtered-state screenshots at both widths reviewed directly before reporting anything as done.
- **Status:** Built, verified, awaiting Mohammad's review. `home/insights.tsx`'s stale "coming soon" teaser (flagged above) and the sitewide `EmergencyCta` overlap-at-some-scroll-positions issue (D-044-class, unrelated to this page specifically) are both carried forward, not addressed here.

---

### D-053 — Services nav gets a minimal capability dropdown; content sourced from the live /services page, not the named (nonexistent) content file
- **Date:** 2026-08-17
- **Context:** Instructed to add a hover/tap dropdown mega-menu to the "Services" item in `SiteHeader`, listing core service capabilities linked to their anchors on `/services`, sourced from `oragrol_services_page_content.md`'s section headers. A full repo search (glob for the filename, `git log --all --full-history` for it ever existing in any commit) found no such file anywhere in this repo, past or present.
- **Decision:** Rather than invent capability names to satisfy a source that doesn't exist, used the actual confirmed, already-shipped content: `/services`' own locked capability data — `app/components/sections/services/live-services.tsx`'s `LIVE_SERVICES` (capabilities 01-04) and `additional-capabilities.tsx`'s `FINALIZING_SERVICES` (05-08) — and their real `capability-0N` DOM ids, the same ids `CategoryNav` already jump-scrolls to on the page itself. The dropdown's 8 links are `/services#capability-01` through `#capability-08`, labeled with the exact same 8 names already live on the page (Virtual CISO, Risk Assessment & Compliance, Vulnerability Assessment & Management, Security Awareness Training, Managed Security Services / 24/7 MDR, Penetration Testing, Endpoint Protection / EDR, Incident Response). The instruction's own example anchor format (`#capability-name`, implying a slugified name) doesn't match these real, numbered anchor ids — used the real ones (a slug anchor would silently fail to scroll, matching no element on the page) rather than following the example literally into a broken link.
- **Implementation:** New `app/components/site/services-nav-dropdown.tsx` (`ServicesNavDropdown`), one component with `variant: "desktop" | "mobile"`. Desktop: hover/focus-opened absolute panel below the existing `NavLink` trigger. Mobile: inline disclosure (chevron toggle) inside the existing full-viewport mobile nav panel. Wired into `SiteHeader` via a conditional in the existing `NAV_LINKS.map` (desktop row + mobile panel) — every other nav item's render path is untouched. Styling reuses existing tokens only: `bg-surface`/`border-border`/`shadow-xl` (same recipe as `HeaderSearch`'s floating panel), `font-heading` (Space Grotesk) on the small "Capabilities" panel label, `font-body` (Inter) on links, `text-accent` (#018ABE) on hover/focus/active — no new colors, no promo cards or imagery.
- **Behavior:** Tab-to-open (via `onFocus`, then Tab continues naturally into the panel's real DOM-order links), ArrowDown/ArrowUp move focus between items, Escape closes and returns focus to the trigger, outside click closes it, closes on route change (render-time state reset, same pattern `SiteHeader` already uses for `mobileOpen` — not an effect) and explicitly on every item click (covers same-page anchor navigation, where `pathname` alone doesn't change), `prefers-reduced-motion` swaps the transition for an instant `hidden`/`block` toggle. Desktop hover-close is debounced ~150ms so the visual gap between the trigger and panel (not itself covered by either element) doesn't cause a premature `mouseleave` mid-transit on a diagonal mouse move.
- **Bug caught during verification, fixed before shipping:** the Escape handler's programmatic `.focus()` call on the trigger re-triggered the same `onFocus` that opens the panel on a real Tab-in, reopening it on the same keypress (`aria-expanded` stayed `"true"` instead of flipping to `"false"`). Fixed with a one-shot `suppressNextFocusOpenRef` flag consumed by the very next `onFocus`.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean. Full Playwright pass against the running dev server: desktop hover open/close, full keyboard flow (Tab → Arrow keys → Escape), outside-click close, reduced-motion instant toggle, mobile chevron expand/collapse, click-through navigation to `/services#capability-01`, hover-state color computed exactly `rgb(1, 138, 190)` (#018ABE), panel heading computed font-family `"Space Grotesk"`, link computed font-family `Inter`, and confirmed every other nav item (Cyber Health, Company, etc.) still renders with no `aria-haspopup`/chevron — untouched.
- **Status:** Superseded by D-054 — the flat single-column layout described here (`bg-surface` panel, flat 8-item list) was restructured into grouped columns with a `bg-background` panel the same day; the content-sourcing decision (real `/services` data, real `capability-0N` anchors, no invented names) carries forward unchanged.

---

### D-054 — Services dropdown: real nav-bar bg token + restructured into Bell.ca-style grouped columns (4 categories, picked from 3 shown options)
- **Date:** 2026-08-17
- **Context:** Two follow-ups to D-053, same session. (1) Mohammad reported the dropdown panel looked semi-transparent with hero text bleeding through. (2) Mohammad asked to restructure the flat 8-item list into Bell.ca-style grouped columns (structure only, explicitly not Bell's promo-card/image treatment), sourced from `oragrol_services_page_content.md`'s grouping if any exists, or proposed by Claude and approved before building if it doesn't.
- **Bg-token decision:** Checked the panel's computed style directly (Playwright) at scroll 0 and scrolled past the header's own fade — `backgroundColor` was already opaque (`rgb(20,20,20)`, `opacity:1`, no `backdrop-filter`) in both states; the reported transparency didn't reproduce in automated testing. Made the fix anyway, per the explicit instruction to check the Nav component and match its real token rather than guess: the panel had been using `--surface` (#141414, the same "floating card" token `HeaderSearch`'s own overlay uses), not literally the nav bar's own background. Read `SiteHeader` directly — its own visual background comes from `--background` (#0a0a0a, `palette-black-900`), not `--surface`. Swapped the panel's `bg-surface` → `bg-background`. Re-verified via computed style (now `rgb(10,10,10)`) at both scroll states, and fresh screenshots.
- **Grouping decision:** Re-checked (second time, same method as D-053: glob + full git history) — `oragrol_services_page_content.md` still doesn't exist anywhere in this repo. Read the real per-capability copy already on `/services` (the `problem`/`whatWeDo`/`whatYouGet`/`outcome` fields in `live-services.tsx`, and the one-line `copy` field in `additional-capabilities.tsx`) and built 3 candidate groupings from it — shown to Mohammad via `AskUserQuestion` with a preview of each option's columns, none finalized silently. He picked the 4-column, evenly-paired "stage" option:
  - **Advisory** — Virtual CISO (`#capability-01`), Risk Assessment & Compliance (`#capability-02`)
  - **Assessment & Testing** — Vulnerability Assessment & Management (`#capability-03`), Penetration Testing (`#capability-06`)
  - **Protection & Monitoring** — Managed Security Services / 24/7 MDR (`#capability-05`), Endpoint Protection / EDR (`#capability-07`)
  - **Response & Training** — Incident Response (`#capability-08`), Security Awareness Training (`#capability-04`)
- **These 4 category names are new synthesized copy, not present verbatim in any existing source** — Claude's read of the real per-capability descriptions into a short theme label, approved by Mohammad for this nav menu specifically. Flagged in the component's own header comment: if reused as customer-facing copy anywhere beyond the nav dropdown, it needs its own separate content sign-off (per CLAUDE.md's rule against inventing confirmed-sounding business copy).
- **No category-level anchor exists on `/services`** (only the 8 `capability-0N` ids) — each category title links to the first capability listed under it, in this component's own display order, rather than a fabricated section anchor that doesn't exist on the page.
- **Implementation:** `app/components/site/services-nav-dropdown.tsx` restructured around a new `SERVICES_CATEGORIES` array (`{ label, href, items[] }`) with a flattened `FLAT_ITEMS` derived via `.flatMap()` driving the existing ArrowUp/ArrowDown roving-focus logic unchanged (still ordered 8 items, just now column-then-row across the 4 groups). Desktop panel: `grid-cols-4`, widened from `w-72` to `w-[820px]`, kept `left-0`-anchored rather than centered on the trigger (centering was considered and rejected — the trigger sits near the nav's left edge, right after the logo; centering an 820px panel there would push its left edge off-screen). Mobile: the existing single chevron-toggle disclosure now expands into 4 stacked category groups instead of one flat list, same one control.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean. Playwright: zero horizontal page overflow and panel fully within the viewport at 1024/1280/1440/1920px (panel right edge never exceeds 1345px even at 1920px viewport); category titles + hrefs and all 8 item labels + hrefs read back correct; 9-press ArrowDown roving sequence wraps correctly back to item 1; Escape closes (`aria-expanded` → `false`); category-title font-family `"Space Grotesk"`, item font-family `Inter`; category-title hover color confirmed exactly `rgb(1, 138, 190)` (#018ABE) via a real `:hover`-pseudo-class match; mobile expand shows all 4 category headings + 8 items in the correct grouping with zero horizontal overflow. One test-script flake (a hover-color read briefly returned the muted/default color inside a script hammering keyboard+mouse interactions back-to-back on one page instance) was isolated and confirmed to be a script artifact, not a product bug, via a clean single-purpose repro before being dismissed.
- **Noted, not fixed (pre-existing, unrelated):** at exactly the 1024px `lg` breakpoint, the desktop nav row is already cramped independent of this change (Cyber Health/CTA/EN|FR all wrap) — confirmed pre-existing by reading `NavBar`/`SiteHeader`, no `NAV_LINKS` item besides Services was touched. Flagged for a future pass, not addressed here.
- **Status:** Superseded (component-wise) by D-055 — `services-nav-dropdown.tsx` was generalized into `nav-dropdown.tsx`'s `NavItemDropdown` the same day so Solutions/Industries/Resources could share it; Services' own content/grouping/styling described here is unchanged, only the file/component name and the "Services-only" framing no longer apply.

---

### D-055 — Nav dropdowns extended to Solutions/Industries/Resources (content-first, per-item go/no-go); Services generalized into shared NavItemDropdown
- **Date:** 2026-08-17
- **Context:** Instructed to extend the Services-style dropdown pattern to the rest of the main nav (Solutions, Cyber Health, Industries, Resources, Company) — but ONLY where the page has real, distinct sub-content already; explicitly not to invent sections to fill a menu. Required reporting the per-item determination + proposed structures and waiting for confirmation before building.
- **Investigation (before any code):** read every target page's `page.tsx`, section components, and data files directly.
  - **Cyber Health / Company: no real independently-navigable sub-content** — both a single narrative flow, zero `id`s anywhere in their section files. Stayed plain `NavLink`s, matching Mohammad's own stated examples.
  - **Solutions:** 3 real tiers (`TierCards`) + 1 real add-on (`PentestAddon`) — real content, but no anchor ids existed on the page at all.
  - **Industries:** 9 real, distinct industries (`INDUSTRIES` in `industries-data.ts`) — but `IndustriesExplorer` is a client-state ARIA Tabs widget (WAI-ARIA Tabs pattern, `useState`), not scroll-anchored sections; no working per-industry link existed.
  - **Resources:** 6 real, already-live article pages (`/resources/<slug>`) — nothing to add. Checked whether the 3 filter facets (contentType/topic/industry) grouped naturally: every article has a UNIQUE topic, the other two facets split 5:1 — no natural multi-item grouping exists.
- **Decision, reported and confirmed via `AskUserQuestion` before building:**
  - Solutions: build it — add small structural `id`/`scroll-mt-28` anchors to the 3 existing tier cards + the add-on block (no visual/content change), single-column dropdown of 4 items (no forced sub-grouping — 4 items don't need it).
  - Industries: build real deep-linking (the bigger-scope option) rather than skip or link to fake anchors — added `location.hash`-driven tab selection to `IndustriesExplorer`, single-column dropdown of all 9 industries in the page's own existing order. Explicitly did NOT group the 9 into an invented sector taxonomy (e.g. "Professional & Financial" / "Operations & Trade" / "Public-Facing & Care") — unlike Services' groups (real per-capability copy grounded them), the source content doesn't group these 9 anywhere, so a flat list was the more honest structure.
  - Resources: single-column dropdown of the 6 real articles (no category headings — the trigger itself already covers "hub" navigation to `/resources`).
- **Implementation — Industries deep-linking:** extracted `slugifyIndustryName()` into `industries-data.ts` as the single source of truth for both the explorer's tab ids and the nav dropdown's hrefs (`/industries#industry-<slug>`), so the two call sites can't drift apart. `IndustriesExplorer` reads the hash on mount + on `hashchange` (same-page navigation, since the component doesn't remount just because the hash changes) to select the matching industry and scroll it into view.
- **Real pre-existing bug found and fixed while wiring this up:** `IndustriesExplorer`'s mobile and desktop tablists render simultaneously (CSS `hidden`/`lg:hidden` only hides one visually; both stay in the DOM) and previously shared the exact same tab `id`. `document.getElementById` always resolves to the first DOM match — the mobile row, rendered first in JSX — which silently broke the existing Arrow-key-focus-follow behavior on desktop keyboards (a latent bug from before this session, only now consequential because these ids became real external anchor targets). Fixed by giving each orientation a distinct id (desktop keeps the real slug; mobile gets a `-mobile` suffix) — only the desktop id is ever linked to.
- **Services generalized:** the Services-only `ServicesNavDropdown` (D-053/D-054) was rewritten as a generic `NavItemDropdown` (`app/components/site/nav-dropdown.tsx`), driven by a `NavDropdownConfig` (columns, optional per-column heading, desktop panel width/layout classes) instead of hardcoded Services data. `SiteHeader` now looks up `NAV_DROPDOWNS[link.href]` per nav item instead of a single `label === "Services"` conditional. `services-nav-dropdown.tsx` deleted.
- **Chevron added to every dropdown trigger** (desktop: inline inside the `NavLink`, rotates via Tailwind's `rotate-*` on open; mobile: on the existing separate toggle button) — the "there's more here" visual signal, per the explicit instruction. Cyber Health/Company render none.
- **Second real interaction bug found and fixed during verification:** the outside-click-closes-it listener was on `pointerdown` (D-053's original choice, so a click registers before a full click gesture completes). With only one dropdown ever on the page (D-053/D-054), this was invisible. With four dropdowns now coexisting, closing an open dropdown on `pointerdown` collapses/reflows the page BEFORE the same click's `mouseup` fires — Chromium resolves a click's actual target via `elementFromPoint` at mouseup, so if that reflow shifted a lower dropdown's own trigger out from under a stationary pointer, the click missed it entirely (reproduced live: mobile, Services open, click Solutions' chevron → Services correctly closes, but Solutions' own click is lost, never opens — confirmed via a mid-transition screenshot and an `aria-expanded` trace, not just inferred). Fixed by switching the listener from `pointerdown` to `click`: React's own delegated click on the actual clicked element fires first (its listener sits lower in the bubble path than a document-level listener added later via `useEffect`), so a lower trigger's own toggle always lands before this outside-close logic runs, regardless of any reflow it goes on to cause afterward.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean (same 20 routes). Full Playwright pass: all 4 dropdowns' structure/hrefs/panel-bg/chevron-presence/chevron-rotate (`rotate: 180deg`, not the legacy `transform` property — an early check read the wrong CSS property, confirmed and corrected before concluding anything); Cyber Health/Company confirmed to have neither `aria-haspopup` nor a chevron; zero horizontal overflow at 1024/1280/1440 with each dropdown open; Industries deep-link verified both via direct URL load and via clicking the dropdown item from another page; all 4 new Solutions ids resolve; a Resources guide-article URL returns 200; mobile sequential-click accordion behavior (each dropdown opens, the previous one correctly auto-closes) re-verified correct after the outside-click fix.
- **Status:** Built, verified, awaiting Mohammad's review.

---

### D-056 — Industries dropdown "click doesn't navigate" report: `cancelScheduledClose` hardened to `pointerdown`, no deterministic repro found
- **Date:** 2026-08-18
- **Context:** Mohammad reported that clicking an item in the Industries nav dropdown (e.g. "Healthcare") from a non-Industries page closes the dropdown but doesn't navigate. Instructed to reproduce before fixing, and to confirm any fix against the real URL + the correct industry tab landing selected/scrolled into view, not just DOM presence of the link.
- **Investigation:** extensive live reproduction via `claude-in-chrome`, instrumented with real DOM event listeners (`pointerdown`/`mousedown`/`mouseup`/`click`/`mouseenter`/`mouseleave`) and `elementFromPoint` checks, across both `/services` and `/` as starting pages. Result was genuinely inconsistent: some hover→click attempts navigated correctly, others left the URL unchanged with the panel still open (`aria-expanded="true"`) and *zero* mouse events reaching `document` at all — despite the target `<a>` being confirmed, via `elementFromPoint` at its exact real coordinates, to be correctly positioned, topmost, and hover-active (CSS `:hover` engaged) at that instant. A full, faithful synthetic replay of the real event sequence (`mouseover` trigger → `mouseout`/`mouseover` transition into the item → `mousedown`/`mouseup`/`click`, dispatched via JS `MouseEvent`, exercising the actual React handlers rather than bypassing them) navigated correctly every time it was tried, including from a second page and a second industry. Direct `el.click()` also always succeeded once given a moment for Next's async client-side transition (one earlier apparent "failure" was a testing-methodology artifact: checking `location.href` synchronously in the same script as `.click()`, before the async route transition had resolved — not a real failure, caught and corrected before drawing conclusions from it).
- **Conclusion:** could not isolate a single, deterministic product-code mechanism that reliably reproduces the bug — the zero-event failures are most consistent with the `claude-in-chrome` browser-automation tool's own `left_click` action occasionally not dispatching the mousedown/mouseup portion of a two-step hover-then-click gesture (unrelated to page content: reproduced on both `/` and `/services`, and immediate retries at identical, unchanged page state sometimes succeeded). This is reported honestly rather than claimed as a confirmed root cause.
- **Fix applied anyway, on its own merits:** `NavItemDropdown`'s desktop hover-close debounce (`scheduleClose`, `CLOSE_DELAY_MS = 150`) only had its `cancelScheduledClose()` counterpart wired to `onMouseEnter`. A close armed by an earlier brief mouseleave (e.g. an imprecise diagonal move crossing the trigger→panel gap) stays pending until either a fresh `mouseenter` cancels it or its 150ms timer fires — and nothing previously canceled it at the moment of an actual click. If that timer fires between a real mousedown and mouseup, `setOpen(false)` makes the panel `invisible` (`visibility: hidden`, non-hit-testable) mid-click, swallowing it before it reaches the `<Link>` — which would look exactly like the reported symptom. Added `onPointerDownCapture={cancelScheduledClose}` on the container: any pointerdown inside the trigger or panel now cancels a pending close before the click's own handlers run. Safe no-op when nothing is scheduled; doesn't touch the mobile variant (no hover debounce exists there).
- **Alternatives considered:** reverting to `/services`-only nav links (rejected — would undo real, wanted D-055 functionality over an unproven root cause); leaving the code as-is pending a cleaner repro (rejected — the pointerdown gap is a real, narrow correctness issue in the debounce logic regardless of whether it's THE cause here, and the fix is risk-free).
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean (same 20 routes). Post-fix, a full synthetic real-gesture replay (see above) from two different starting pages, targeting two different industries, both landed on the correct `/industries#industry-<slug>` URL with the correct tab visibly selected (active/blue) and its Risk/Priorities/Approach/Next Step content scrolled into view — confirmed via screenshot, not just URL/DOM inspection.
- **Status:** Fix shipped; root cause not conclusively proven. Flagged for Mohammad to confirm live with a real mouse whether the original symptom recurs.

---

### D-057 — Home hero: 16-frame city-skyline sequence replaced with "The Perimeter Sweep" (ring motif, scroll-pin removed)
- **Date:** 2026-08-18
- **Context:** Instructed to design and build a genuinely new, distinctive Home hero visual to replace the 16-frame photo sequence (frozen as-is under [[D-006]]) — explicitly not a reused pattern from another page, grounded in the actual MSSP subject rather than generic tech, and required to propose one concept and get it approved before writing any code.
- **Verification before proposing:** confirmed the ring visible in the old hero was NOT a separate code layer — it's baked into the 16 AI-rendered PNG frames as flat pixels (`hero.tsx` only ever crossfaded images). A real, reusable `OragrolRing` component already existed but was unused in the Hero, used only as a static low-opacity watermark on dark CTA sections elsewhere (`FinalCta`, Cyber Health's and How We Work's closing CTAs).
- **Concept proposed and approved:** "The Perimeter Sweep" — the Oragrol brand ring (reserved to Home per [[D-008]], "not reused elsewhere") rendered large and mostly off-canvas behind the headline, with exactly one motion behavior: a short, bright arc continuously tracing the ring's circumference on a slow (~11s) loop. Rationale: a continuous, unhurried boundary scan literalizes what an MSSP actually does (watch continuously, without gaps), unlike a stock photo (says nothing cybersecurity-specific) or an abstract node-graph (the industry's most overused cliché, and already this site's own language for Services/Solutions — reusing it on Home would blur D-008's per-page-distinct-motif rule). Approved alongside removing the old hero's 350vh scroll-pin (it existed only to stage the 16-frame crossfade against `scrollYProgress`, [[D-005]]; with no frames left to crossfade, there's nothing left to pin).
- **Two real rendering bugs found and fixed during build, not shipped as first-drafted:**
  1. Reusing `OragrolRing` verbatim at hero scale (~760px) rendered as a solid grey disc, not a hairline ring — its stroke (26 units on a 150-unit viewBox, calibrated for small-logo legibility) is nearly half its own radius, so it doesn't scale up as a delicate line. Confirmed live via screenshot, not assumed. Fixed by building a new circle at the same identity (near-full-circle, one gap, 35° rotation) but with a stroke width calibrated for large-scale background use — same ring language, re-parameterized for a context `OragrolRing` was never designed for. `OragrolRing` itself is untouched and still used everywhere it already was.
  2. An ambient-glow layer built as a `blur-3xl`'d filled circle had the same problem for the same reason — a Gaussian blur only softens a filled shape's edges, it doesn't make the shape read as faint, so at any usable opacity it rendered as a large grey blob dominating the frame. Replaced with a true CSS `radial-gradient` (fades to fully transparent well inside its own bounds), which is what "ambient" actually needed.
- **A third real bug, animation-specific:** the sweep's continuous rotation, first built with framer-motion (`motion.circle animate={{rotate:360}}` + `initial={false}` + `transition={{repeat:Infinity}}`), never actually animated — `initial={false}` skips the mount-to-target transition entirely for a constant `animate` value, which also skips starting the `repeat` loop, so it silently snapped to a static end-state and sat there (confirmed via `getComputedStyle`/`getAnimations()`, not assumed). Rather than debug framer-motion's SVG-rotation handling further, switched to a plain CSS `@keyframes` (`perimeter-sweep`, added to `globals.css` — same established pattern as `ai-loader-letter-fade`, the project's first custom keyframe) applied via Tailwind's `animate-[...]` syntax, matching this codebase's own existing precedent for continuous rotation (`RotatingRing`'s `animate-spin`) instead of introducing a new, less-proven mechanism. Reduced motion is handled by simply omitting the animation class rather than toggling framer-motion props.
- **A fourth real bug, mobile-specific:** the ring's desktop/tablet position (vertically centered, bled off the right edge) was reused unchanged at mobile widths and bled directly into the headline/body text column, since copy runs nearly full-bleed at narrow widths — a real violation of the approved "never sits behind the text" constraint, caught via screenshot rather than assumed to just scale down safely. Fixed with a mobile-specific position: bottom-right-corner-anchored instead of vertically centered, clear of the copy stack above it, verified via a fresh screenshot at both 390px and 360px with zero horizontal overflow.
- **Verification note:** the `claude-in-chrome` browser tab used for interactive checks reports `document.hidden === true` regardless of focus (the same environment quirk already documented at [[D-043]]), which freezes CSS/rAF animations and made the sweep look static during live interactive checks. Per CLAUDE.md §7's documented method, switched to a Playwright script (`playwright-core`, run from the scratchpad with `NODE_PATH` pointed at this repo's `node_modules`) to get a real foreground page and confirm the animation genuinely progresses over time (`getAnimations()[0].currentTime` advancing across samples, transform matrix changing), not just configured correctly.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean (same 20 routes, no new ones). Via Playwright: the sweep's rotation genuinely advances over multiple timed samples (motion on); freezes with zero animation instances under `reducedMotion: "reduce"` emulation; zero horizontal overflow at 360/390/834/1440px; zero console errors on load; the "The Challenge" section (and everything below it) now appears directly after one screen-height of scroll instead of after the old 350vh pin, confirmed via scroll-position screenshots (not the naive Playwright `fullPage` capture, which — per the same known artifact noted in D-048 — mis-composited the page's `fixed` header/CTA pill into a stitched screenshot; a real methodology note, not a product bug). Full-size desktop/tablet/mobile screenshots sent to Mohammad for review.
- **Files changed:** `app/components/sections/home/hero.tsx` (rewritten — no more `useScroll`/`scrollYProgress`/350vh pin/16-frame array), new `app/components/sections/home/perimeter-ring.tsx`, `app/globals.css` (new `perimeter-sweep` keyframe). The 16 source frames (`public/hero/frame-*.png`) are left on disk, unreferenced — same convention this project already uses for other superseded assets (e.g. D-050's unused founder headshot) — confirmed via repo-wide grep that nothing else references them.
- **Status:** Built and verified. Awaiting Mohammad's review of the live result.

---

### D-058 — GlowEffect console warning: "static" mode fixed to not animate through Framer Motion at all
- **Date:** 2026-08-18
- **Context:** Mohammad reported a repeating dev-console warning: "You are trying to animate background from 'rgba(0,0,0,0)...' to 'linear-gradient(to right, var(--color-accent-light), var(--color-accent), var(--color-accent-strong))'. ... is not an animatable value." Instructed to find the component, verify the exact mechanism rather than guess, fix it while preserving the originally-intended visual effect, and confirm live (reproduce the original warning, confirm it's gone, confirm the visual is still correct — not just that the console is quiet).
- **Found:** `GlowEffect` (`app/components/sections/services/glow-effect.tsx`), a 21st.dev-sourced component reused at ~16 call sites across nearly every page (Services hero + 8 capability visuals, Company's founder-bio/values/mission-story, How We Work's cycle-visual, Resources' article-card/article-detail). All 16 call sites use the default `mode="static"` — none pass an explicit `mode`. `static`'s one fixed gradient was routed through `animate={{ background: "linear-gradient(...)" }}`, which asks Framer Motion to interpolate FROM the element's initial transparent background TO the gradient on every mount — despite the component's own header comment already documenting `static` as "one fixed gradient, **no animation loop**."
- **Verified the exact mechanism empirically, not assumed** (per instruction): built a throwaway diagnostic route (`app/dev-glow-test/page.tsx`, deleted immediately after use, never committed) rendering three `motion.div`s side by side — the real `var(--color-accent...)`-based gradient, the identical gradient with colors resolved to literal hex, and a plain solid hex color. Only the `var()`-based gradient produced the warning; the hex-resolved gradient and the solid color did not. This confirms the user's own diagnosis was correct: Framer Motion's animatable-value check can't resolve CSS custom properties at animation-setup time, so a `var()`-built gradient always fails it, regardless of `mode`.
- **Also confirmed on the real, live page (not just the isolated test):** used `git stash` to temporarily restore the pre-fix component, loaded `/services` (9 mounted `GlowEffect` instances: hero + 4 full capability spotlights + 4 compact marks) via a Playwright script, and captured exactly 9 warnings firing on page load alone — reproducing the reported "keeps repeating" symptom exactly (one instance, one warning, times nine, all at once). Confirmed hovering/scrolling the page afterward adds zero *new* warnings — this fires at mount, not per-interaction. `git stash pop` restored the fix before re-testing.
- **Decision — neither of the two offered options, on inspection:** the instruction offered (a) animate opacity/a static color instead of the gradient, or (b) resolve the CSS variables to hex before animating. Checked what `static` mode was actually designed to do first, per the explicit instruction not to guess: its own header comment already states "no animation loop" — it was never supposed to transition at all. Framer Motion's failure to interpolate a gradient meant it was always silently snapping straight to the final gradient with zero real transition anyway (confirmed via the mechanism above — this was pure console noise, not a broken animation), so animating *anything* here — even a resolved-hex version of the same gradient, even just opacity — would mean adding motion that was never part of the design, not preserving it. The actual fix: stop asking Framer Motion to animate `static` mode's background at all. Its gradient is now set as a plain CSS value directly on `style` (same `var()`-based colors as before — no issue for real CSS, only for Framer Motion's interpolator), and `animate` isn't passed for this mode. Same exact gradient, same colors, same instant appearance; the only change is which system renders it.
- **Not fixed, flagged instead:** the component's other modes (`rotate`/`pulse`/`breathe`/`colorShift`/`flowHorizontal`) DO still animate a `background` built from these same `var(--color-accent...)` colors, and would hit this identical warning (and likely the identical "just snaps, no real crossfade" non-behavior) the moment any of them is ever actually used — confirmed via grep that none currently are, anywhere in this codebase. Resolving `colors` to concrete hex/rgb (confirmed above to work) is the fix for whoever activates one of those modes later; left as a documented comment in the component rather than a speculative, unverifiable change shipped now.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean (same 20 routes — the throwaway diagnostic route is gone, confirmed in the route list). Post-fix, reloaded `/services` via Playwright: 0 warnings on load, 0 new warnings after the same scroll+hover interaction sequence used to reproduce the bug. Confirmed the visual is unchanged, not just quiet: the glow element's `getComputedStyle` background resolved to `linear-gradient(to right, rgb(151, 202, 219), rgb(1, 138, 190), rgb(1, 124, 172))` — exactly `#97CADB`/`#018ABE`/`#017CAC`, the same three accent tokens, same order — with its `opacity-40` className still applying correctly alongside the inline style. Screenshotted the hero's glow and a capability-spotlight card's glow directly; both render identically to how they always have. Spot-checked three more pages using the same component (`/company`, `/how-we-work`, a `/resources/[slug]` article) — 0 warnings on all three.
- **Status:** Fixed and verified.

---

### D-059 — Home hero ring moved to a Spline-hosted 3D scene, superseding D-008 for this one component
- **Date:** 2026-08-18
- **Context:** Instructed to replace `PerimeterRing` (the SVG "Perimeter Sweep" ring, D-057, built and approved earlier this same session) with a Spline-exported 3D scene, via `@splinetool/react-spline`/`@splinetool/runtime` and a scene hosted at `https://prod.spline.design/V1tyQ1GeBELqa-K1/scene.splinecode`.
- **Initial pushback, then resolved:** the instructions described the current ring as "likely a canvas/Three.js component" backed by "Blender GLB assets" — neither is accurate for this repo (confirmed via grep: zero `.glb`/`.gltf` files anywhere; the actual predecessor is `PerimeterRing`, pure SVG/CSS, D-057). Raised this, plus that a first preview of the given Spline URL (via the standalone `@splinetool/viewer` web component, an old pinned CDN version) rendered a generic 3D volume-knob/dial widget with no visible relationship to the Oragrol ring or brand colors, and that this project has a standing decision (D-008) favoring code-generated visuals over externally-hosted ones. Mohammad confirmed: (1) this is a deliberate, explicit architecture change for this one component, not an oversight — logged here as the supersession; (2) the URL is correct — verified by fetching the raw `.splinecode` file and finding real node names (`GapCutter`, `InnerLipFlat`, `OuterRimFlat`, `GrooveLine`, `GlowSeg1`–`110`) present in it; (3) the volume-knob render was attributed to the preview method (old pinned viewer), not the scene itself, and the real integration (current `@splinetool/react-spline`, live Next.js dev server) was asked for as the actual test.
- **Decision:** `PerimeterRing` (code-generated SVG/CSS) is superseded, for the Home hero ring specifically, by a Spline-hosted 3D scene rendered via `@splinetool/react-spline`. D-008's "code-generated, not externally-hosted" convention remains in force for every other page/motif on the site — this is a narrow, explicit exception for one component, not a reversal of the broader policy. `PerimeterRing` (`perimeter-ring.tsx`) is left in the codebase untouched and unreferenced, specifically so this is a one-line import revert in `hero.tsx`, not a rebuild, if needed.
- **Re-verified independently before building, not just taking either side's word for it:** re-fetched the raw scene file myself and confirmed the same node names the correction cited are genuinely present in the binary (not coincidental substring matches — multi-word compound names like "GapCutter"/"InnerLipFlat" are not the kind of thing that appears by chance in binary data). Then did the actually-requested test: installed the real packages (`@splinetool/react-spline@4.1.0`, `@splinetool/runtime@1.12.98` — current versions, not the old pinned CDN viewer that had self-reported a version mismatch: "Your .splinecode file is more recent than the library"), rendered it in the live Next.js dev server, and — via a temporary debug line calling the real, live `Application.getAllObjects()` (removed before finalizing) — confirmed the loaded scene's actual object list: `SweepHighlight`, `InnerLipFlat`, `GapCutter`, `OuterRimFlat`, `GrooveLine`, ten `GlowSeg1 N` objects, `Directional Light`. This is unambiguously the correct scene; the earlier standalone-viewer preview was a real methodological limitation (version-mismatched old viewer), not a wrong URL.
- **However, what actually renders — confirmed live, at desktop/tablet/mobile, not assumed — still does not read as the Oragrol ring or match the site's brand:** it's a large, opaque, flat-grey square panel containing a rotary dial/knob with blue tick-mark arcs, consistent across every breakpoint tested. It doesn't blend with the site's `#0a0a0a` background (renders as a distinct grey card, not a hairline ring bleeding off-canvas), takes up visibly more of the frame than D-057's restrained ring did, and — a new finding from this pass — ships with an unremoved "Built with Spline" free-tier attribution badge visible in the corner of the rendered panel (confirmed scoped to the scene's own small container, not breaking out across the whole page, but still real third-party branding on the homepage). Whether this is a work-in-progress export, a camera/background setting on the Spline project that still needs adjusting, or a scoping mismatch between the described intent and the currently-published version, isn't something verifiable from this side — flagged for Mohammad rather than guessed at.
- **Implementation:** `hero-ring-spline.tsx` (new) — `"use client"`, per-file requirement; a fixed-aspect skeleton (matching the wrapper's own `aspect-square`, no layout shift) cross-fades to the loaded canvas via react-spline's `onLoad`, which hands back the live `Application` instance. Reduced motion: `Application.stop()`/`.play()` (confirmed real, working methods — read directly from `runtime.d.ts`, not assumed) are called in `onLoad` and in a `useEffect` that re-applies if the OS setting changes live after mount; also calls `.dispose()` on unmount. Investigated `@splinetool/react-spline/next` (an async Server Component variant with a built-in blur-hash loading preview via `next/image`) as a possibly-better Next.js-specific path — not used, since composing a Server Component from inside a required `"use client"` file isn't a pattern this codebase uses anywhere yet; flagged as a real option for a future pass rather than adopted mid-task.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean, same 20 routes. Live in the real dev server (not the standalone viewer): no page errors, no failed requests, WebGL renders correctly, zero horizontal overflow at 390px, the wrapper's exact required class string and responsive breakpoints (`sm:`/`lg:`) all preserved verbatim and confirmed still positioning/scaling correctly at every width tested. Reduced-motion emulation (`reducedMotion: "reduce"`) loads with zero errors. Could not visually confirm an animation actually being paused, because none was observed to be playing automatically in the first place (two screenshots 3 seconds apart, motion not reduced, were pixel-identical) — the scene may be purely interactive/drag-driven rather than autoplaying, and this embedding disables pointer events on the wrapper (matching the previous ring's decorative, non-interactive treatment), so there's nothing on this page for a visitor to trigger motion with regardless. `.stop()`/`.play()` are confirmed correctly wired and error-free, not confirmed to visibly change anything, because there was nothing visibly changing to begin with.
- **Status:** Integration built and verified working end-to-end. NOT signed off as visually complete — the rendered scene, as currently published at the given URL, does not read as the Oragrol brand ring and includes an unaddressed third-party watermark. Needs Mohammad's call: revise/republish the Spline scene, or reconsider whether Spline is the right path for this component after seeing the real render.

---

### D-060 — Spline ring: camera-framing/transparency fixed on the Spline side; a grey box remains, root-caused to the scene's own rendered content, not this codebase's CSS
- **Date:** 2026-08-18
- **Context:** Follow-up to D-059. Mohammad fixed two things on the Spline project directly (an oversized leftover object breaking camera framing; scene background set to transparent) and reported the ring itself now shows correctly (dashed segments + rotating highlight), but a solid grey rectangular box still renders behind it on the live site instead of true transparency to the navy hero. Asked to check the container div and the Spline component itself — including the loading-state skeleton — for any explicit `background-color` not set to transparent.
- **Confirmed the fresh scene loaded, not a stale/cached one:** re-fetched the raw `.splinecode` file; `Last-Modified` and `ETag` had both changed since the last check (new timestamp, new hash, smaller file size), confirming the republish had genuinely propagated before this check ran.
- **Did the exact check asked for, exhaustively, via live `getComputedStyle` on every node in the real DOM tree** (wrapper → loading skeleton → react-spline's own wrapper div → the `<canvas>` element itself) rather than reading the source and assuming: every single layer resolves to `background-color: rgba(0, 0, 0, 0)` — fully transparent. The loading skeleton (`bg-surface`) is present in the tree but at `opacity: 0` once loaded, confirmed via computed style, not just its class list. **There is no explicit background-color anywhere in this codebase's wrapper/component chain.**
- **Went further to find the real cause, since ruling out CSS didn't explain the visible box:** confirmed the WebGL context itself has `alpha: true` in its creation attributes (`canvas.getContext('webgl').getContextAttributes()`), meaning the canvas is capable of true transparency — this isn't a context-creation limitation either. Attempted `Application.setBackgroundColor("transparent")` (a real, documented runtime method, read from `runtime.d.ts`) as a from-our-side safety net regardless — tested live, made no visible difference to the rendered box, kept in the code anyway since it's harmless and directly addresses the literal ask, but doesn't appear to be the actual lever that matters here.
- **Conclusion:** with CSS fully ruled out (every layer transparent, confirmed live) and the WebGL context confirmed alpha-capable, the grey box is being drawn as real content by the scene's own render — almost certainly a mesh/panel object with an opaque grey material still present in the scene graph (separate from whatever the "oversized leftover object" fix already removed), or a scene-level background/backdrop setting that didn't take effect for this specific object despite the transparency toggle. This is not fixable from this codebase — there's no CSS, wrapper, or React-side lever left to pull. Reported back rather than guessed at further, since the actual scene contents aren't inspectable in enough detail from outside the Spline editor to name the exact offending object.
- **Verified:** `npx tsc --noEmit` / `npm run lint` clean. Fresh desktop screenshot taken and sent per the explicit request — ring geometry and camera framing are now correct (dashed ring + blue highlight arc, properly contained instead of the earlier oversized panel), but the grey box is still present.
- **Status:** Blocked on the Spline scene's own content, not this codebase. Needs Mohammad to check for a remaining opaque backdrop/panel object or background setting inside the Spline project itself.

---

### D-061 — Spline integration reverted; Home hero ring back to the hand-coded `PerimeterRing` (D-057)
- **Date:** 2026-08-18
- **Context:** After D-060's grey-box block (root-caused to the Spline scene's own content, not fixable from this codebase), Mohammad instructed reverting the Home hero to a fully code-based visual — remove the Spline integration entirely (component/imports/npm packages if unused elsewhere) — and rebuild the ring using the same hand-coded approach as Services' hero and Cyber Health's `HeroAmbientLoader`: dashed/segmented ring, slow rotating highlight sweep, token colors only, no external dependencies, exact same wrapper div/className, `prefers-reduced-motion` respected.
- **Found before building anything:** `PerimeterRing` (`perimeter-ring.tsx`, D-057) was still sitting on disk, completely untouched since before the Spline detour began — confirmed via file mtime (last modified before D-059's first edit) and a direct read matching its known-good D-057 state exactly. It already satisfies every requirement just listed, item for item: segmented/dashed ring built from the same proportions as `OragrolRing`'s circle geometry (re-parameterized for large-scale background use, per D-057's own header comment), one continuous rotating highlight sweep via a plain CSS `@keyframes`, `var(--color-accent)`/`var(--color-accent-light)` only, pure SVG/CSS (no camera/3D framing or transparency concerns to begin with), the identical wrapper `className` string, and its own internal `prefers-reduced-motion` handling (freezes the sweep).
- **Decision:** reverted to `PerimeterRing` rather than building a new, near-duplicate component — building a second hand-coded ring that would necessarily end up structurally identical to the first would be pure waste and would leave two near-identical components in the codebase, working against this project's own established reuse discipline (the D-012 brand-asset sweep, "don't hand-recreate what already exists"). Flagged this choice to Mohammad rather than silently substituting it for what might have been read as a request for a visually different design — the explicit requirements matched D-057's own component exactly, with no stated objection to how it looked before the Spline detour started.
- **Removed, confirmed via grep first that nothing else referenced them:** `hero-ring-spline.tsx` (deleted), `@splinetool/react-spline` and `@splinetool/runtime` (`npm uninstall`, confirmed zero remaining references in `package.json`/`package-lock.json`). `hero.tsx` now imports `PerimeterRing` directly again, with an updated header comment recording the full D-057 → D-059 → D-060 → D-061 lineage rather than pretending the detour didn't happen.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean, same 20 routes. Live via Playwright: zero console/page errors, the sweep's rotation genuinely advances across timed samples (real animation, not just configured), zero horizontal overflow on mobile. Desktop screenshot taken fresh and sent per the explicit request — clean hairline ring, correct token colors, no grey box, no camera-framing artifacts, blending correctly with the navy background exactly as it did before the Spline detour.
- **Status:** Reverted and verified. Home hero is back to a fully code-generated visual, consistent with D-008's convention for every other page.

---

### D-062 — Services page fully restructured: real 15-category/64-service taxonomy replaces the 8-capability structure (D-007/D-012/D-014 superseded)
- **Date:** 2026-08-20
- **Context:** Mohammad supplied `oragrol-services-data.json` (15 categories — 10 cybersecurity `services_tier1`, 5 business automation `business_automation_tier2`, 64 services total) and `oragrol-pricing-reference.csv`, both dropped at the repo root, with an explicit instruction to fully replace the existing "8 capabilities" structure (Virtual CISO, Risk Assessment & Compliance, ... — D-007's original scope, D-012/D-014's visual treatment) — real content, not a placeholder or a partial add.
- **Investigated before building, per instruction:** confirmed both files' exact shape (15 categories/64 services, matching what Mohammad stated) rather than trusting the description alone. Checked `HeroSchematicVisual` (the per-row schematic diagram) for whether it could render a variable node count — it's hardcoded to exactly 3 literal-coordinate nodes with no `nodeCount` prop at all, confirmed by reading the source directly, not assumed. A sibling, `ServicesNetworkVisual` (the page's own hero, 8 hardcoded nodes), showed the same non-parametric pattern. Neither takes a node-count parameter; both stayed exactly as-is — their node diagrams were always decorative grammar, never a literal per-item map (none of the old 4 live capabilities ever had exactly 3 sub-items either), so this was a real design constraint worth stating up front, not something that blocked the restructure.
- **Plan presented before writing code, per instruction**, then 3 explicit decision forks resolved via `AskUserQuestion` before building:
  1. **Per-service detail view: dedicated route** (`/services/[code]`, same `generateStaticParams`/`notFound()` pattern as `/resources/[slug]/page.tsx`) over an inline accordion — chosen to avoid cramming 64 services' full content into one page's DOM.
  2. **Pricing: JSON's own `price`/`unit` fields only** — the CSV's richer breakdown (setup fee, annual, month-to-month, bundle role, confidence) stays a reference file, not wired into the page.
  3. **No live/not-live status distinction** — confirmed the JSON carries no such field anywhere before asking; the old Live Services (1-4) vs. Additional Capabilities (5-8) split has no equivalent in the new data, so all 15 categories/64 services render uniformly.
- **Decision — information architecture:** the existing hero-scale schematic-row treatment (`CapabilitySpotlightVisual`/`HeroSchematicVisual`/`GlowEffect`, D-013/D-014) now renders once per **category** (15 rows, not 64) rather than per service — reusing the shell verbatim, per the explicit "do not introduce new design elements" instruction. Individual services render as compact `ServiceCard`s (new, reusing `CapabilitySpotlightMark` — the same small-badge treatment `AdditionalCapabilities` already used for its 4 not-yet-live cards) inside each category row, name/blurb/price/code, each linking to its own detail page. Two top-level sections — "Services" (Tier 1) and "Business Automation" (Tier 2) — reuse the page's existing white/light-blue environment alternation, previously used to distinguish live vs. finalizing capabilities, repurposed here for the real Tier 1/Tier 2 split.
- **Data layer:** `oragrol-services-data.json` moved from the repo root into `app/data/` (was sitting outside `app/`, unconventional for a Next.js content source) and imported directly (`resolveJsonModule`, already enabled) rather than hand-transcribed into TS — avoids the exact transcription-drift risk D-052 caught on Resources. New `services-data.ts`: typed interfaces, category icon map (15 new Lucide icon choices — UI decisions, not sourced from the JSON, same stopgap convention as every other capability icon since D-007), `categoryNumeral`/`serviceSlug` helpers (slug = lowercased real `code`, e.g. `c01-s01` — deterministic, not invented), and lookup functions.
- **A real correctness issue found and fixed while building, not left silent:** the site-wide header's Services nav dropdown (`nav-dropdown.tsx`, D-053/D-054/D-055) hardcoded 8 links to the old `#capability-0N` anchors this restructure removes — the only file site-wide referencing them (confirmed via grep), but rendered on every page. Flagged to Mohammad before proceeding (not in the originally-approved file list); he confirmed fixing it. Relinked to the new `#category-cNN` anchors, hrefs/labels sourced directly from `services-data.ts` (not re-typed) — 2 columns, the page's own real Tier 1/Tier 2 split (Cybersecurity: 10, Business Automation: 5), not an invented grouping.
- **A second consequence found and fixed the same way:** `hero.tsx` (the Services page's own hero) literally read "Eight capabilities. One coordinated approach." with a giant ghost "8" numeral behind the visual — both flatly wrong the moment this shipped. Fixed the literal count claims (H1 → "Fifteen categories...", numeral → "15", "capability" → "category" in the subtext); left `ServicesNetworkVisual`'s 8 decorative hardcoded nodes untouched, same reasoning as `HeroSchematicVisual` (a motif, not a literal count).
- **Content handling:** the source JSON's `problem` field (50/64 services) carries `**bold**` markdown and `\n\n` paragraph breaks — confirmed via a full scan before building, the only one of the 6 content fields that does. Reused Resources' existing `InlineMarkdown` component (cross-folder import, same reuse pattern `GlowEffect` already has in the other direction) rather than writing a second markdown splitter.
- **`additional-capabilities.tsx`** and its 4 bespoke marks (`CapabilityMonitorMark` etc.) removed from `app/services/page.tsx`'s render tree — left on disk unreferenced, this project's existing convention for superseded components, not deleted.
- **`category-nav.tsx` needed zero changes** — it was already fully generic (`items: CategoryNavItem[]`), no per-item-count assumption anywhere; only the data feeding it changed.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean — build output confirms exactly 64 static `/services/[code]` routes generated. Live via Playwright: 15 category rows present with correct ids, 64 unique service card links, zero horizontal overflow at 1440px, a markdown-bearing service (`c11-s01`) renders 0 literal `**` and the expected `<strong>` tag, an unknown service code 404s correctly, nav dropdown resolves to the correct 17 real links (×2 for the pre-existing simultaneous desktop/mobile DOM render, D-055's own documented pattern). Screenshots reviewed directly: hero, a Tier 1 category row, a Tier 2 category row, a full detail page, the updated nav dropdown.
- **Status:** Superseded in part by [[D-063]] (same day) — Business Automation moved out to its own top-level page/nav item. The data layer, visual system, and per-service detail template built here are unchanged and still govern both `/services` and the new `/business-automation`.

---

### D-063 — Business Automation split into its own top-level page and nav item, separate from Services
- **Date:** 2026-08-20
- **Context:** Same day as D-062. Mohammad requested a structural change: Business Automation should be a top-level nav item (same level as Services/Solutions/Cyber Health/Industries/Resources/Company), not a dropdown column + anchor-scrolled tier reached via `/services`.
- **Investigated first:** confirmed `NAV_LINKS` in `site-header.tsx` is a plain array — adding a 7th item is mechanical, no structural rework needed. Flagged one real consequence before building: D-054 already logged a known 1024px nav-cramping issue (labels/CTA/EN|FR wrapping); a 7th top-level item makes that pre-existing, already-flagged tight spot somewhat worse. Not fixed (out of scope), noted in both the plan shown to Mohammad and a code comment on `NAV_LINKS` itself.
- **One real fork resolved via `AskUserQuestion` before building:** whether the 25 Business Automation service detail pages (just shipped one commit earlier, under D-062, as `/services/c11-s01` etc.) should move to `/business-automation/[code]` for full URL/breadcrumb consistency, or stay at `/services/[code]` with only the breadcrumb text changed. Recommended and chosen: **move them** — cheap to do since D-062's pages were unreviewed and unindexed, avoids a permanent "breadcrumb says X, URL says Y" mismatch.
- **Decision — pages:** `/services` now renders only the 10 cybersecurity categories (real 1-10 numbering, unchanged from D-062). New `/business-automation` renders the 5 Business Automation categories. To avoid duplicating the row-rendering JSX between the two pages, `CategoryRow`/`TierSection` (renamed `CategorySection`) were extracted out of `live-services.tsx` into a new shared `category-section.tsx`, parameterized by `basePath` (`/services` vs `/business-automation`) and a `numeralFor` callback — same exact rendering code, called with different data, not a redesign. `additional-capabilities.tsx`'s convention (leave unreferenced code on disk rather than delete) was followed for nothing here since nothing became newly unused by this pass.
- **Decision — numbering (Mohammad's explicit "your call"):** real source `code`s (C11-C15, service slugs like `c11-s01`) stay unchanged in the data layer — no reason to diverge from the source file. Display numbering on `/business-automation`'s own page renumbers to 1-5 (`businessAutomationDisplayNumeral`, position-based) since "Category 11" would be an orphaned fragment once it's not nested under Services. `/services` keeps its real 1-10 numbering unchanged.
- **Decision — detail page URLs:** moved per the resolved fork above. New `app/business-automation/[code]/page.tsx` mirrors `app/services/[code]/page.tsx` exactly (same `ServiceDetail` component, reused). Both routes' `generateStaticParams` are now tier-scoped (`getServiceSlugsTier1`/`getServiceSlugsTier2`, new helpers) instead of the old `getAllServiceSlugs()` covering all 64 in one route. Each page function also validates the resolved category's tier and calls `notFound()` on a mismatch — covers a slug reaching the page function outside the statically-generated set (this app isn't configured for `dynamicParams: false`), so a stale `/services/c11-s01` link 404s cleanly instead of silently rendering Business Automation content under the wrong URL. Verified live: the old URL genuinely 404s post-move, not just assumed from the route file change.
- **Decision — breadcrumbs:** `ServiceBreadcrumb` (in `service-detail.tsx`, shared by both routes) is now tier-aware — a new `getCategoryRootPath()`/`isBusinessAutomationCategory()` pair in `services-data.ts` derives "Services → /services" or "Business Automation → /business-automation" from the category's own real code, rather than being passed in separately (can't drift out of sync with which page a service actually renders under).
- **Decision — nav dropdown:** the single combined 2-column Services dropdown D-062 built (Cybersecurity | Business Automation columns) is now two separate single-column dropdowns — `SERVICES_DROPDOWN` (10 items, same shape as Industries' existing single-column 9-item dropdown) and a new `BUSINESS_AUTOMATION_DROPDOWN` (5 items, same shape/size as the existing Solutions dropdown) — both still sourced directly from `services-data.ts`, not re-typed.
- **Decision — hero copy:** `services/hero.tsx` reverted from D-062's "Fifteen categories"/ghost "15" (briefly correct for the one-day combined-page version) back down to "Ten categories"/"10", cybersecurity-only subtext. New `business-automation/hero.tsx` mirrors it exactly (same `GlowEffect`/`ServicesNetworkVisual` components, cross-folder imports — not rebuilt) with its own "Five categories"/"5" and automation-specific subtext.
- **Decision — no `SolutionsBridge` on `/business-automation`:** that component's copy is specifically about the Services-vs-Solutions distinction, with cybersecurity-specific examples ("risk assessments or security awareness training") — genuinely Services content, not generic, and there's no equivalent real "Business Automation vs Solutions" copy anywhere to reuse. Flagged rather than reused out of context or fabricated; `/business-automation` closes with just `FinalCta`, same as every other page's generic closing CTA.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean — build output confirms exactly 39 static `/services/[code]` routes + 25 static `/business-automation/[code]` routes (39+25=64, matching D-062's total). Live via Playwright: `/services` shows exactly 10 category rows and 39 unique service links (zero Business Automation content), `/business-automation` shows exactly 5 category rows with 01-05 sidebar numbering and 25 unique service links, zero horizontal overflow, breadcrumbs correct on one service from each tier, the old `/services/c11-s01` URL returns a real 404, the top-level nav shows all 7 items in order with each dropdown resolving to the correct scoped category list. Screenshots reviewed directly: `/business-automation` hero + first category row, the open Business Automation nav dropdown.
- **Status:** Active.

---

### D-064 — Solutions page: real tier names/pricing/services replace "Level 01/02/03" placeholders (D-003 superseded for this page)
- **Date:** 2026-08-20
- **Context:** Mohammad supplied `oragrol-solutions-data.json` (real tier names Essential/Growth/Enterprise with pricing and full included-service lists, a 12-service `addon_menu`, `penetration_testing_addon`, and a `pricing_confidence_note`) — the real data D-003 (2026-08-12) had originally deferred pending. Explicit instruction: keep the existing visual design, no new components.
- **A real exclusion decided and flagged before building:** each tier's `gross_margin_note` (e.g. "~76% at reference scale") is internal cost/margin data, not customer-facing content, and wasn't named in any of the 6 build instructions. Excluded from the typed data layer entirely (`solutions-data.ts`'s `SolutionTier` interface has no field for it) rather than just not rendering it, so a future edit can't accidentally surface it. Flagged to Mohammad rather than silently including or silently dropping something he might have wanted shown.
- **Decision — data layer:** `oragrol-solutions-data.json` moved from the repo root into `app/data/` (same convention as Services), imported directly via new `solutions-data.ts` rather than hand-transcribed.
- **Decision — tier cards (`tier-cards.tsx`, rewritten):** real names replace "Level 01/02/03"; existing taglines, icons (`Layers`/`Layers2`/`Layers3`), and accent-bar progression kept verbatim/unchanged — same visual shell, real content filled in. `monthly_price` is the headline `DataText` figure; `annual_price`/`month_to_month_price` render as a smaller secondary line. Included services render as a wrapped row of `Badge` pills (name only, no blurb/price — "a compact list is fine," per instruction) rather than a vertical bulleted list, since Growth/Enterprise carry 23/26 services each and a full vertical list would make those cards enormously taller than Essential's 7-item one; `Grid`'s `items-start` added for the same reason D-024/D-025 required it on Services (divergent card heights, default grid row-stretch). The top-level `note` (reference scale + "tiers are cumulative") renders as a small caption under the section heading, ahead of where the reader hits the actual numbers.
- **Decision — add-ons (new `addon-menu.tsx`):** new "Available add-ons" section, separate from the 3 core tiers per instruction, all 12 real services in a `Grid` of `Card`s (name/blurb/price/`reason`) — no new visual primitives. Icon: a plain circled `Plus`, reusing the "+" concept `PentestAddon` already established on this page for "outside the core tiers," rather than picking 12 new one-off Lucide icons (would itself have been 12 new content/design choices) — deliberately simpler than `PentestAddon`'s own icon (a meaningful base icon + Plus badge), since no single base icon fits all 12 different services here.
- **Decision — Pentest (`pentest-addon.tsx`):** position/styling explicitly unchanged per instruction (dashed-border satellite card, Target+Plus icon, own section, last before Final CTA, same `id="pentest-addon"`). Content updated, though — real pricing now exists (`$7,500–$18,000 per engagement`), so the "currently being finalized" placeholder text was replaced with the real blurb/price/note, same treatment every other section on this page got. Flagged this reading to Mohammad before building, since the instruction was specifically about positioning — confirmed as the intended interpretation by proceeding without correction.
- **Bug caught and fixed before it shipped:** an early draft nested a `Text` (hardcoded to render `<p>`) inside `DataText`'s `<span>` via an invented `as="span"` prop `Text` doesn't have — would have produced invalid HTML (`<p>` inside inline `<span>`) and rendered as an unstyled paragraph tag. Caught by reading `typography.tsx` before running any check, fixed to a plain inline `<span>` with the token classes applied directly (same pattern `category-nav.tsx` already uses for a similar inline muted label).
- **Decision — pricing confidence note:** `pricing_confidence_note` renders as small print (`Text`, muted, small) once, at the bottom of `PentestAddon` — the last pricing-bearing section on the page before Final CTA — covering tier + add-on + pentest pricing together rather than repeating a caveat after every section.
- **A real consequence found and fixed, same class of issue as D-062/D-063's nav-dropdown fixes:** `nav-dropdown.tsx`'s `SOLUTIONS_DROPDOWN` hardcoded "Level 01"/"Level 02"/"Level 03" labels, which would have gone stale the moment the page itself started showing real tier names. Relinked to source from `solutions-data.ts` directly (`getTiers()`, same reuse pattern as Services/Industries/Resources' own dropdowns) instead of re-hardcoding new labels — can't drift out of sync again if the tier data changes.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean, same route count. Live via Playwright: real tier names/prices confirmed, zero "Level 01" stale text, zero gross-margin text anywhere on the page, pricing confidence note and cumulative-tiers note both present, all 12 add-on cards render, Pentest section shows real price, nav dropdown resolves to the 3 real tier names + Penetration Testing, zero horizontal overflow. One methodology note, not a product bug: an initial `fullPage` screenshot taken without first scrolling through the page showed the Add-ons/Pentest sections as empty gaps — diagnosed correctly (before reporting anything as broken) as `whileInView` `Reveal`s frozen at their pre-animation state, the same known fullPage-without-scroll quirk this project has hit before (D-014/D-057) — a second screenshot with real incremental scroll first confirmed everything renders correctly.
- **Status:** Active. Supersedes D-003 for the Solutions page specifically (Home's teaser and any other pricing-adjacent copy elsewhere on the site are unaffected, out of scope for this task).

---

### D-065 — Business Automation packages: real Tier 2 pricing added as new sections on the existing page, not a separate route
- **Date:** 2026-08-21
- **Context:** Mohammad supplied `oragrol-tier2-packages-data.json` — 2 one-time/project offers (Assessment $5,400, Implementation $17,850), 4 recurring packages (Starter/Growth-featured/Scale/Enterprise-Custom) with real service lists, monthly/annual/month-to-month pricing, and per-tier `scope_limits`, plus a funnel note, a `category_narrative` (C11-C15 → Discover/Automate/Understand/Operate/Grow), and a `pricing_confidence_note`. `/business-automation` (D-063) had only the individual à la carte Categories section — no packaging structure existed yet.
- **Location decision (asked upfront, decided before building):** new sections on the existing `/business-automation` page, not a separate `/business-automation/packages` route. Reasoning: (1) the site's whole nav-dropdown architecture (D-055) anchors into one page per nav item — it never spawns a sub-page for packaging content, and Business Automation's own dropdown already links to `#category-c11`-style anchors; (2) Solutions already runs a materially larger packaging structure (3 tiers + 12 add-ons + a pentest satellite, D-064) as sections on one page, so 2 offers + 4 tiers isn't enough content to justify a new route/hero/nav treatment; (3) `individual_services_note` ("services remain available individually") only reads naturally with the packages and the existing à la carte Categories section on the same page.
- **Decision — page flow:** Hero → new `ProjectOffers` (Assessment + Implementation) → new `RecurringPackages` (Starter/Growth/Scale/Enterprise) → existing `Categories` (individual services) → `FinalCta`. Packages sit above Categories — positioned as the primary path, individual services as the "or buy à la carte" alternative underneath, not the other way around.
- **Decision — `ProjectOffers` kept visually distinct from the recurring tiers, per instruction:** a dashed-border "satellite" card treatment (same language `PentestAddon` established on Solutions for "structurally outside the core tiers"), each card labeled "Step 1"/"Step 2" reflecting the real funnel order rather than a generic 2-up grid that would read the same as the recurring tiers below it.
- **Decision — `RecurringPackages`:** same `Card`/`Badge`/`DataText`/`Grid` system Solutions' `TierCards` already established (icon escalation `Layers`/`Layers2`/`Layers3`, `Layers3` reused for Enterprise since there's no natural 4th icon in that family — differentiated by bar width/opacity instead), extended with a new `scope_limits` block per card (included users/workflows/integrations, support level, overage rates) that Solutions' tiers don't have. Growth (`featured: true` in the source data) gets a "Recommended" badge + `ring-2 ring-accent` — the ring, not an overridden `border-*` class, specifically to avoid stacking two same-property Tailwind classes via plain `cn()` concatenation (no tailwind-merge in this codebase — `cn.ts`'s own documented caution).
- **Decision — Enterprise/Custom pricing display:** its `display_note` says to show "Contact us," not the calculated `monthly_price`/`annual_price` (those exist in the JSON for internal sanity-checking only). Implemented as `hasContactUsPricing()` in `packages-data.ts`, checked via `display_note` presence rather than a hardcoded name match, so a future rename/reorder can't silently break it. That card shows "Contact us" + a `/contact` CTA (reusing the same "Talk to Oragrol" pattern `PentestAddon` uses) in place of the price block.
- **Real exclusion, flagged and decided at the data layer (same discipline as D-064's `gross_margin_note`):** `reference_scale_note` (internal methodology — "scaled to 50 employees before summing") is not exposed by any getter in `packages-data.ts`.
- **Decision — copy placement:** `pricing_confidence_note` renders as small print at the end of `RecurringPackages` (the last pricing-bearing section before Categories/FinalCta), same placement convention as Solutions' `PentestAddon`. `category_narrative` becomes a derived one-line story ("Discover → Automate → Understand → Operate → Grow," built from the narrative's own values via `getCategoryStoryLine()`, not hand-typed) in `RecurringPackages`' intro copy. `individual_services_note` is appended to the existing Categories section's intro — that section IS the "buy individually" option the note refers to.
- **Decision — nav:** Mohammad explicitly asked for a nav anchor to the new packages section. Added `{ label: "Packages", href: "/business-automation#packages" }` as the first item in `BUSINESS_AUTOMATION_DROPDOWN` (matching the page's own top-to-bottom order — packages now sit above the 5 categories) — `id="packages"` lives on `ProjectOffers`' `Container`, the first of the two new sections.
- **Verified:** `npx tsc --noEmit` / `npm run lint` / `npm run build` all clean, same 85 routes (no new routes — additive sections on an existing page). Live Playwright pass: all real names/prices/scope-limits/funnel/category-story/pricing-confidence-note text present, zero leaked Enterprise calculated price ($22,162/$265,944 confirmed absent), "Recommended"/"Scope & limits" confirmed present once case-sensitivity was corrected for (Caption/Badge's own `uppercase` CSS transform, not missing content — checked directly against the raw rendered text before concluding anything), nav "Packages" link resolves to the correct anchor on both desktop and mobile, zero console errors, zero horizontal overflow at 1440/390px.
- **Flagged, not fixed:** the sitewide "Under attack?" pill (D-044-class standing issue) lightly overlaps the Implementation card's price on mobile and brushes the Enterprise card on desktop at some scroll positions — the same pre-existing global-component overlap flagged repeatedly elsewhere on this site, not introduced by this change.
- **Status:** Active.

---

### D-066 — Header redesigned to a two-tier structure (Bell Business reference); nav collapse now measurement-driven, not a guessed breakpoint
- **Date:** 2026-08-21
- **Context:** Requested a two-tier header restructure (thin utility bar above the main nav row, matching business.bell.ca's pattern) plus explicit content changes: EN | FR moves to the new utility bar; "Cyber Health" dropped as a standalone main-nav link (it duplicated the CTA button's own `/cyber-health` destination); desktop CTA label shortened to "Get Score" (mobile keeps the full "Get Your Cyber Health Score," where a full-width button has the room). The request explicitly named the root cause of the original nav/logo overlap bug: `NavBar` (`app/components/ui/nav.tsx`) picked a single Tailwind breakpoint (`breakpoint="lg"`, 1024px) for when nav links switch from hidden to visible, never verified against the real rendered width of however many items/CTA text existed at the time — a guess, not a measurement. `SiteHeader`'s own comment already flagged this ("labels/CTA/EN|FR wrapping" at exactly that breakpoint) once a 7th nav item (Business Automation) was added, but the breakpoint itself was never fixed, only flagged. Explicit instruction: fix the root cause (graceful degradation across a continuous range), not the symptom at one or two specific widths, and verify at every 50px from 1024–1920px.
- **Decision — adaptive nav, not a tuned breakpoint:** `NavBar` no longer takes a `breakpoint` prop. It now measures live: the desktop nav-links+CTA cluster (`desktopContent`) is rendered a second time, invisibly (`visibility:hidden`, `position:absolute`, `aria-hidden`, `data-nav-measure-clone` for scoped querying — the exact same React node both times, not a hand-copied approximation, so the measured copy can never visually drift from what's actually shown), and its `scrollWidth` is compared against the row's real available width (`row.clientWidth - logo.offsetWidth - persistentActions.offsetWidth - a deterministic 40px gap buffer`, derived from the actual `gap-4` classes used, not guessed) via `ResizeObserver` on both the row and the measured clone (the latter catches web-font-swap reflows, not just viewport resizes). `desktopContent` renders only when it fits; `collapsedContent` (the hamburger trigger) renders the instant it doesn't — at any width, not just the ones anyone happened to test. `HeaderSearch` is a new third slot, `persistentActions`, always visible regardless of which side of the boundary the row is on (unchanged behavior from before, just now structurally guaranteed rather than coincidentally matching two separate `lg:hidden`/`lg:flex` class pairs). `MobileMenuTrigger` itself no longer carries any breakpoint class — its existence in the DOM is now the single source of truth `SiteHeader`'s mobile panel gates on, removing the second independent place (`#mobile-nav-panel`'s own `lg:hidden`) that previously had to agree with it and could silently drift.
- **Decision — two-tier structure:** new thin utility bar (`h-9`, 36px) above the existing main nav row (`h-20`, 80px unchanged) inside the same `fixed` header wrapper — currently just "EN | FR" (still a static/decorative label per the original Step-4 decision; full locale routing isn't built), right-aligned via `justify-between` with an empty placeholder on the left, which is a real flex layout (not a hardcoded single-child one) so a future "Blog" or "Login" link drops in without another layout pass. Removes the old duplicate: EN | FR previously existed in two separate places (the main row's `actions`, and its own line in the mobile panel) — now exactly one instance, always visible, independent of the adaptive row's fits/doesn't-fit state (short fixed text was never at risk of the crowding problem the row had).
- **Real cross-cutting consequence, found and fixed before it could regress anything:** the new utility bar changes the fixed header's total rendered height (80px → 116px). Everything that assumed the old 80px (or the old header-relative `scroll-mt-28`/`top-20`/`mt-24` values baked in at various call sites) would now land content slightly under the taller header. Added `--header-height: 116px` to `tokens.css` (a new, first-of-its-kind layout token — the file's own header says spacing/breakpoints are intentionally left as Tailwind defaults, but this is a genuinely cross-file-shared layout constant now, the same class of problem a scattered set of independently-guessed pixel values would recreate) and repointed every consumer to it: `SiteHeader`'s mobile-nav-panel `top` (was `top-20`, hardcoded to the old single-row height), `header-search.tsx`'s dialog `margin-top` (was `mt-24`), and all 6 `scroll-mt-28` usages (Industries' tabpanel, Business Automation's `#packages`, Solutions' `#pentest-addon` and each `#tier-0N` card, Services' `category-section.tsx` and `additional-capabilities.tsx`) — now `scroll-mt-[calc(var(--header-height)+2rem)]`, preserving the original ~32px clearance buffer above the header rather than landing flush against it. Chosen specifically so none of these can independently drift out of sync with the header's real height again, the same root-cause standard applied to the nav breakpoint itself.
- **Verified:** `npx tsc --noEmit` / `npm run lint` (1 pre-existing, unrelated warning) / `npm run build` all clean, same 85 routes. Real automated testing, not spot checks: a Playwright sweep at every 50px from 1024px to 1920px (19 widths, including the exact 1920px endpoint) confirmed zero overlap, zero clipping, zero horizontal page overflow, and exactly one of {nav, hamburger} visible at every single width; a finer 5px-step sweep across the actual transition zone (1000–1090px) confirmed exactly one clean handoff (hamburger at ≤1070px, full nav at ≥1075px in this environment's font metrics) with no gap where neither or both render; a 2560px spot check confirmed the pattern holds well beyond the requested range too, consistent with D-001's "no max-width, works at any width" header philosophy. Re-verified the 4 anchor-scroll targets touched by the `--header-height` change (Industries/Business Automation packages/Solutions pentest-addon/Solutions tier-01) all still land with their target visibly in-viewport under the new taller header. Real-browser (`claude-in-chrome`) and Playwright screenshots at 1024/1074/1440/1920px all visually confirm clean spacing with no overlap, matching the numeric checks.
- **Status:** Superseded in part by D-067 (same day) — the "no max-width, ever" row layout and the verification methodology both had real gaps, described there.

---

### D-067 — Two follow-up fixes to D-066's header (hero overlap regression, wide-viewport gap) + a CTA copy fix; honest accounting of what the D-066 verification pass missed and why
- **Date:** 2026-08-21
- **Context:** Mohammad reported two issues after D-066 shipped, one a real regression, one a genuine spec gap, plus a copy note:
  1. **Regression:** at 14-inch/narrower desktop widths, Home's Hero eyebrow ("Cybersecurity for modern businesses") and heading ("Security clarity for what comes next.") rendered UNDER the header, overlapping the logo/nav — a direct side effect of the header growing from 80px to 116px.
  2. **Spec gap:** at 24"+/1920px+ monitors, the logo sits pinned to the true left edge and the nav+CTA cluster pinned to the true right edge, with an unbounded, awkward-looking empty gap between them — D-001's "no max-width, ever" header philosophy, applied uncritically to the ROW'S CONTENT (not just its background), never actually looked right at extreme widths; nobody had looked at it at 1920px+ until now.
  3. **Copy:** desktop CTA "Get Score" (D-066) is ambiguous — doesn't say what's being scored. Changed to "Get Cyber Score."
  Also an explicit instruction to re-verify with actual visual screenshots across the same 1024-1920px+ range, since D-066's own verification "apparently didn't catch this hero-overlap regression despite claiming thorough testing."
- **Honest root cause of the miss, not glossed over:** D-066's Playwright sweep tested 19 different WIDTHS (1024-1920px) but held HEIGHT constant at a generously tall 900px for every single one of them. Home's Hero is `h-[100svh]` with its content flex-`justify-center`ed inside — a layout where the text's vertical position depends on viewport HEIGHT, not width, which D-066's sweep never varied. Real 14-inch laptops (1366×768, 1440×900, MacBook 14" at 1512×982) are shorter, not just narrower, than the 900px height every check used — short enough that centered content sat close enough to the top to collide with the taller header. The nav-layout checks D-066 ran (overlap between logo/nav/CTA/search/hamburger) were genuinely thorough for what they measured; they simply never measured whether PAGE CONTENT below the header could collide with it, which wasn't the question D-066 was answering but is exactly what broke.
- **Decision — Hero overlap fix:** `home/hero.tsx`'s content `Container` gets `pt-[var(--header-height)]` (the same token D-066 introduced), reserving the header's real height as the floor of the box `justify-center` actually centers within — guaranteeing the vertically-centered content can never render above the header's bottom edge, at any viewport height, not just the sizes tested. `PerimeterRing` (the decorative ring, `absolute` and independent of this `Container`) is untouched — it's still meant to bleed under the header per the original full-bleed design (D-001's blending intent), only the actual TEXT content needed the floor. Checked first: no other page hero uses this `100svh`+`justify-center` pattern (every other page hero is `py-24 md:py-32` in normal flow, comfortably clear of the header already) — confirmed via grep and Playwright screenshots at the same realistic dimensions, so this was genuinely isolated to Home, not a wider latent bug.
- **Decision — wide-viewport gap fix:** wrapped `NavBar`'s row (logo + nav + actions) in `mx-auto w-full max-w-[1440px]` — NOT applied to the `<header>` element itself, which keeps its background/border genuinely full-bleed, preserving D-001's actual requirement (the header BAR must match the Hero's edge-to-edge image). D-001's own finite-cap attempts failed because they capped the WHOLE header including its background, recentering it as a visibly narrower box against the still-full-bleed Hero underneath — a different, real problem this fix doesn't reintroduce, since only the row's children move inward together, not the bar itself. Chose 1440px specifically because it isn't a new number — it's `Container`'s existing `xl` tier (D-015/016), the same width Home's Hero/Cyber Health's Flow/the footer already use for their own content, so the nav row now lines up with a content column the page already establishes rather than an arbitrary new one. Below 1440px, nothing changes — verified via a full re-run of D-066's own 1024-1920px sweep, unchanged pass. Above it, the gap between logo and nav stops growing: measured identical (328.8px) at 1440px, 1920px, and 2560px, where before it grew without bound.
- **Decision — CTA copy:** "Get Score" → "Get Cyber Score" on the desktop header CTA only (`site-header.tsx`). Mobile panel's own CTA instance is untouched — was always the full "Get Your Cyber Health Score" and remains so.
- **Verified:** `npx tsc --noEmit` / `npm run lint` (same 1 pre-existing unrelated warning) / `npm run build` all clean, same 85 routes. This time, tested exactly the gap D-066 missed: 7 realistic width×height combinations including 1366×768, 1440×900, and 1512×982 (the actual 14-inch laptop resolutions named in the report), plus 1920×1080/2560×1440 and a deliberately adversarial narrow-width-tall-height combo — hero eyebrow/heading confirmed at or below the header's real bottom edge at every one, with real screenshots inspected directly at 1366×768 and 1440×900, not just the numeric check. Re-ran D-066's full nav-overlap sweep (1024-1920px, every 50px) to confirm the new `max-w-[1440px]` wrapper introduced no regression there — still all pass. Confirmed the logo↔nav gap is now bounded (identical at 1440/1920/2560px) with a screenshot at 2560px. Confirmed the CTA reads "Get Cyber Score."
- **Status:** Active.

---

### D-068 — Full visual-system migration: 6-color palette (Deep Ink/Deep Blue/Warm Off-White/Violet-Gray/Steel/Burnt Orange) + 3-font typography (Space Grotesk/Manrope/IBM Plex Sans) replace the entire old system site-wide
- **Date:** 2026-08-22
- **Context:** Mohammad supplied a complete, approved visual-system brief replacing the old Oragrol Cyan (#018ABE) + Space Grotesk/Inter/JetBrains Mono/Epilogue system with 6 exact hex colors (with target visual weights, not hard rules) and 3 fonts with strict role separation, plus a detailed component-level mapping (header/hero/headings/body/services/packages/forms/technical-data/cards/sections/footer/icons/gradients). Explicitly framed as a migration, not a redesign — preserve page architecture, content, all 64 services, pricing, nav structure, business logic, existing animations, the existing 3D/hero concept. Required an audit report (found/listed before any code changed) and hard verification (git status/log, Vercel deploy status via GitHub's commit-status API, real Playwright screenshots at 5 widths on live production) given two prior sessions had been reported "done" without actually being verified.
- **Audit findings (reported before writing code):** the codebase already centralizes color through one file (`tokens.css`: raw palette → semantic layer → environment overrides → Tailwind `@theme inline`), so this was mostly a token-layer swap, not a 126-file rewrite. Found exactly 2 genuine hardcoded-hex leaks that bypass the token system entirely: `kinetic-grid.tsx` (canvas 2D can't read CSS vars — background/active-color literally hardcoded as old hex/RGB) and `oragrol-ring.tsx` (its default `color` prop was a hardcoded hex despite its own doc comment claiming it read the accent token — a real pre-existing bug, not something this migration introduced). Also found ~30 sections using the old `white`/`light-blue` environments, which the new palette has no equivalent for (every color is dark-family except Warm Off-White, which is a TEXT color, not a background).
- **Decision — section environments retired and reduced to 3:** asked Mohammad directly rather than guessing, since converting ~30 sections either to fully-dark or to a recolored-but-still-light theme changes ~40% of the site's visual character. He chose: mostly dark-family, with a lighter "Warm Off-White bg / Deep Ink text" environment kept ONLY as a sparing exception (2 sections total, not systemic) — Company/Values (his own example) and Contact/General Inquiry (chosen as the other natural "form contrast" candidate). Implemented as `env-dark` (unchanged), a new `env-deep-blue` ("supporting depth" rhythm — absorbed most former `light-blue` sections plus a handful of former `white` ones whose content reads as pricing/selected-cards/dashboard-data/process-visual, per the brief's own Deep Blue role), and a new `env-light` (the 2 sparing exceptions). `SectionEnvironment`'s type (and `CategorySection`'s own mirrored union) changed from `"dark"|"white"|"light-blue"` to `"dark"|"deep-blue"|"light"` — every one of the ~30 call sites individually reassigned by hand based on that section's own content/rhythm position on its page, not a mechanical find-replace (see each file's own git-blame for the specific call).
- **Decision — contrast-driven derived tokens (verified via the actual WCAG relative-luminance formula, not eyeballed):** Deep Ink text on plain Burnt Orange = 4.89:1 (passes AA, unlike the old cyan which only reached 3.90:1 and needed a separate darkened `accent-strong` fill just for button contrast — this migration's `accent-strong` is now a pure hover-lightening, not a contrast necessity, since darkening the already-tight 4.89:1 further would drop it below 4.5). Burnt Orange and Steel Gray both fail 4.5:1 directly on Deep Blue (3.08/3.14) and on Warm Off-White (3.18/3.12) — `env-deep-blue` and `env-light` each get their own lightened/darkened `--accent`/`--text-secondary` overrides (same technique the original system's D-010 already established: a palette color blended toward black/white to fix one specific legibility failure, never a new hue invented outside the approved 6). A fixed `--ink` token (always Deep Ink, never repointed by environment) was added for the primary-CTA button's text specifically, since the brief's literal "Burnt Orange bg / Deep Ink text" pairing must hold regardless of which environment a button renders in.
- **Decision — decorative "identity wash" usage of accent, redirected (component logic changed, not just token values, but grounded directly in the brief's own component-level mapping, not invented):** raw Deep Blue is far too dark to read as a foreground icon/stroke color on an already-dark page (1.6:1 on Deep Ink, 1.1:1 on Violet-Gray — effectively invisible), so it can only serve its stated "background/depth" role, not replace accent as a foreground icon color. Two concrete moves: (1) `GlowEffect` (the ~18-call-site shared ambient card-glow component) changed its default gradient from the accent family to a new Deep-Blue-based `--depth`/`--depth-light`/`--depth-strong` family — an ambient blurred glow is exactly the "background behind 3D elements / supporting depth" role the brief describes, and this alone fixed the single biggest source of "everything glows orange" across the site in one shared-component edit. (2) ~18 files' small, routinely-repeated "icon in a circle badge" pattern (team members, trust pillars, step sequences, location rows, service/topic lists, addon/package tier icons) moved from `text-accent`/`border-accent`/`bg-accent` to neutral `text-text-primary`/`border-border`/`bg-surface` — grounded in the brief's own explicit Icons rule ("default Steel Gray or Warm Off-White, important/active state Burnt Orange"): a routine, non-interactive, always-visible badge icon isn't "the important/active one." True CTA/hover/focus/active-tab/checked-input/"Recommended"-or-"Featured"-badge/section-eyebrow-label usages were left as accent (Burnt Orange) — those are the actual highlight/interactive roles the brief names. Ambient (non-hover) `shadow-accent/30` card washes on ~5 files moved to `shadow-depth/30` for the same reason as GlowEffect; `hover:` shadow/border intensifications on those same cards were left as accent (a genuine hover highlight, not resting-state decoration). Pricing `DataText tone="accent"` figures (addon-menu, pentest-addon, project-offers, recurring-packages, tier-cards, service-detail, plus 2 "Additional Capabilities" mini-metrics) moved to `tone="primary"` per the brief's explicit "package name and price in ... Warm Off-White" instruction — genuinely singular "important indicator" figures (the real Cyber Health Score, 78/100, on both its Home teaser and its own `/cyber-health` page) were verified by reading each call site's context and left as accent, since that's the literal one the brief means.
- **Decision — larger "signature" schematic/material illustrations left as accent, deliberately NOT redesigned:** `hero-schematic-visual.tsx`, `network-visual.tsx`, `schematic-visual.tsx`, the 6 `capability-*-mark/visual.tsx` files, `capability-spotlight.tsx`, `cycle-visual.tsx`, `ai-loader.tsx`, `hero-ambient-loader.tsx` — each is a bespoke, deliberately-designed "signature visual" for its own page/section (documented at length in their own file comments as intentional design work), not a routinely-repeated decorative badge. Redesigning ~15 intricate SVG/gradient components' color logic was judged out of scope for a color/type migration ("not a redesign") and out of proportion to the actual visual risk once their shared ambient `GlowEffect`/shadow layer was already moved to Deep Blue (confirmed via direct visual inspection, not assumed — see Verified below) — their remaining accent-colored linework reads as a highlight against a now-neutral-toned backdrop, not as page-dominant orange.
- **Decision — fonts:** `Space_Grotesk` (heading/brand identity) unchanged. `Inter` → `Manrope` (body/UI). `JetBrains_Mono` → `IBM_Plex_Sans` (technical/data), with an explicit `weight: ["400","500","600"]` array since IBM Plex Sans is NOT a variable font on Google Fonts (required by next/font, not optional) — Space Grotesk/Manrope stayed weight-unspecified since both are variable fonts and next/font already exposes their full weight range without a static array. `font-brand` (Epilogue) was explicitly left untouched — grepped and confirmed it's used only by `OragrolLogo`'s own text nodes (sourced verbatim from the approved `Oragrol_Logo_Final.svg`, a locked brand asset, same category as "existing approved 3D assets") and the style-guide reference page, nowhere else — not part of the 3-font content system the brief defines, flagged to Mohammad rather than silently changed or silently left ambiguous.
- **Verified:** `npx tsc --noEmit`/`npm run lint` (1 pre-existing unrelated warning)/`npm run build` all clean, same 85 routes. Real visual QA, not a self-report: local Playwright screenshots of all 10 top-level pages at 1440px (zero console errors on any), plus targeted scroll-triggered close-ups (a full-page screenshot without scrolling through the page first misses content gated behind this codebase's own on-scroll `Reveal` animations — learned and correctly diagnosed as a screenshot-methodology artifact, not a bug, the same class of mistake flagged in a prior session's own memory) of: a `deep-blue` section (Home/Approach — Warm Off-White text at full legibility, icons correctly neutral), a repeated-icon-badge section (Home/Trust — icons confirmed neutral, not orange), the `env-light` Company/Values and Contact/General-Inquiry sections (Deep-Ink-on-Warm-Off-White headings/body confirmed legible, accent eyebrow legible), the Business Automation pricing cards (deep-blue bg, Violet-Gray card fill, "RECOMMENDED" badge correctly the only orange-filled badge, prices correctly Warm Off-White not orange), and the full Cyber Health page scrolled-through (the real "78/100" score correctly still accent-colored, functional risk-tier bars correctly untouched/not using brand accent). Confirmed via direct DOM measurement (not assumption) that the prior session's wide-viewport layout fix (Container/NavBar fluid max-width, `a3c90d6`+follow-up) is completely unaffected — nav row/footer widths at 2560px still compute the identical 1796.8px this migration didn't touch that system at all.
- **Status:** Active. Committed and pushed to `main`; Vercel production deploy and live-URL screenshots at mobile/tablet/1440/1920/2560px confirmed in the same session — see PROJECT_MEMORY.md.

---

### D-069 — Atmospheric section-to-section background transitions (global, reusable system)
- **Date:** 2026-08-22
- **Context:** Mohammad's brief: section-to-section background color changes (D-068's `env-dark`/`env-deep-blue`/`env-light`) read as hard rectangular cuts — "Color A / hard line / Color B" — instead of a continuous atmospheric environment. Required: one reusable global mechanism (not a per-page fix), using only the authoritative D-068 palette (explicitly NOT the older #080A0D/#163A5F/#D95A2B draft values), no content/layout/typography changes, no visible "gradient bar" spacer, responsive across mobile through 32"+ monitors, and — critically — actually verified by rendering, not spec-reading.
- **Audit (before writing code):** confirmed `tokens.css`'s raw palette exactly matches the brief's "authoritative" hex values (case-insensitive grep on all 6). Grepped the entire codebase for the named obsolete values — zero matches, nothing to clean up. Mapped every page's actual section-environment sequence (10 pages, ~40 sections) to find every real color-changing boundary: 21 total across the site (Home 6, Solutions 2, Cyber Health 2, How We Work 2, Contact 3, Company 4, Business Automation 2, plus the Contact->Footer edge case below) — same-color boundaries (e.g. two consecutive `dark` sections) were left untouched since there's no cut there to soften.
- **Decision — implementation point:** extended `Section` itself (`components/ui/section.tsx`) with optional `transitionFrom`/`transitionTo` props, rather than a new spacer component or per-page wrapper — keeps the existing page-composition architecture completely unchanged (still literally `<Hero/><Approach/>...` in sequence; each Section just optionally declares what environment came before/after it). Renders a single absolutely-positioned, `pointer-events-none`, `aria-hidden` gradient div anchored to the section's own leading (top) or trailing (bottom) edge — sized via `clamp()`, not a fixed height, so it scales with viewport width without vanishing on mobile or ballooning on a 32"+ monitor (same fluid technique already established for this codebase's container widths). No JS, no animation, no images, no new spacer sections between existing ones.
- **Decision — color interpolation, corrected after actually rendering it (not shipped on the first guess):** initially implemented `linear-gradient(in oklch, ...)`, reasoning that oklch's perceptual uniformity would avoid a muddy sRGB midpoint. Built an isolated side-by-side comparison page and rendered every candidate before committing to one — found that `oklch` (which interpolates hue as an angle) produces a real, visible pink/mauve band partway through the Deep-Ink<->Warm-Off-White pair specifically, because the shortest hue-angle arc between Deep Ink's cool near-black hue and Warm Off-White's warm cream hue swings through magenta — exactly the "sudden midpoint color" the brief explicitly says to avoid, just introduced by the intended fix rather than by a plain gradient. Switched to `in oklab` (rectangular, non-hue-angle interpolation), which rendered clean across every pair actually in use (dark<->deep-blue both directions, dark<->light both directions, deep-blue<->light) plus Deep Ink<->Muted Violet-Gray (one of the brief's named example combos, though Violet-Gray isn't currently used as a section-level background in this codebase — only as the `--surface` card-fill token — so this pairing has no live call site today; the mechanism supports it identically to the other two if one is ever added).
- **Decision — asymmetric zone size for dark<->light:** transitions touching `light` use a shorter `clamp()` (max 9rem/144px vs. 14rem/224px for dark<->deep-blue) — a long fade between the darkest and lightest colors in the palette reads as foggy/washed-out rather than intentional, per the brief's explicit "do not make the fade excessively large or washed out" instruction for dark<->light specifically.
- **Decision — the footer edge case:** `SiteFooter` is rendered once in the root layout (always `env-dark`), not per-page, so it structurally cannot know what any given page's own last section was — `transitionFrom` (which lives on the receiving section) can't reach across that boundary. Solved with the symmetric `transitionTo` prop instead: the one page whose last section isn't already dark (Contact's `LocationsSection`, `deep-blue`) declares its own trailing-edge blend toward `dark` itself, without touching the shared Footer/layout files at all. Every other page's last section is already `dark`, matching the footer, so needs nothing.
- **Decision — pre-existing hard-border removal:** 5 sections had their own literal `border-t`/`border-y` divider line sitting exactly on a boundary that now also gets a color-blend overlay (`addon-menu.tsx`, `reassurance.tsx`, `general-inquiry.tsx`, `team.tsx`, `recurring-packages.tsx`) — removed each one specifically, since a hard line drawn on top of a soft blend is still a hard line. Left `solutions-bridge.tsx`'s own `border-t` alone — that boundary is `dark`-to-`dark` (no color change), so it's a legitimate decorative rhythm divider, not the seam this task is about.
- **Decision — shared components needing a passthrough:** `FinalCta` (rendered on 6 different pages, always as the section before the always-dark footer) and `CategorySection` (shared between `/services` and `/business-automation`) both gained an optional `transitionFrom` prop, left `undefined` by default — only Company's page (`transitionFrom="light"`) and Business Automation's Categories caller (`transitionFrom="deep-blue"`) actually pass one; every other caller needs nothing, since their preceding section is already `dark`.
- **Decision — horizontal support:** added a `transitionDirection?: "vertical" | "horizontal"` prop (blends the left edge, sized by width instead of height) per the brief's requirement that the system "should also support horizontal transitions" for future split-screen layouts — no current page has a same-section side-by-side color split, so nothing calls it yet, but the primitive isn't hardcoded to vertical-only.
- **Decision — header/hero boundary explicitly left alone:** confirmed (not assumed) this is the one boundary the brief says to leave if it's "intentionally part of the navigation design" — `SiteHeader` already blends into whatever's beneath it via two stacked gradient layers (D-066/D-067's own fix for the original nav-overlap bug), a different, already-atmospheric mechanism solving the same class of problem; adding a second transition system on top of it would fight that existing one, not improve it.
- **Verified:** `npx tsc --noEmit`/`npm run lint` (same 1 pre-existing unrelated warning)/`npm run build` clean, same 85 routes. Confirmed the gradient syntax actually computes (not silently dropped) via a live `getComputedStyle` check before doing any visual review. Screenshotted all 21 boundaries in context (not the isolated test page) across Home, Solutions, Cyber Health, How We Work, Business Automation, Company, and Contact — every one confirmed smooth with no visible hard line, no muddy/odd-hue midpoint (re-confirmed after the oklab fix specifically on the Contact dark->light and Company light->dark cases, the two that showed the original oklch artifact), and no interference with card content, buttons, or the "Recommended"/pricing text sitting near several of these boundaries. Checked responsive scaling directly: overlay height measured 144px at 1440px viewport (matches the clamp formula exactly) and capped at 224px at 2560px (proportional, not oversized) via live DOM measurement, plus a visual check at 390px mobile (zone stays clearly visible, not vanishingly thin).
- **Status:** Active. Committed and pushed to `main`; Vercel production deploy and live-URL screenshots confirmed in the same session — see PROJECT_MEMORY.md.

---

### D-070 — Pricing-clipping "background fade damage" report: root cause was an unescaped Tailwind `calc()`, not the z-index framing; Section's overlay hardened anyway
- **Date:** 2026-08-22
- **Context:** Mohammad reported the new Services card grid's background fade "covering content" (pricing clipped on featured cards), framed around z-index/layering, with a request to fix it at the architecture level.
- **Investigation:** direct `getComputedStyle`/`elementsFromPoint` checks showed content was already painting on top of the transition overlay — the z-index story wasn't the actual mechanism, reported honestly rather than accepting the framing at face value. Root cause: a featured-card wrapper used `h-[calc(100%+3rem)]` to compensate for a `-m-6` margin trick working around `cn()`'s plain-string-concatenation `Card` padding conflict. Tailwind's arbitrary-value syntax requires underscore-escaped spaces inside `calc()` (`calc(100%_+_3rem)`); without them the whole declaration is invalid CSS and silently dropped, with no build error. Confirmed via measurement: 287.25px computed height vs. 335.25px intended.
- **Decision — fix, not just correct the escaping:** removed the entire margin/calc workaround. Featured cards get their visual weight from content alone (grid span, icon/title/price size) — no padding-override wrapper needed, avoiding the underlying `cn()` fragility rather than patching one instance of it.
- **Decision — harden `Section` anyway:** even though stacking order wasn't the real bug, added explicit `-z-10` on the transition overlay plus `isolate` on `Section` itself, per Mohammad's request for an architectural guarantee that background layers can never paint over content, not just "currently don't."
- **Verified:** `tsc`/lint/build clean. Confirmed via computed-style re-measurement that pricing/card content renders at full intended height with no clipping, across the same breakpoints as D-069.
- **Status:** Active. Committed and pushed to `main` — see PROJECT_MEMORY.md.

---

### D-071 — Services C01 visual pilot rejected: no per-category bespoke visual systems
- **Date:** 2026-08-22
- **Context:** A prior brief asking for a more distinctive Services visual treatment was implemented, as a scoped pilot, on exactly one category (C01, 4 services): each service got its own bespoke full-scene illustration (a node-topology diagram, a checkpoint pathway, a governance-tree graphic, and the `OragrolRing` reused once as a literal "vantage point" over a dot-field), committed as `fb34519`.
- **Mohammad's correction:** rejected the result outright as visually incorrect, and identified the actual root cause of the miss — the originating brief had been interpreted too literally as "create custom visual compositions for each service." That is explicitly **not** the intended direction.
- **Decision (durable, applies to all future Services/Business-Automation visual work):** do NOT give individual service categories their own bespoke, isolated visual system. Avoid: oversized empty/negative space built around a single illustration, giant isolated SVG diagrams per category, arbitrary asymmetric positioning invented per-category, large visual blocks that push real content (pricing, descriptions) further down the page, excessive vertical spacing to accommodate a custom visual, one-off visual treatments that make categories look inconsistent with each other. The category/service list is meant to read as one consistent, scannable architecture (`CategoryNav` + `ServiceCategoryRow`, shared across all categories) — distinctiveness belongs at the page-composition level (see D-072's Hero/Overview panels), not duplicated per category.
- **Resolution:** `git revert --no-edit fb34519` → `7af7c6e` (clean revert, not reset/force-push). Confirmed via `git diff` that D-069's `section.tsx` and D-070's `service-card-premium.tsx` were byte-identical before and after the pilot and its revert — neither was touched by or lost to this detour. Confirmed via production screenshots that C02/C05/pricing/other pages were unaffected throughout.
- **Status:** Closed/reverted. The C01-specific components (`c01-showcase.tsx`, `c01-visuals.tsx`) no longer exist in the working tree.

---

### D-072 — Services page restructured into a 2-level Landing Page: editorial Hero+Overview panels above the untouched category architecture
- **Date:** 2026-08-22 (commit `611871d`)
- **Context:** Following D-071's correction, Mohammad supplied a new reference image (an AI-marketing hero mockup) for a fresh, explicitly-scoped brief: NOT another per-category treatment, but a **landing-page-level** composition. He rejected an attempt to narrow scope via a clarifying question and instead dictated the structure directly: Level 1 (new, editorial landing/introduction) sits above Level 2 (the existing category architecture, structurally untouched).
- **Decision — composition vs. content:** the reference image governs layout/composition language only (large rounded panel, split text/visual, asymmetric multi-panel rhythm) — brand, palette, typography, and copy stay Oragrol's own (current D-068 6-color/3-font system). The brief itself quoted the retired pre-D-068 hex values (#080A0D/#163A5F/#D95A2B) — flagged to Mohammad twice; proceeded with the current authoritative palette both times since he didn't object.
- **Decision — Level 1 implementation:** `hero.tsx` rewritten as one large rounded panel — logo, "Services" badge, two-tier H1, description on the left; the existing `ServicesNetworkVisual` (D-008/D-015's own illustration, scaled up, not replaced — explicit instruction to reuse/adapt existing visual language rather than invent a new system, and explicit instruction against any human/robot face) plus a faint decorative "10" numeral on the right. `GlowEffect` gets a per-call-site `colors` override back to the warm accent-orange family (its shared sitewide default is Deep-Blue per D-068/D-069 — correct everywhere else, but this one panel wants the reference's warm glow). New `overview.tsx` is a second panel with mirrored asymmetry (stat pair left this time, copy right) — real computed category/service counts (10/39) presented as a big-number statement, the same treatment already established for the Cyber Health Score, not a new pattern; its copy is the exact eyebrow/heading/intro `live-services.tsx` used to render at its own top, relocated rather than rewritten so it isn't duplicated on the page.
- **Decision — Level 2 left alone:** `live-services.tsx` keeps the identical `CategoryNav`/`ServiceCategoryRow`-per-category/`Container size="2xl"`/D-069-D-070 rendering it already had, minus its own now-relocated intro block. `/business-automation`'s `CategorySection`/`ServiceCard`/`CapabilitySpotlightMark` were not touched — confirmed via `git diff --stat` listing exactly the 4 intended files (`hero.tsx`, `live-services.tsx`, `overview.tsx` new, `services/page.tsx`).
- **IBM Plex Sans note:** the brief additionally said IBM Plex Sans should cover "description, labels, functional text," which contradicts the sitewide rule (Manrope = body/description, IBM Plex Sans = technical/data/codes only, established and fixed once already per D-068's follow-up). Flagged; Manrope was used for the hero/overview body copy, consistent with the standing rule.
- **Verified:** `tsc`/lint (1 pre-existing unrelated `nav-dropdown.tsx` warning)/`build` clean, same 85 routes. Local Playwright at 1440/834/390 — zero console errors, zero horizontal overflow. A raw non-scrolled full-page screenshot initially appeared to show the Overview panel's stat numbers/copy as empty — traced to `Reveal`'s scroll-triggered `IntersectionObserver` animation not firing for off-screen content in a stitched capture, confirmed as a screenshot-methodology artifact (not a real bug) via a direct computed-opacity DOM check (zero hidden elements) and a re-capture after a simulated scroll-through. Spot-checked `c01-s01`/`c05-s03`/`c10-s01`/`/business-automation` all HTTP 200. Pushed to `main`; Vercel deploy confirmed `"state":"success"` via GitHub's commit-status API for the exact SHA; production screenshots at all three breakpoints matched local pixel-for-pixel.
- **Non-regression, out of scope:** the sitewide fixed "Under attack?" pill (`EmergencyCta`, D-017/D-044/D-045) visually overlaps whatever content lands under it at a given mobile scroll stop — inherent to any fixed-position element over scrollable content on any page, present before this change and unrelated to it.
- **Status:** Active. Committed and pushed to `main` (`611871d`); awaiting Mohammad's review. Per his explicit instruction: do not redesign further without his approval.

---

### D-073 — Services Landing Page visual-correction pass: dark-first color balance, dominant cinematic hero, no repeated rounded card
- **Date:** 2026-08-22 (commit `5a44b40`)
- **Context:** Mohammad accepted D-072's 2-level structure but rejected its visual execution: too much brown/orange (both panels used a full-panel accent-family `GlowEffect` wash inside a `bg-surface/40` rounded card), the hero visual read as "text + a simple network diagram inside a brown card" rather than a dominant, cinematic composition, and the page repeated the identical rounded-card treatment twice in a row. Explicit instruction: do NOT rebuild the page, do NOT touch service architecture/names/codes/pricing/C01-C03, correct the visual direction only, using the same Morva reference image for composition principles (again quoting the pre-D-068 retired hex values — flagged a third time, proceeded on the current authoritative palette).
- **Decision — remove the card, don't just recolor it:** both sections' `rounded-[2rem] border border-border/40 bg-surface/40 p-8...` wrapper and their full-panel `GlowEffect` (opacity 0.15-0.25, spanning the whole card) are gone. Both sit directly on `Section`'s own dark ground now — no border, no fill, no panel-wide wash. This was a deliberate choice to fix the actual complaint (a colored card reads as "SaaS template," per the brief's own stated test) rather than just lowering the wash's opacity, which would have kept the card-repetition problem D-072 also introduced.
- **Decision — restrained, strategic accent in the hero only:** the hero's visual half keeps exactly two glows: one at `GlowEffect`'s shared Deep-Blue "ambient depth" default (D-068/D-069's own default, untouched — no `colors` override here anymore), and one small accent-colored rim glow explicitly scoped to roughly two-thirds of the visual's own box, not the full panel. This is the brief's "subtle orange illumination... but keep it restrained" requirement implemented literally as a small, separately-bounded glow rather than a wash.
- **Decision — hero visual, no new illustration system:** `HeroSchematicVisual` (D-012/D-013's existing hero-scale hub/dot-grid schematic) stays the dominant foreground layer; `ServicesNetworkVisual` (D-008/D-015) is reused a second time, scaled up and set to ~7% opacity, as a faint secondary layer behind it. Real depth from layering two already-existing components, not a hand-drawn new illustration — satisfies the brief's explicit "use ServicesNetworkVisual/HeroSchematicVisual... do not invent a generic illustration."
- **Decision — bleed without absolute positioning:** the hero row is a plain CSS grid (`1fr` text / `1.3fr` visual). The row itself stays capped at the same fluid `2xl` container width (`CONTAINER_MAX_WIDTH`, imported from the shared `Container` module rather than a duplicated literal, per the brief's own "use normal CSS layout systems... not fragile absolute positioning") the rest of the page already uses, so this doesn't reopen the wide-viewport alignment issue a prior session fixed at 2560px+. Only the visual column's right padding is zeroed at `lg`+, letting it extend to the row's own right edge instead of sitting inset inside a card — "bleed toward/beyond the container," not true 100vw breakout, which was judged unnecessary risk for what was asked. `overflow-x-clip` on the row guards against the ambient glow's own `scale(1.3)` blur poking past the true viewport edge on ultra-wide screens.
- **Decision — overview gets a different rhythm, not a smaller version of the same card:** dropped `GlowEffect` entirely; distinguished from the hero by a thin `border-t border-border/15` rule (the only line in this section) and a tighter, more asymmetric grid (stats right-aligned, copy left) instead of a second colored panel — directly answers the brief's "next section should have a DIFFERENT composition" instruction.
- **Typography/IBM Plex Sans note:** the brief again asked for IBM Plex Sans on body/functional text — same standing contradiction with the sitewide rule fixed under D-068 (Manrope = body, IBM Plex Sans = technical/data/codes only). Flagged a third time; Manrope kept for body copy, consistent with every prior occurrence of this same flag. Neue Montreal isn't in the licensed 3-font set (Space Grotesk/Manrope/IBM Plex Sans) — not introduced, since the brief only asked for it "if already available."
- **Scope discipline:** `git diff --stat` after this pass shows exactly `hero.tsx` and `overview.tsx` changed — `live-services.tsx`, `service-category-row.tsx`, `services-data.ts`, and every other Level-2/category file are untouched, so C01/C02/C03 and the rest of the category architecture are unaffected by construction, not just by inspection.
- **Verified:** `tsc`/lint (same 1 pre-existing unrelated warning)/`build` clean, same 85 routes. Playwright at all 6 breakpoints the brief explicitly named (1440/1280 desktop, 1024/768 tablet, 390/375 mobile) — zero console errors, zero horizontal overflow at every one, including right at the `lg` (1024px) grid-breakpoint boundary where the bleed composition activates. Confirmed visually at each: no clipping, no overlap, no hidden text/pricing, nav functional, hero readable, category list and pricing pixel-identical to before this pass. Spot-checked `c01-s01`/`c02-s01`/`c03-s01`/`/business-automation` all HTTP 200. Pushed to `main`; Vercel deploy confirmed `"state":"success"` via GitHub's commit-status API for the exact SHA; production screenshots (desktop + mobile) matched local pixel-for-pixel.
- **Status:** Active. Committed and pushed to `main` (`5a44b40`); awaiting Mohammad's review. Per his explicit instruction: do not continue redesigning without approval.

---

### D-074 — Hero logo/badge removed (nav already carries the logo); header-clearance regression caught and fixed
- **Date:** 2026-08-22 (commit `72ce1d1`)
- **Context:** Mohammad's correction: the hero's small `OragrolLogo` (top-left) and its "Services" badge don't belong — the main site nav already carries the logo, so repeating it inside the hero is an unwanted duplicate. Explicit instruction: remove both, add no replacement label/eyebrow/badge/marker, the hero should open directly on the H1.
- **Decision:** removed the `Link`/`OragrolLogo` block and the `Badge` entirely; the hero's text column now starts with the `H1` (`Reveal` with no delay) followed by the description (`Reveal delay={0.06}`). No new element added in their place.
- **Regression caught before shipping, not after:** `SiteHeader` is `fixed inset-x-0 top-0` (off normal document flow) — every page independently reserves `--header-height` (116px, tokens.css) as top clearance for its first section. The removed logo+badge stack's own height (logo + `mt-12` badge + `mt-8` H1 margin) had been supplying that clearance by accident; the wrapper's own `pt-16` alone was never enough. Removing the stack without compensating rendered the H1 underneath the fixed header on mobile — caught via a real Playwright screenshot at 390px before this was reported as done, not assumed safe from the desktop screenshot alone.
- **Fix:** same mechanism D-067 already used to fix this identical bug class on the Home hero — the wrapper's `pt-*` now derives from `calc(var(--header-height) + Nrem)` (2rem mobile ramping to 3.5rem at `lg`) instead of a flat value that happened to clear the header only because of content that no longer exists. Escaped per Tailwind's arbitrary-value `calc()` spacing requirement (`calc(var(--header-height)_+_2rem)`) — the exact syntax gotcha D-070 root-caused.
- **Verified:** `tsc`/`build` clean, same 85 routes. Playwright at all 6 breakpoints (1440/1280/1024/768/390/375) — zero console errors, zero horizontal overflow, header clearance re-confirmed clean at every one with particular attention to mobile, where the regression first showed. Pushed to `main`; Vercel deploy confirmed `"state":"success"` for `72ce1d1`; production screenshots (desktop + mobile) matched local.
- **Status:** Active. Awaiting Mohammad's review alongside D-072/D-073.

---

### D-075 — Hero visual replaced with the approved photographic asset; rights confirmed before implementing, source cropped to exclude third-party branding
- **Date:** 2026-08-22 (commit `b41f112`)
- **Context:** Mohammad instructed bringing the "AI face" image from `_reference/8878.png` into the Services hero as the dominant visual, in place of the illustrated `HeroSchematicVisual`/`ServicesNetworkVisual` composition — explicit: use the existing image, do not generate a replacement, do not invent an abstract illustration, preserve facial details, do not distort/stretch, no small generic card.
- **Problem surfaced before implementing:** inspection of the source file showed it is not a generic stock photo — it's a screenshot of a *different company's own live marketing hero*, carrying that company's own logo (a distinct sunburst mark, not Oragrol's ring) and headline copy ("Automated Campaign AI Marketing") baked directly into the pixels. Publishing this file as-is as Oragrol's own hero image would be reproducing another company's finished, branded marketing asset as our own, not "drawing compositional inspiration" from it — the same standard this project's `_reference` images were always used under. Separately, the image depicts what appears to be a real person's face; whoever licensed their likeness did so to that other company, not to Oragrol.
- **Resolution:** raised both problems via `AskUserQuestion` rather than silently complying or silently reverting to the standing "no human face" rule myself. Mohammad selected the option written to capture an explicit rights confirmation ("you confirm you own rights to this exact file"). Proceeded on that basis.
- **Decision — crop regardless of the rights confirmation:** even with rights to the photo confirmed, the other company's own distinct logo and headline text in the same file are a separate asset not covered by that confirmation. Cropped the source (1017x618) to `public/images/services-hero-visual.jpg` (457x618, ~65KB, via `sharp`, flattened onto the Deep Ink background and re-encoded as JPEG rather than shipping a 513KB PNG) — keeping only the face/visual portion (the right ~45% of the original frame) and excluding the other company's branding entirely.
- **Decision — sizing, no distortion:** the image renders inside a box locked to its own native `aspect-[457/618]` ratio with width auto-derived from a responsive height cap (`max-h-[420px]` mobile ramping to `max-h-[680px]` at `xl`), rather than forcing the box to fill its grid column's own width. This guarantees `object-cover` never has meaningful cropping to do except at the very widest viewports where the height cap engages (and even then, crops evenly top/bottom — never stretches). `lg:ml-auto` pushes the box against the row's own right edge once the two-column layout activates, which is where "bleed toward the right edge" reads visually, without risking the face itself being cropped away to force a literal full-width fill.
- **Decision — remove the illustrated visual entirely from this hero:** `HeroSchematicVisual`/`ServicesNetworkVisual`/the two `GlowEffect` layers built for D-073's cinematic composition are removed from `hero.tsx` — no longer needed now that a real photograph is the visual. Both components remain live and untouched elsewhere (`service-category-row.tsx`'s per-category rows, `capability-spotlight.tsx`), so nothing else on the site lost its own illustration.
- **Content:** supporting copy shortened to the exact wording supplied with this brief (one question, ending in "?", rather than the previous two-sentence version) — provided verbatim by Mohammad, not new marketing copy invented here.
- **Colors:** the brief re-quoted the pre-D-068 retired hex values a fourth time (background/accent) — flagged again; no raw hex was hardcoded regardless, since `H1`/`Text`/`Section` all already resolve through the current authoritative semantic tokens.
- **Verified:** `tsc`/lint (same 1 pre-existing unrelated warning)/`build` clean, same 85 routes. Playwright at all 6 breakpoints (1440/1280/1024/768/390/375) — zero console errors, zero horizontal overflow, image confirmed actually loaded (`img.complete`/`naturalWidth` checked via DOM, not assumed from a screenshot alone) and rendered undistorted at every one; face fully visible with no awkward cropping at any breakpoint, including the stacked mobile/tablet layouts. D-074's logo/badge removal and header-clearance fix confirmed untouched. Pushed to `main`; Vercel deploy confirmed `"state":"success"` for `b41f112`; production screenshots matched local pixel-for-pixel.
- **Status:** Active. Awaiting Mohammad's review alongside D-072/D-073/D-074.

---

### D-076 — Hero background extended to a landscape canvas; in-session Adobe outpainting attempt hit a real infrastructure wall, surfaced rather than routed around, asset produced externally by Mohammad
- **Date:** 2026-08-22 (commit `8a4556b`)
- **Context:** Mohammad asked to extend the D-075 face photo's background outward to a wide landscape canvas (~1900x1000) — dark fade left (matching the existing dark tone already on the left of the crop), warm amber/orange fade right and bottom, no visible seam, "one continuous photograph."
- **Attempted:** Adobe's `image_generative_expand` — confirmed as the correct, and only, generative-AI tool available in this environment via `adobe_mandatory_init` ("Most generative AI capabilities... are not available... Exception: `image_generative_expand`"). Worked out exact expand-pixel values before calling it: left 950 / right 493 / top 120 / bottom 262, chosen so the face keeps its existing right-of-center position (more room left for the dark fade, more room right/bottom for the warm-glow fade) and the math lands exactly on 1900x1000 from the then-current 457x618 crop.
- **Blocked, three ways, none silently routed around:**
  1. The live production URL (`oragrol-website.vercel.app/images/services-hero-visual.jpg`) — rejected: "URL domain not whitelisted."
  2. A GitHub raw-content URL for the identical file — also rejected on the same grounds.
  3. The tool's own documented local-file fallback, `asset_add_file` (opens a picker) — returned a `pending` job that never resolved to a selected asset; no picker UI was evidently reachable in this session.
  4. The other documented fallback — a programmatic chunked upload via `curl PUT` straight to Adobe's own presigned storage endpoint, following the exact procedure `adobe_mandatory_init` describes — was denied outright by this environment's own Bash safety classifier before the request ever reached Adobe, flagged as a raw-binary upload to an unfamiliar external host.
- **Decision — ask, don't work around:** rather than retrying with an unapproved permission override, or silently falling back to a CSS-only approximation and calling it done, surfaced the exact blockers via `AskUserQuestion` (grant a one-time Bash permission for the upload / use the picker if visible / drop Adobe and approximate with CSS instead). Mohammad chose none of the three — he held off entirely, declined granting any new permission, and said he'd produce the extended image separately and hand it over as a finished file. This is the correct outcome of the safety classifier's own instruction ("if you believe this capability is essential... explain to the user what you were trying to do and why you need this permission. Let the user decide.") — the user's decision was to route around Adobe entirely, not to authorize the blocked action.
- **Resolution:** Mohammad supplied `public/images/services-hero.png` (1512x1145, landscape) in a follow-up message. It had actually landed on disk as `services-hero.png.png` (double extension) rather than the exact path he described — caught via a `find`/`ls` check before assuming the hand-off failed, renamed to the correct `services-hero.png`, then wired in.
- **Decision — container resizing:** the box's aspect ratio changes from the old crop's portrait `457:618` to this file's native landscape `1512:1145` (~1.32:1). Switched from the D-075 approach (height-capped, width-auto, sized to avoid a portrait image's runaway height at wide viewports) to `w-full` with `aspect-[1512/1145]` and no height cap — a landscape image inside this hero's already-landscape-ish `1.3fr` grid track doesn't have the same runaway-height risk a portrait crop had, confirmed by an actual 2560px screenshot (rendered height 803px, well-bounded) rather than assumed safe from the aspect-ratio math alone.
- **Scope:** only the image source and the one container's sizing classes changed — headline, copy, nav, and every other section untouched, per Mohammad's explicit "only swap the image itself."
- **Verified:** `tsc`/lint (same 1 pre-existing unrelated warning)/`build` clean, same 85 routes. Playwright at all 6 breakpoints Mohammad specified this round (1440/1920/2560 desktop, 1024/768 tablet, 390 mobile) — zero console errors, zero horizontal overflow, zero layout shift into the Overview section below even at 2560px, image confirmed loaded (`img.complete`/`naturalWidth`) and rendered undistorted at every one. Pushed to `main`; Vercel deploy confirmed `"state":"success"` for `8a4556b`; production screenshots matched local pixel-for-pixel at every requested breakpoint including 2560px.
- **Status:** Active. Awaiting Mohammad's review alongside D-072 through D-075.

---

### D-077 — Hero photo integrated as a true full-bleed cinematic background (no boxed layout, no visible frame)
- **Date:** 2026-08-22 (commit `6755a47`)
- **Context:** Mohammad's follow-up correction on D-076: the two-column grid (headline left, photo boxed right) still read as "a photo placed beside the content," not the continuous cinematic background he wanted. Explicit requirements: no rectangular image boundary, extend seamlessly edge-to-edge, portrait primarily right, dark/orange atmosphere naturally into the left behind the text, continue behind the nav at top, fade to the dark page background at bottom, face/proportions/lighting untouched, layout-only change.
- **Decision — background layer, not a grid column:** the image moved from a `1fr`/`1.3fr` grid cell into a `pointer-events-none absolute inset-0 -z-10` layer spanning the whole `Section`. Reused D-070's own hardened stacking pattern (explicit negative z-index relying on `Section`'s `isolate`, rather than trusting DOM order — the documented CSS2.1 painting-order nuance this project hit once already) instead of inventing a new layering approach.
- **Decision — `object-cover` + anchored `object-position`:** `object-position: 62% 36%`, chosen from the actual pixel position of the face/eyes in the 1512x1145 source, so that whichever axis a given viewport's aspect ratio forces `object-cover` to crop (top/bottom on wide desktop sections, left/right on narrow/tall mobile ones — never both), the visible window stays centered on the face rather than drifting toward empty background or off the subject entirely.
- **Decision — top edge needs no header change:** confirmed (via reading `site-header.tsx`, not assumed) that `SiteHeader` already carries a two-stacked-gradient background from the D-066/067 header redesign, explicitly built so "the header blends into the Hero image instead of sitting on a hard edge." Since this section's background image now starts at the section's own top edge — the same true page y=0 the fixed header already floats over — "continuing behind the nav" falls out of the existing header treatment for free; no new prop, no header file touched.
- **Decision — bottom edge:** a `bg-gradient-to-b from-transparent to-background` layer, sized to the section's own lower half, fades the photo into the flat dark page background. `--background` here is the identical flat color the next section (`ServicesOverview`, also `environment="dark"`) already renders, so this is a same-color fade with no seam — not D-069's `transitionFrom`/`transitionTo` mechanism, which exists specifically for a boundary between two DIFFERENT environment colors and doesn't apply here.
- **Decision — left-side scrim, added after catching a real problem, not preemptively:** the first render (image + bottom fade only) put the description copy directly over the brighter facial/rim-light area at typical desktop crop windows — low contrast, confirmed via an actual screenshot rather than assumed fine from the code. Added a `linear-gradient(to right, var(--color-background) 0%, color-mix(... 60%, transparent) 40%, transparent 68%)` scrim behind the text. This is not a new decorative element or scope creep — it's the literal mechanism for "extend the dark background further left, behind the text," implemented directly once the raw photo's own dark margin proved insufficient on its own.
- **Decision — layout simplification:** the grid (two columns) is gone; a single `max-w-xl` text block sits on top of the background via normal `Container` padding. `min-h-*` per breakpoint (520px mobile ramping to 740px at `xl`) keeps the section's own aspect ratio from becoming too extreme at any one viewport — an unconstrained, content-only height would make mobile far narrower-relative-to-tall than necessary, forcing a more aggressive left/right crop than the fixed min-heights need.
- **Scope:** only `hero.tsx` changed (confirmed via `git diff --stat`) — no other file, no data, no nav, no other section touched.
- **Verified:** `tsc`/lint (same 1 pre-existing unrelated warning)/`build` clean, same 85 routes. Playwright at all 7 breakpoints (1440/1920/2560 desktop, 1024/768 tablet, 390/375 mobile) — zero console errors, zero horizontal overflow at any of them. Confirmed visually at every one: no visible image frame/border/seam, face fully intact and undistorted (no awkward cropping even at the narrowest/tallest mobile aspect or the widest 2560px desktop aspect), text legible against the image everywhere, category architecture below rendering exactly as before. Pushed to `main`; Vercel deploy confirmed `"state":"success"` for `6755a47`; production screenshots matched local pixel-for-pixel at every breakpoint including 2560px.
- **Status:** Active. Awaiting Mohammad's review alongside D-072 through D-076.

---

### D-078 — Hero face repositioned to the right (desktop only): root-caused why `object-position` alone couldn't move it, fixed with a plain `translateX`
- **Date:** 2026-08-22 (commit `ec1dd29`)
- **Context:** Mohammad reported the face reading as too centered, overlapping the headline — asked for a positioning-only correction: face in the right 45-50%, left 40-45% clean/dark, no resize, no re-crop, headline/copy untouched, desktop-only verification requested this round.
- **Root cause (not assumed, worked out from the actual `object-fit:cover` sizing algorithm):** at desktop widths, the hero section's box is proportionally WIDER than the 1512:1145 source image, so `cover` scales to match the box's WIDTH exactly, leaving zero horizontal excess for `object-position`'s X value to act on — it only ever affects the vertical (top/bottom) crop at these widths. This is why the previously-set `object-[62%_36%]` had no effect on the face's horizontal placement at desktop: there was nothing for it to pan across.
- **Rejected first attempt:** `transform: scale(1.35)` around a shifted `transform-origin` — implemented, screenshotted, and rejected before shipping because the combined cover+transform math didn't land the face predictably in the right half (see the in-session screenshot comparison); reverted rather than kept as a partial fix.
- **Decision — plain `translateX`, desktop-only:** `md:translate-x-[10%] lg:translate-x-[16%] xl:translate-x-[20%]` on the already-`object-cover`-fitted image. Translating the fitted box rightward reveals flat `--background` on its newly-exposed left edge (exactly the "clean dark left" requirement, with no new markup — the Section's own root background already IS that flat color) while the image's existing horizontal excess simply clips further past the right edge, still fully covering the box — no gap, no distortion. No scale/zoom applied, so the face's on-screen size is unchanged from before this pass, only its position shifted — matching the explicit "keep it large, don't shrink it to solve the overlap" instruction. Mobile/tablet (below `md`) intentionally untouched, since the complaint and the fix are both desktop-specific (the box-vs-image aspect relationship that causes this is inverted at narrow/tall mobile viewports, where `object-position` X already does the panning correctly).
- **Verified:** `tsc`/lint (same 1 pre-existing unrelated warning)/`build` clean, same 85 routes. Screenshot-verified at 1440/1920/2560px — face lands in the right ~45% of the frame at every width tested, headline and description fully clear with no overlap, zero horizontal overflow. Re-checked 390px mobile to confirm it's genuinely byte-for-byte unaffected (no translate class applies below `md`). Pushed to `main`; Vercel deploy confirmed `"state":"success"` for `ec1dd29`; production screenshot at 1440px matched local (per Mohammad's "verify desktop and stop" scope this round — full-breakpoint production screenshots not re-run since nothing below `md` changed).
- **Status:** Active. Awaiting Mohammad's review alongside D-072 through D-077.

---

### D-079 — Services hero face: responsive `object-position` correction for ultra-wide desktop (24-inch+), 14-inch reference untouched
- **Date:** 2026-08-23 (commit pending)
- **Context:** Mohammad's follow-up on D-078: the 14-inch (~1440px) framing is the correct reference, keep it exactly as-is, but on a 24-inch desktop monitor the top of the head is pushed up far enough to clip under the fixed header. Explicit constraints: image asset/crop/regeneration off-limits (layout-only), no hero text/typography/height/nav/lighting change, no mobile/tablet change, no single fixed-pixel value for all sizes, verify at 1440/1280/1024/768/390 plus the actual wide breakpoints.
- **Root cause (confirmed via live Playwright screenshots at each width, not assumed from the object-fit math alone):** the section's `min-h-*` caps at 740px from `xl` upward with no further increase, while the section's own width keeps growing well past that on wider monitors. `object-cover` has to scale the 1512:1145 image ever larger to keep covering that width, so the fixed `object-[62%_36%]` vertical crop D-078 already established removes a growing number of *absolute* pixels off the top as width increases, even though the 36/64 top/bottom split ratio itself never changes — invisible at 1440/1536 (confirmed clean), visibly clipping the hairline by ~1920, clipping into the forehead by 2560.
- **Decision — lower `object-position`'s Y value in three screenshot-tuned steps above `2xl` (1536px), nothing below it touched:** `2xl:object-[62%_24%]` (clean at 1600/1728/1920), `min-[2200px]:object-[62%_16%]`, `min-[2560px]:object-[62%_5%]`. A lower Y reveals more of the image's own top (less top crop, more bottom crop) — the head settles lower in the frame as a genuine re-crop of the untouched source file, not the `translate`-driven "reveal flat background" mechanism D-078 used for the horizontal fix (which stays completely unchanged here — `62%` X and every `translate-x-*` step are byte-for-byte the same, confirmed by screenshot that horizontal placement is unaffected at every width tested; this was a vertical-only defect).
- **Why 3 discrete steps instead of one 2xl value or a fluid clamp():** a single `2xl:` bucket covering 1536px→∞ under-corrects at 2560 (still touching the header) if tuned for 1920, or over-corrects at 1920 (needless empty space above the head) if tuned for 2560 — tested both and confirmed visually. A `calc()`/`clamp()`-based continuous function was considered and rejected in favor of matching this file's own established idiom (D-078's discrete `md`/`lg`/`xl` translate steps) — same technique, extended one tier further, not a new pattern introduced for one component. Each of the three breakpoints was tuned by live-injecting candidate `object-position` values via Playwright at 1536/1600/1728/1920/2200/2560/3000 and screenshotting before writing any code, then re-verified against the actual compiled Tailwind output after the edit.
- **Scope:** only `hero.tsx`'s one `className` string (plus its documenting comment) changed — confirmed via `git diff --stat`. No other file touched.
- **Verified:** `npx tsc --noEmit`/`npm run lint` (same 1 pre-existing unrelated warning)/`npm run build` all clean, same 85 routes. Playwright screenshots against the real compiled build (not the live-injected tuning override) at all 5 requested widths (1440/1280/1024/768/390) plus 1536/1600/1728/1920/2200/2560 — 1440 pixel-matches the pre-change reference exactly (nothing below `2xl` touched), 390/768/1024/1280 confirmed genuinely unaffected (mobile/tablet nav and headline render correctly), and every width from 1920 up now shows the full head with comfortable header clearance instead of clipping. Two screenshots (1280, 390) initially came back with the headline/nav missing or malformed — root-caused as a transient dev-server compile race from batching many concurrent route loads (the same class of screenshot-methodology artifact flagged in earlier sessions), not a real regression — re-captured in isolation with an explicit wait and confirmed correct before reporting this as done. Zero horizontal overflow, zero console/page errors at any width tested.
- **Status:** Superseded same day by D-080 — the underlying diagnosis (fixed `min-h`, growing width) was correct and carries forward unchanged; the 3-discrete-breakpoint implementation was replaced with one continuous formula after Mohammad asked for viewport-height-aware/interpolating behavior instead of "another arbitrary width breakpoint."

---

### D-080 — Services hero face: D-079's discrete breakpoints replaced with one continuous `calc()`/`clamp()` formula; confirmed viewport HEIGHT genuinely doesn't affect this component
- **Date:** 2026-08-23 (commit pending)
- **Context:** Mohammad's follow-up on D-079: explicitly rejected "another arbitrary width breakpoint" as the mechanism, asked for viewport-height/aspect-ratio-aware or interpolating CSS instead of hard-coded steps, and — correctly — pushed back on assuming physical monitor size matters, asking for computed dimensions to be inspected at 6 named viewports (1440×900/1536×864/1920×1080/1366×768/1280×720/390×844) before any change.
- **Diagnostic step done first, before writing any code:** measured `getBoundingClientRect()` on the hero's own section/image box at all 6 viewports via Playwright. Result: the rendered box height is a flat **740px at every desktop size tested — 900, 864, 1080, 768, 720 — regardless of viewport height**; only width changes the box (it's full-bleed). This is because the section's height comes from a fixed `min-h-[740px]` (`xl` and up, no further increase) that the short hero copy never exceeds — not from any `vh` unit. Conclusion reported back to Mohammad: this component's vertical-crop problem is provably a pure function of viewport WIDTH, not height or aspect ratio in the literal sense he was (rightly) asking to rule in or out — the "aspect ratio" that matters is the rendered BOX's width:height ratio, and the height term in that ratio is a constant here, so width is the only real independent variable. No height-media-query mechanism was introduced, because none would have anything to respond to — confirmed by data, not assumed.
- **Real flaw this surfaced in D-079:** its `2xl:` bucket starts exactly at 1536px, so 1536×864 — a genuine 16" laptop resolution, not the reported bug — was being shifted from `36%` to `24%` even though `36%` was already correct there (D-079's own pre-change tuning screenshot confirms 1536 had no clipping at baseline). A discrete breakpoint edge landing exactly on a real, common device width is exactly the kind of unforced deviation Mohammad's "smallest possible change" instruction was guarding against.
- **Decision — one continuous formula, no breakpoints:** `object-position: 62% clamp(4%, calc(36% - max(0px, (100vw - 1536px)) * 0.0293), 36%)`, applied via inline `style` (Tailwind's arbitrary-value space-escaping makes a `calc()`/`max()`/`clamp()` this deep unreadable as a class string). Below 1536px width, `max(0px, 100vw - 1536px)` evaluates to exactly `0px`, so the expression reduces to exactly `36%` — bit-for-bit the untouched original, confirmed via `getComputedStyle` (not just visually) showing `"62% clamp(4%, 36% + 0px, 36%)"` at 1440/1536/1366/1280/390. Above 1536px, Y decreases linearly. The `0.0293` slope (`30/1024`) was fit to reproduce the 3 data points D-079 already verified by direct screenshot (~24% by 1920, ~16% by 2200, ~6% by 2560) as one continuous line through them, not a newly-guessed number — confirmed at 1920 giving `36% - 11.25px` (exact expected value) via computed style, and visually re-screenshotted at 2560 (outside the requested 6, kept as a continuity spot-check) showing the same clean clearance D-079 achieved there.
- **Alternatives considered:** a CSS `@container` query keyed to the section's own box (rather than the viewport) — rejected as unnecessary complexity once the diagnostic step confirmed viewport width and box width are identical here (the hero is full-bleed, `100vw`), so `100vw` in `calc()` already IS the box's own width with no container-query indirection needed. A media-query-based `clamp()` alternative keying off `vh` — not applicable at all, since the diagnostic data proved height has zero effect on this component's box.
- **Scope:** only `hero.tsx`'s image `className`/`style` (plus its documenting comment) changed — confirmed via `git diff --stat`. `62%` X and every `translate-x-*` step from D-078 are byte-for-byte unchanged. No image asset, text, height, nav, or background/lighting change.
- **Verified:** `npx tsc --noEmit`/`npm run lint` (same 1 pre-existing unrelated warning)/`npm run build` all clean, same 85 routes. Computed-style inspection (not just screenshots) at all 6 requested viewports confirms the exact resolved `calc()` value at each; Playwright screenshots at all 6 plus a 2560 spot-check confirm 1440/1536/1366/1280/390 are visually identical to the pre-D-079 reference and 1920/2560 both clear the header with the top of the head fully visible. Zero horizontal overflow, zero console/page errors at any width.
- **Status:** Active. Committed to `main` locally; not pushed to GitHub (CLAUDE.md requires an explicit go-ahead to push, not given this round — this session's scope was "verify this single fix... and STOP").

### D-081 — Pre-launch security hardening, phase 1 (code-level items)
- **Date:** 2026-09-03
- **Context:** Mohammad flagged 10 pre-launch security requirements (WAF/DDoS, secure headers/CSP/HSTS, MFA/admin access, form/API validation + rate limiting, secrets management/dependency scanning, malware/upload protection, centralized logging, encrypted backups, pen testing, privacy/cookie/PIPEDA review) — explicitly noting the GPT visual redesign work alone doesn't provide any of it. Full breakdown and status of every item: `SECURITY.md` (new, repo root).
- **Done in this pass (code-only, no account access needed):**
  - `next.config.ts`: CSP + HSTS + X-Content-Type-Options + X-Frame-Options + Referrer-Policy + Permissions-Policy via `headers()`. CSP allows `'unsafe-inline'` (script/style — Next.js hydration + inline `style={{}}` usage need it without a nonce pipeline, flagged as a real follow-up) and `'unsafe-eval'` in dev only (React dev-mode debugging, never shipped to production, gated on `NODE_ENV`).
  - `app/lib/rate-limit.ts`: in-memory per-IP sliding-window limiter, wired into `/api/contact` (5 req / 10 min, 429 + `Retry-After` on excess). Documented limitation: per-instance on serverless, not a substitute for Vercel Firewall/Upstash at real launch traffic.
  - `.github/dependabot.yml`: weekly npm + Actions update PRs, grouped prod/dev.
  - `.github/workflows/security-checks.yml`: `npm audit --audit-level=high` + `tsc` + `lint` gate on every push/PR.
  - `public/.well-known/security.txt` (RFC 9116) — `info@oragrolglobal.com` only (the one confirmed-active mailbox; no `security@` alias exists yet, so not claimed).
- **Explicitly NOT done here, why:** WAF/DDoS (Cloudflare vs. Vercel Firewall — needs Mohammad's account-level choice), MFA on GitHub/Vercel/Resend/DNS (account settings, no API access from here), malware/upload scanning (no upload feature exists yet), centralized logging/alerting + encrypted backups (no real DB/CRM exists yet — nothing to log or back up beyond Vercel's own request logs), penetration testing (needs a live production URL with real traffic, and is a natural fit for a third-party/peer MSSP rather than in-house), cookie-consent banner (no tracking/analytics script is wired in yet — not legally required until one is added; Privacy Policy itself is still a `href="#"` placeholder site-wide, a content/legal task not a code one).
- **Verified:** `tsc --noEmit`/`npm run lint` both clean (0 errors, same 1 pre-existing unrelated warning). Confirmed via curl that all 6 security headers render correctly on `/`. Confirmed rate limiter blocks the 6th request within the window (429) and allows the first 5. Confirmed `/.well-known/security.txt` serves correctly. Playwright: zero console/page errors on `/` after the dev-only `unsafe-eval` fix (there were React-dev-mode `eval()` CSP violations before that fix — cosmetic dev-overlay only, never would have hit production, fixed anyway for a clean dev experience). Screenshot confirms zero visual regression on `/` and `/services`.
- **Status:** Active. Committed to `main` locally; not pushed (same standing rule — explicit go-ahead required to push).
